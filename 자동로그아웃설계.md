# 관리자 자동 로그아웃 — 설계안 (보류)

> **상태: 만들지 않았습니다.** 혼자 쓰는 동안에는 필요가 크지 않아 미뤘습니다.
> **직원이 생기면 이 문서대로 만듭니다.**
>
> 작성 2026-08-25 · 당시 기준 커밋 `4d64016`
> 관련: 관리자 로그인을 이메일+비밀번호로 바꾸는 작업의 3단계 앞쪽 절반
> (뒤쪽 절반인 "로그인 기록"은 이 문서에 없습니다 — 따로 진행)

---

## 1. 왜 필요한가 — 지금 상태

관리자로 들어오는 길이 둘인데, **끝나는 방식이 서로 다르고 둘 다 허술합니다.**

### 옛 길 — 비밀번호 쿠키 (`jzl_admin_session`)

`lib/admin-auth.ts`

```
SESSION_MAX_AGE = 60 * 60 * 24 * 7;        // 7일
expiresAt = now + SESSION_MAX_AGE          // 로그인 순간에 도장 한 번
```

로그인할 때 "7일 뒤"를 찍고 끝입니다. 쓰든 안 쓰든 7일입니다.
**자리를 비웠는지는 전혀 보지 않습니다.**

### 새 길 — Supabase 세션

`middleware.ts` · `lib/admin-guard.ts`

**끊는 장치가 한 줄도 없습니다.** 미들웨어가 요청마다 `getUser()` 를 부르는데,
이게 만료된 토큰을 **자동으로 갱신**합니다. 브라우저를 지우지 않으면 계속 관리자입니다.

> 카페 노트북에서 한 번 로그인하면 그 노트북은 계속 관리자입니다.

작성 시점에 `lib/` · `app/` · `middleware.ts` 전체에서 유휴·idle 관련 코드를 찾아봤습니다 — **0건**.

---

## 2. 핵심 결정 — 시계는 브라우저가, 자물쇠는 서버가

처음에는 "서버에 요청이 올 때마다 연장" 으로 설계했다가 **버렸습니다.**

> 상품 설명을 30분 쓰는 동안에는 서버에 요청이 안 갑니다.
> 자리에 앉아 있는데 유휴로 잡힙니다.

그래서 **화면 안의 실제 움직임**(마우스·키보드·터치·스크롤)으로 셉니다.

```
브라우저 (화면 안)                      서버
─────────────────────                  ──────────────────
마우스·키보드·터치·스크롤   ──┐
  → 변수 하나만 갱신           │  (요청 0번)
                               │
15초마다 한 번 확인 ───────────┤
  · 활동 있었고 10분 지났나?  ─┼──▶ 연장  ──▶ 쿠키에 "방금 움직임" 다시 서명
  · 55분째 조용한가?          ─┤   (화면에 "곧 잠깁니다" 안내)
  · 60분째 조용한가?          ─┼──▶ 잠금  ──▶ 쿠키 삭제
                               │
비밀번호 넣고 열기 ────────────┴──▶ 해제  ──▶ 비번 확인 후 쿠키 재발급
```

### 왜 둘 다 필요한가 (제일 중요)

- **덮개만 있으면 장식입니다.** 노트북을 가져간 사람이 새 탭에서 `/admin/orders` 를
  그냥 열면 덮개 없는 화면이 뜹니다. 그래서 잠기는 순간 **서버 쿠키를 실제로 지웁니다.**
- **서버만 있으면 쓰던 게 날아갑니다.** 로그인 페이지로 튕기면 작성 중이던 내용이 사라집니다.

---

## 3. 시간

| | 값 | 누가 판단 |
|---|---|---|
| 잠금 | 조용한 지 **60분** | 브라우저 (정확) |
| 경고 | 조용한 지 **55분** | 브라우저 |
| 절대 | 로그인한 지 **7일** | 서버 |
| 서버 뒷문 | 마지막 연락 + **70분** | 서버 |

### "서버 뒷문 70분" 이 왜 필요한가

서버는 10분에 한 번만 소식을 듣습니다. 그래서 서버가 아는 "마지막 움직임" 은
실제보다 **최대 10분 낡았습니다.** 60분으로 딱 자르면 **실제로는 50분 만에 잠기는**
일이 생깁니다. 그래서 서버는 10분 더 여유를 둡니다.

- 브라우저가 살아 있으면 → **60분에 정확히** 잠깁니다 (브라우저가 "잠금" 을 보내니까)
- 브라우저를 강제 종료했거나 인터넷이 끊겼으면 → 서버가 **70분**에 막습니다
- **최악의 경우가 70분입니다.** 이게 정직한 숫자입니다.

절대 7일은 지금 옛 쿠키의 `SESSION_MAX_AGE` 7일과 값이 같아, 따로 설명할 게 없습니다.

---

## 4. 움직임 세기 — 요청을 보내지 않습니다

```
mousemove · keydown · touchstart · scroll · wheel · click
   → lastActivity = 지금 시각        ← 이게 전부
```

- 변수 하나에 숫자를 넣는 것뿐입니다. **React state 를 건드리지 않아 화면을 다시 그리지 않습니다.**
  초당 60번이 들어와도 부담이 없습니다. (`{ passive: true }` 로 붙일 것)
- 판단은 **15초에 한 번 도는 타이머**가 혼자 합니다.
- `visibilitychange` — 탭을 다시 켜면 즉시 한 번 봅니다.
  **노트북 덮개를 3시간 닫아 뒀다면 켜는 즉시 잠깁니다.**
  (시각을 비교하므로 잠든 시간도 그대로 세어집니다)

### 서버에 말을 거는 순간 — 딱 세 가지

| 언제 | 무엇 | 빈도 |
|---|---|---|
| 움직임이 있는데 마지막 연락이 10분 넘었을 때 | 연장 | **최대 10분에 1번** |
| 60분째 조용할 때 | 잠금 | 잠길 때 1번 |
| 덮개에서 비밀번호를 넣었을 때 | 해제 | 열 때 1번 |

하루 8시간 내내 일해도 **연장 요청 48번**입니다.

---

## 5. 잠금 덮개

- `AdminShell` 안에 `position: fixed` 덮개. **화면을 갈아치우지 않습니다.**
  쓰던 상품 설명은 덮개 아래 그대로 살아 있습니다.
- 여는 법: **비밀번호 한 칸.** 서버가 알아서 판단합니다.
  - 이메일로 들어왔으면 → 그 **Supabase 계정 비밀번호**.
    이메일은 아직 살아 있는 Supabase 세션에서 읽어 `admin@… 님` 으로 자동으로 채웁니다.
  - 옛 비밀번호로 들어왔으면 → **`ADMIN_PASSWORD`**.
  - 이메일을 못 찾는 경우에만 이메일 칸이 함께 나타납니다.
- **시도 제한 5회/분** (`lib/rate-limit.ts` 재사용, 키 `admin-unlock:{IP}`).
  안 걸면 이 덮개가 비밀번호를 무한히 시험하는 창구가 됩니다.
- 아래쪽에 작게 **"로그아웃하고 로그인 화면으로"**.
  비밀번호가 기억 안 날 때 갇히지 않게. 누르면 쓰던 게 사라진다고 안내하고 한 번 더 묻기.
- 해제 성공 시 `loginAt` 을 지금으로 새로 찍습니다. (비밀번호를 다시 넣은 것이므로 새 세션)

---

## 6. 5분 전 안내

- 55분째에 화면 오른쪽 아래 작은 띠: **"5분 뒤 화면이 잠깁니다 · 계속하기"**
- **마우스를 움직이거나 아무 키나 누르면 그냥 사라지고** 시간이 다시 세어집니다.
  그 순간 연장도 함께 보냅니다.
- "계속하기" 버튼은 같은 일을 하는 확실한 손잡이입니다.

---

## 7. 탭을 여러 개 열어 두었을 때 ⚠️

주문 탭에서 일하고 상품 탭은 열어만 뒀다면,
**상품 탭이 60분 뒤 혼자 "잠금" 을 보내서 주문 탭까지 끊어버립니다.**

→ 마지막 움직임 시각을 **localStorage 에 함께 적습니다** (10초에 한 번만).
다른 탭이 `storage` 이벤트로 그걸 읽으므로 **어느 탭에서 움직이든 전부 연장**됩니다.
`AdminShell` 이 이미 `jzl-admin-nav-open` 으로 쓰는 방식이라 새로 들여올 게 없습니다.

---

## 8. 검사를 넣어야 할 곳 — 두 군데 ⚠️

작성 시점에 확인한 사실:

- `middleware.ts` 의 matcher 는 **`/admin/:path*` 만** 봅니다. `/api/admin/*` 는 **안 봅니다.**
- `/api/admin/*` 와 서버 액션은 2단계에서 하나로 모은 **`lib/admin-guard.ts` 의 `isAdmin()`** 이 지킵니다.

> **한 곳만 넣으면 덮개가 씌워진 채로 API 를 직접 불러 상품을 지울 수 있습니다.**
> 반드시 `middleware.ts`(화면)와 `lib/admin-guard.ts`(API·액션) **양쪽**에 넣어야 합니다.

또 하나 확인한 것: **관리자 화면에 주기적 요청(폴링)이 없습니다.**
사이드바 숫자는 화면 열 때 한 번만 셉니다. 폴링이 있었다면 백그라운드 탭이
저절로 세션을 연장해 이 기능이 조용히 무력화됐을 것입니다.
**나중에 폴링을 넣게 되면 이 설계를 다시 봐야 합니다.**

---

## 9. `ADMIN_SESSION_SECRET`

새 쿠키(`jzl_admin_seen`)의 서명 열쇠입니다. `openssl rand -hex 32` 형식(16진수 64자).

**Vercel 넣는 법**

1. Vercel → `jzl-closet` → Settings → Environment Variables
2. Add New — Key `ADMIN_SESSION_SECRET`, Value 64자,
   Environments **Production · Preview · Development 셋 다 체크**
3. Save
4. **Deployments → 맨 위 → ⋯ → Redeploy** ← 이걸 해야 반영됩니다

**비워 두면** `ADMIN_PASSWORD` 를 대신 씁니다.
→ 환경변수를 안 넣고 배포해도 잠기는 사고가 없습니다. (`ORDER_TOKEN_SECRET` 과 같은 방식)

> ⚠️ **하지만 반드시 넣어야 합니다.**
> 안 넣으면 자동로그아웃 쿠키까지 `ADMIN_PASSWORD` 로 서명됩니다.
> 그러면 **관리자 비밀번호를 바꿀 때마다 이메일로 들어와 있던 사람까지 전부 로그아웃**됩니다.
> 지금(이 기능 없을 때)은 끊기는 게 옛 쿠키뿐이라 감당되지만, 만든 뒤에는 그렇지 않습니다.

---

## 10. 손댈 파일 (9개 · 새 파일 2개)

| 파일 | 무엇 |
|---|---|
| `lib/admin-auth.ts` | 새 쿠키 서명·검증, 시간 상수 (기존 `sign()` 재사용) |
| `middleware.ts` | `/admin/*` 화면 요청에서 유휴·절대 검사 |
| `lib/admin-guard.ts` | `isAdmin()` 에도 같은 검사 ← **API·액션 구멍 막기** |
| `app/api/admin/login/route.ts` | 로그인 성공 시 새 쿠키 발급 |
| `app/admin/login-actions.ts` | 이메일 로그인 시 발급 + 잠금해제 액션 |
| 🆕 `app/api/admin/session/route.ts` | 연장 · 잠금 |
| 🆕 `components/admin/AdminIdleGuard.tsx` | 움직임 감지 · 경고 띠 · 덮개 |
| `components/admin/AdminShell.tsx` | 덮개 붙이기 |
| `.env.local.example` | 새 환경변수 설명 |

---

## 11. ⚠️ 배포하면 생기는 일

**그 시점에 로그인돼 있던 상태가 한 번 풀립니다.**
새 쿠키가 없는 세션은 "잠긴 것" 으로 봐야 하기 때문입니다.

여기서 타협하면 안 됩니다 — **"쿠키가 없으면 그냥 통과" 로 하면
쿠키를 지우기만 해도 자동 로그아웃을 통째로 건너뛸 수 있습니다.**
그러면 만든 의미가 없습니다. 배포 후 한 번만 다시 로그인하면 됩니다.

---

## 12. 만들기 전에 다시 정할 것

1. **연장 주기 10분** — 짧게 하면 서버가 아는 시각이 정확해지지만 요청이 늘고,
   길게 하면 "뒷문 70분" 이 더 길어집니다. 10분이 균형점.
2. **덮개의 "로그아웃하고 나가기" 링크** — 갇힘 방지용이지만 잘못 누르면 쓰던 게 날아감.
3. **유휴 60분** — 직원이 여럿이면 더 짧게(30분) 가는 것도 검토.
4. 직원이 생기면 `ADMIN_EMAILS` 에 사람이 늘어납니다.
   그때는 **누가 언제 들어왔는지(로그인 기록)** 가 이 기능보다 먼저입니다.

---

## 13. 만들 때 순서

**① 서버 쪽** (쿠키 · 두 곳의 검사 · 연장/잠금 창구)
→ 여기까지만 해도 "70분 뒤 막힘" 은 동작합니다. 다만 로그인 화면으로 튕깁니다.

**② 화면 쪽** (움직임 감지 · 경고 띠 · 덮개)
→ 튕기는 대신 덮개가 씌워집니다.

중간에 한 번 확인하고 넘어갈 것.
