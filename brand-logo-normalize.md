# 브랜드 로고 크기 균일화

## 0. 작업 전 반드시 지킬 것

- **추측하지 말고 실제로 확인해줘.** 파일 경로, 테이블 컬럼명, 컴포넌트 이름을 기억이나 관례로 짐작하지 말고 반드시 코드를 열어서 확인한 뒤 작업할 것.
- 확실하지 않은 부분은 임의로 결정하지 말고 **`## 질문` 섹션에 모아서 남길 것.** 추측해서 진행한 코드는 나중에 어디가 깨졌는지 못 찾는다.
- 이 작업은 **이미지 처리와 화면 표시만** 건드린다. 주문·결제·재고 관련 코드는 한 줄도 수정하지 말 것.
- 기존 원본 로고 파일은 **절대 덮어쓰지 말 것.** 되돌릴 수 없게 된다.

---

## 1. 문제

브랜드 로고 17개가 화면에서 제각각 크기로 보인다.

- GANNI, COMME des GARÇONS, Calvin Klein → 너무 크게 보임
- MAISON KITSUNÉ, ARC'TERYX, POLO RALPH LAUREN → 너무 작아서 안 보임

원인은 **원본 파일과 화면 CSS가 둘 다 높이 기준이기 때문**입니다. 실제로 17개 파일을 열어 확인한 결과입니다.

1. **원본 파일이 전부 세로 400~420px로 통일되어 있습니다.** 이미 높이 기준으로 맞춰진 파일들입니다.
2. 화면 CSS도 높이 기준이면 **높이는 같아지고 가로 비율 차이만 그대로 남습니다.**

```
크게 보임    꼼데가르송 9.27:1   캘빈클라인 6.41:1   가니 3.93:1
안 보임      메종키츠네 1.97:1   폴로 1.83:1   아크테릭스 1.64:1
```

같은 높이에서 꼼데가르송은 가로로 9배 뻗고, 아크테릭스는 새 그림과 글자가 위아래로 쌓여 있어 글자가 전체 높이의 3분의 1밖에 차지하지 못합니다.

**여백은 원인이 아닙니다.** 17개 중 16개가 트림해도 크기가 그대로입니다(잘라낼 여백이 없음). 트리밍은 앞으로 올라올 파일을 위한 안전망으로만 넣습니다.

**화면 CSS가 실제로 높이 기준인지 코드로 확인하세요.** 위는 파일 실측에서 나온 추론입니다.

---

## 2. 해결 방향

**높이가 아니라 면적(가로×세로) 기준으로 맞춘다.**

사람 눈은 로고의 "높이"가 아니라 "차지하는 면적"으로 크기를 인식한다. 그래서 가로로 긴 로고는 높이를 낮추고, 정사각형 로고는 높이를 키워야 서로 같은 무게로 보인다.

구현은 **CSS가 아니라 이미지 자체에 굽는 방식**으로 한다. 모든 로고를 동일한 크기의 투명 캔버스 안에 면적 기준으로 배치해서 저장하면, 화면에서는 그냥 같은 박스에 `object-contain`으로 넣기만 하면 된다. 브랜드별 예외 CSS가 필요 없어진다.

처리 순서:

```
원본 → ① 여백 자동 트리밍 → ② 면적 기준 크기 계산 → ③ 고정 캔버스 중앙 배치 → ④ WebP 저장
```

---

## 3. 이미지 처리 로직

### 3-1. 위치

`sharp`를 쓰는 기존 R2 업로드 유틸이 이미 있다. **실제 파일 경로를 찾아서 확인할 것.** (`lib/` 아래 R2 업로드/WebP 변환 코드) 새로 만들지 말고 기존 유틸 옆에 함수를 추가한다.

새 함수 이름: `normalizeBrandLogo(inputBuffer: Buffer): Promise<Buffer>`

### 3-2. 상수

```ts
const CANVAS_W = 800;              // 캔버스 가로
const CANVAS_H = 360;              // 캔버스 세로
const TARGET_AREA = 55000;         // 로고가 차지할 목표 면적 (px²)
const MAX_W_RATIO = 0.92;          // 캔버스 가로의 92% 초과 금지
const MAX_H_RATIO = 0.85;          // 캔버스 세로의 85% 초과 금지
```

**이 값은 임의로 바꾸지 마세요.** 실제 17개 파일로 계산해 상한에 걸리는 로고가 하나도 없는 조합을 찾은 것입니다.
600×300(2:1)으로 하면 꼼데가르송(9.27:1)·아미(8.79:1)·캘빈클라인(6.41:1)이 폭 상한에 걸려 목표 면적에 도달하지 못하고, 고친 뒤에도 여전히 작게 보입니다.

### 3-3. ① 여백 트리밍

**현재 등록된 로고에는 잘라낼 여백이 거의 없습니다.** 앞으로 올라올 파일을 위한 안전망입니다. 트리밍 때문에 오히려 이미지가 망가지지 않게 하는 쪽이 더 중요합니다.

`sharp`의 `.trim()`을 쓰되, **알파 채널 유무에 따라 분기하세요:**

- `metadata.hasAlpha === true` → 투명 영역 기준으로 트리밍
- 알파 없음 → **트리밍하지 마세요.** 원본 그대로 씁니다

⚠️ **알파 없는 파일을 모서리 색 기준으로 트리밍하면 안 됩니다.**

파타고니아 로고가 그런 경우입니다. 산 실루엣이 들어간 사각형 자체가 정식 로고라 배경이 아닙니다. 모서리 기준으로 자르면 **로고를 잘라내게 됩니다.** 알파가 없으면 건드리지 마세요.

트리밍 결과가 **원본의 5% 미만으로 줄어들면 실패로 간주**하고 원본을 그대로 씁니다.

트리밍 후 실제 가로 `w`, 세로 `h`를 다시 측정합니다.

### 3-4. ② 면적 기준 크기 계산 (+ 잉크 밀도 보정)

**면적만 맞추면 부족합니다.** 로고마다 실제로 칠해진 부분의 비율이 크게 다릅니다.

```
파타고니아   100%  속이 꽉 찬 사각형
폴로랄프로렌   9%  가는 선뿐
```

같은 면적이면 파타고니아가 11배 무겁게 보입니다. 그래서 **잉크 밀도를 재서 보정**합니다.

**잉크 밀도 측정** — 트리밍된 영역에서 알파값이 25를 넘는 픽셀의 비율입니다.
알파 채널이 없는 파일(파타고니아)은 `1.0`으로 봅니다.

```ts
const F_REF = 0.30;   // 기준 밀도

// 잉크가 옅으면 크게, 진하면 작게
let k = Math.pow(F_REF / inkRatio, 0.5);
k = Math.min(Math.max(k, 0.55), 1.9);        // 보정 범위 제한

let scale = Math.sqrt((TARGET_AREA * k * logoScale) / (w * h));

// 캔버스를 넘지 않도록 보정
scale = Math.min(scale, (CANVAS_W * MAX_W_RATIO) / w, (CANVAS_H * MAX_H_RATIO) / h);

const finalW = Math.round(w * scale);
const finalH = Math.round(h * scale);
```

**측정값을 하드코딩하지 마세요.** 앞으로 올라올 로고도 자동으로 보정되어야 합니다.

원본보다 크게 확대되는 경우(`scale > 1`)도 그대로 진행하되, `scale > 3`이면 원본 해상도가 너무 낮다는 뜻이므로 **콘솔에 경고를 남기세요.**

### 3-5. ③ 캔버스 중앙 배치 + ④ 저장

- 600×300 **완전 투명** 캔버스를 만들고 리사이즈한 로고를 정중앙에 합성한다.
- WebP로 저장. `quality: 90`, **알파 채널 반드시 유지.**
- 흰 배경으로 깔지 말 것. 다크모드에서 흰 박스가 튄다.

---

## 4. DB 변경

`brands` 테이블의 **실제 컬럼명을 먼저 확인할 것.** (`logo_url`인지 `logo`인지 다른 이름인지 추측 금지)

추가할 컬럼:

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `logo_original_url` | text, nullable | 원본 파일 URL. 나중에 기준값을 바꿔서 다시 처리할 때 필요 |
| `logo_scale` | numeric, default 1.0 | 브랜드별 미세 조정 배율 |

`logo_scale`이 필요한 이유: 잉크 밀도 보정을 자동으로 넣어도 **눈으로 봐야 알 수 있는 차이가 남는다.** 그때 이미지 처리 로직을 통째로 바꾸지 말고 그 브랜드만 `1.15` 같은 값을 주면 된다. 계산식의 `logoScale` 자리에 들어간다. 허용 범위는 `0.7 ~ 1.5`.

**특히 파타고니아를 눈으로 확인할 것.** 유일하게 속이 꽉 찬 로고라 자동 보정이 가장 크게 걸린 브랜드다(0.74). 너무 작아 보이면 `logo_scale`을 1.1~1.2로 올린다.

SQL 마이그레이션 파일로 만들어서 별도로 실행할 수 있게 할 것. 코드에서 자동 실행하지 말 것.

---

## 5. 관리자 화면

브랜드 관리는 **"상품 관리" 메뉴 아래에 있다.** 다른 곳에서 찾지 말 것.

- 로고 업로드 시 위 처리를 자동 적용. 원본은 `logo_original_url`에 따로 저장.
- 브랜드 수정 화면에 `logo_scale` 입력 필드 추가 (0.7~1.5, 기본 1.0).
- `logo_scale`을 바꾸고 저장하면 **원본에서 다시 처리해서 덮어쓴다.** 이미 축소된 이미지를 또 키우면 화질이 깨진다.
- 관리자 저장은 `service_role` 키를 써야 RLS를 통과한다. 기존 브랜드 저장 코드가 이미 그렇게 되어 있는지 확인하고 동일하게 맞출 것.

---

## 6. 기존 17개 일괄 재처리

`scripts/normalize-brand-logos.ts` 로 만든다. **자동 실행 금지, 수동으로 한 번 돌린다.**

동작:

1. `brands` 테이블 전체를 읽는다.
2. 각 로고를 R2에서 내려받는다.
3. **먼저 원본을 `brands/original/{slug}.{ext}` 로 R2에 복사**하고 `logo_original_url`에 기록한다. (이 단계가 실패하면 그 브랜드는 건너뛴다. 원본 없이 덮어쓰면 복구 불가)
4. 정규화 처리 후 `brands/{slug}-v2.webp` 같은 **새 키로 업로드.** 기존 키를 덮어쓰지 말 것 — Cloudflare 캐시 때문에 옛 이미지가 계속 보인다.
5. `logo_url`을 새 URL로 업데이트.
6. 브랜드별로 `이름 / 트리밍 전후 크기 / scale값 / 결과` 를 한 줄씩 출력.

`--dry-run` 옵션을 붙여서 **업로드·DB 수정 없이 계산 결과만 출력**할 수 있게 할 것. 이걸로 먼저 확인한다.

### 기대 결과 (검증용)

미리 계산해 둔 값입니다. `--dry-run` 출력이 아래와 **다르면 계산식이 잘못된 것**이니 그대로 진행하지 말고 보고하세요.

| 브랜드 | 비율 | 잉크 | 보정 | 최종 크기 |
|---|---|---|---|---|
| 꼼데가르송 | 9.27 | 32% | 0.98 | 701×76 |
| 캘빈클라인 | 6.41 | 29% | 1.01 | 600×94 |
| 가니 | 3.93 | 47% | 0.89 | 415×106 |
| 라코스테 | 2.44 | 25% | 1.05 | 385×158 |
| 메종키츠네 | 1.97 | 10% | 1.31 | 431×219 |
| 폴로랄프로렌 | 1.83 | 9% | 1.35 | 427×233 |
| 단톤 | 1.73 | 30% | 1.00 | 309×179 |
| 아크테릭스 | 1.64 | 14% | 1.21 | 363×221 |
| 비비안웨스트우드 | 1.55 | 24% | 1.06 | 309×200 |
| 알로 | 1.46 | 35% | 0.96 | 272×186 |
| 스투시 | 1.38 | 26% | 1.03 | 285×206 |
| 휴먼메이드 | 1.14 | 44% | 0.91 | 228×200 |
| 팔라스 | 1.14 | 49% | 0.88 | 221×194 |
| 파타고니아 | 1.00 | 100% | 0.74 | 174×174 |
| 아미(하트A) | 0.61 | 51% | 0.88 | 160×264 |

`logo_scale`은 전부 기본값 1.0 기준입니다. **상한(736×306)에 걸리는 로고는 하나도 없어야 합니다.**

---

## 7. 화면 쪽 수정

로고가 실제로 렌더링되는 곳을 **grep으로 전부 찾아서 목록을 먼저 보고할 것.** 짐작으로 몇 군데만 고치면 반드시 빠지는 곳이 생긴다. 최소한 아래는 확인 대상:

- 메인 페이지 BRANDS 섹션
- `/brands` 페이지
- `/brand/{slug}` 개별 브랜드 페이지 상단
- 상품 상세 / 상품 카드에 브랜드 로고가 들어가는지
- 관리자 브랜드 목록

모든 곳에서 공통 규칙:

```
고정 비율 800:360 (약 2.22:1) 박스 + object-contain + 브랜드별 예외 CSS 전부 제거
```

브랜드별로 `h-6`, `h-10` 처럼 따로 주고 있던 값이 있으면 **전부 지웁니다.** 이미지에 균일화가 구워져 있으므로 CSS에서 또 조정하면 다시 어긋납니다.

박스의 실제 픽셀 크기는 화면마다 달라도 되지만, **비율은 반드시 800:360으로 통일**하세요. 다른 비율의 박스에 넣으면 위아래 또는 좌우에 여백이 생겨 균일화가 무너집니다.

---

## 8. 다크모드 확인

관리자 화면은 다크모드가 있다. 검은색 단색 로고는 다크모드에서 안 보인다.

이번 작업에서 해결하지는 말고, **다크모드에서 안 보이는 브랜드가 몇 개인지 확인만 해서 보고할 것.** 개수에 따라 다음 작업 범위를 정한다.

---

## 9. 완료 후 보고할 것

- 로고가 렌더링되는 파일 전체 목록
- `--dry-run` 결과 17개 브랜드의 트리밍 전후 크기와 scale 값
- `scale > 3`으로 경고가 뜬 브랜드 (원본 해상도가 낮아 교체가 필요한 것들)
- 트리밍이 실패해서 원본을 그대로 쓴 브랜드
- 다크모드에서 안 보이는 브랜드 개수

---

## 질문

작업 중 확실하지 않은 것은 여기에 모아서 남길 것. 임의로 결정하지 말 것.
