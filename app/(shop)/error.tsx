'use client';

/**
 * ============================================================
 * ★★ 손님 화면 오류 — 헤더·푸터를 남깁니다 (2026-08-25)
 * ============================================================
 *
 * ★★ 왜 이 파일이 따로 필요한가
 *   app/error.tsx 하나만 두면 그것이 루트 레이아웃 바로 밑에 붙습니다.
 *   그러면 오류가 났을 때 (shop) 레이아웃까지 통째로 대신하게 되어
 *   **헤더도 푸터도 장바구니도 전부 사라집니다.** 실제로 눌러 보고 알았습니다.
 *   손님은 오류 화면 하나만 덩그러니 보게 되고, 다른 데로 갈 길이 없습니다.
 *
 *   이 파일이 (shop) 안쪽에 있으면 오류가 여기서 먼저 잡힙니다.
 *   헤더·푸터는 그대로 있고 본문만 안내 화면으로 바뀝니다.
 *   손님은 위 메뉴로 그냥 다른 화면에 가시면 됩니다.
 *
 * ★ 화면과 로그는 app/error.tsx 와 똑같습니다. 그대로 가져다 씁니다.
 *   같은 글을 두 벌 두면 반드시 한쪽만 고쳐집니다.
 */
export { default } from '../error';
