# 테스트 데이터 정리 계획 (2026-08-25)

> **아직 아무것도 지우지 않았습니다.** 이 문서는 "무엇을 지우면 무슨 일이 일어나는지" 를
> 코드와 DB 구조에서 확인해 적은 것입니다. 보시고 정하시면 그때 진행합니다.

---

## ⚠️ 먼저 — 가장 중요한 사실 하나

### 주문을 지우면 재고가 돌아오지 않습니다

재고를 되돌리는 것은 **DB 가 아니라 코드**가 합니다. (`releaseOrderStock()`)
주문 행을 `delete` 하면 그 코드는 **불리지 않습니다.**

그래서 **재고를 잡고 있는 주문을 그냥 지우면, 그 수량이 영원히 묶입니다.**
팔 수 있는 물건이 품절로 보이고, 되돌릴 방법이 없습니다.

> **반드시 이 순서로 해야 합니다.**
> **① 관리자 화면에서 취소 처리 → (재고가 돌아옴) → ② 그 다음에 지우기**
>
> 관리자에서 취소하면 `releaseOrderStock()` 이 돌아 재고를 되돌리고,
> `orders.stock_released_at` 에 시각이 찍힙니다. 그 칸에 값이 있으면 안전합니다.

**지우기 전에 확인하는 SQL** (읽기만 합니다)

```sql
select order_no, status, stock_released_at
  from public.orders
 order by created_at;
```

`stock_released_at` 이 **비어 있는데** 상태가 취소·실패가 아닌 주문은
**아직 재고를 잡고 있는 것**입니다. 먼저 취소 처리부터 하세요.

---

## 1. 지울 것과 남길 것

**정확한 목록은 아래 SQL 로 뽑아 주세요.** 제가 DB 를 읽지 않아 24건의 내용을 모릅니다.

```sql
select order_no,
       created_at,
       status,
       payment_method,
       total_amount,
       pg_comm_con_id,
       stock_released_at,
       user_id
  from public.orders
 order by created_at;
```

### 남겨야 하는 것

| 주문 | 왜 |
|---|---|
| **ORD-20260825-0003** | **실제 결제 1,000원.** 돈이 오간 기록이라 지우면 안 됩니다 |
| 그 밖에 `pg_comm_con_id` 가 있고 `KSNET_MODE=live` 로 한 주문 | 실제 결제일 수 있습니다. 위 SQL 결과에서 확인하세요 |

### 지워도 되는 것 (판단 기준)

| 기준 | 설명 |
|---|---|
| 운영 전환 **전에** 만든 카드 주문 | 테스트 상점아이디(`2999199999`)로 한 것이라 실제 돈이 아닙니다. 오늘 운영 전환(11시대) **이전** 주문이 전부 해당합니다 |
| 무통장입금 시험 주문 | 통장에 실제 입금이 없었다면 지워도 됩니다 |
| 결제창을 열었다 닫은 주문 (`failed`·`cancelled`) | 돈이 오가지 않았습니다 |

> **⚠️ 세금계산·정산 근거가 필요한 주문은 지우지 마세요.**
> 실제 결제는 지우는 것보다 **취소 처리 후 남겨 두는 편**이 안전합니다.
> 나중에 "이 돈이 왜 들어왔지" 를 되짚을 유일한 근거입니다.

---

## 2. 주문 하나를 지우면 같이 지워지는 것

DB 의 외래키 설정을 그대로 옮긴 것입니다.

### 함께 **지워집니다** (cascade)

| 표 | 무엇 | 근거 |
|---|---|---|
| `order_items` | 그 주문의 상품 줄 전부 | `schema-2a.sql:80` `on delete cascade` |
| `order_status_history` | 상태가 바뀐 이력 전부 | `schema-2a.sql:102` `on delete cascade` |

### **남습니다** — 연결만 끊깁니다 (set null)

| 표 | 무엇이 남나 | 근거 |
|---|---|---|
| `payment_logs` | **결제 원문이 그대로 남습니다.** `order_id` 만 비고 `order_no` 는 글자로 남아 있어 나중에도 찾을 수 있습니다 | `schema-4a.sql:84` |
| `stock_moves` | 재고가 움직인 기록이 남습니다. `order_no` 도 남습니다 | `schema-4b.sql:60` |
| `inquiries` | 그 주문으로 남긴 1:1 문의가 남습니다 (주문 연결만 끊김) | `schema-2b.sql:68` |
| `reviews` | 그 주문으로 쓴 리뷰가 남습니다 | `schema-3a.sql:28` |

> **`payment_logs` 는 일부러 이렇게 만들어 두었습니다.** 주문을 지워도 결제 원문은 남습니다.
> 돈 문제의 마지막 근거라서요. (`rls-4a.sql` 주석) **이 표는 지우지 마세요.**

### ❌ 아무 일도 일어나지 않는 것 — 직접 챙겨야 합니다

| 무엇 | 왜 위험한가 |
|---|---|
| **재고** | 위에서 말한 그대로입니다. **주문을 지워도 재고는 안 돌아옵니다.** 먼저 취소하세요 |
| **포인트** | `point_transactions.ref_id` 는 **외래키가 아닙니다** (`schema-3a.sql:67` 그냥 `uuid`). 주문을 지워도 포인트 내역이 그대로 남고, **`profiles.point_balance` 도 그대로입니다.** 시험하며 적립된 포인트가 진짜 포인트로 남습니다 |
| **주문번호 일련번호** | 다음 주문번호가 `0001` 부터 다시 시작하지 않을 수 있습니다. 문제는 아닙니다 |

---

## 3. 회원 계정과 포인트 — 세 가지 길

| | 방법 | 결과 | 추천 |
|---|---|---|---|
| **가** | 계정은 두고 **포인트만 0 으로** | 주문 이력만 사라지고 계정은 남습니다. `point_transactions` 에 `reason='admin'` 로 조정 내역을 남기면 근거가 남습니다 | ⭐ **가장 안전합니다** |
| **나** | 시험용 회원 계정을 **통째로 삭제** | `profiles` 와 `point_transactions` 가 함께 사라집니다 (`on delete cascade`). 단 **Supabase Authentication 의 계정도 따로 지워야** 완전히 없어집니다 | 시험 전용 계정일 때만 |
| **다** | 그냥 둔다 | 포인트가 남습니다. 나중에 그 계정으로 진짜 주문하면 그 포인트를 씁니다 | 사장님 본인 계정이면 괜찮습니다 |

> **관리자 이메일 계정(`ADMIN_EMAILS`)은 절대 지우지 마세요.** 로그인이 막힙니다.
> 손님 계정과 다른 이메일을 쓰셨으니 그 계정은 건드릴 일이 없습니다.

**포인트 현황 확인** (읽기만 합니다)

```sql
select p.id, p.point_balance, count(t.id) as 내역수
  from public.profiles p
  left join public.point_transactions t on t.user_id = p.id
 group by p.id, p.point_balance
 order by p.point_balance desc;
```

---

## 4. 지우기 전 백업 — 세 겹으로 권합니다

### ① 관리자 화면에서 CSV 내려받기 *(제일 쉽습니다)*

**관리자 → 주문 관리 → 내려받기 → `전체 항목` 양식**

`/api/admin/export/orders?format=full` 이 주문의 모든 칸을 CSV 로 뽑아 줍니다.
엑셀에서 바로 열립니다 (한글 안 깨지게 BOM 이 들어갑니다).
**회원·상품도 같은 자리에서 받을 수 있습니다.**

### ② Supabase 에서 표째 내려받기

Supabase → **Table Editor** → 표 선택 → 우측 상단 **Export → Download as CSV**

받아 둘 표: `orders` · `order_items` · `order_status_history` · `payment_logs` · `stock_moves` · `point_transactions` · `profiles`

### ③ 프로젝트 전체 백업 *(가장 확실합니다)*

Supabase → **Database → Backups**
유료 플랜이면 시점 복구가 됩니다. **지우기 직전에 백업 시각을 적어 두세요.**

> **무료 플랜이면 ③이 없습니다.** 그때는 ①②를 반드시 하세요.
> **`delete` 는 되돌릴 수 없습니다.** Supabase 대시보드에 "실행 취소" 가 없습니다.

---

## 5. 정하시면 이 순서로 합니다

1. **위 SQL 로 24건 목록을 뽑아 저에게 보여 주세요** — 지울 것과 남길 것을 함께 정합니다
2. **백업** ①②(가능하면 ③까지)
3. **재고를 잡고 있는 주문은 관리자 화면에서 먼저 취소** — 재고가 돌아옵니다
4. **포인트 처리 방식 결정** (가·나·다 중)
5. 지울 주문번호 목록을 확정하고, **제가 SQL 을 만들어 드립니다.** 실행은 사장님이 하십니다
6. 지운 뒤 **재고·포인트·주문 건수**를 다시 확인합니다

> **제가 임의로 지우지 않습니다.** SQL 도 만들어서 보여만 드리고 실행하지 않습니다.

---

## 6. 지우지 말아야 할 것 — 한눈에

| ❌ 절대 |
|---|
| **ORD-20260825-0003** (실제 결제 1,000원) |
| **`payment_logs`** 표 — 주문을 지워도 이건 남겨야 합니다. 돈 문제의 마지막 근거입니다 |
| **관리자 이메일 계정** — 로그인이 막힙니다 |
| **재고를 아직 잡고 있는 주문** — 취소 처리를 먼저 하세요 |
