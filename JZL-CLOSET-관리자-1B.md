JZL CLOSET 관리자 페이지 1단계-B 작업이다.
범위: 분류 관리 · 브랜드 관리 · 설정 · 디자인 관리 · GA4 연동.
주문/회원/문의/리뷰는 다음 단계이므로 이번에 만들지 마라.

질문하지 말고 끝까지 완성하고, npm run build가 통과할 때까지 스스로 고쳐라.

────────────────────────────────
[0] 먼저 고칠 것 (1-A 잔여 이슈)
────────────────────────────────
0-1. 옵션 조합 표의 재고수량 칸이 헷갈린다.
  - 빈칸 = 재고 미관리(항상 판매), 0 = 품절 인데 구분이 안 된다.
  - 빈칸일 때 placeholder를 "미관리"로 표시하고, 옅은 회색으로 보이게 하라.
  - 재고가 0인 행은 행 배경을 옅은 붉은색으로 칠하고 "품절" 뱃지를 붙여라.
  - 표 상단에 안내 한 줄: "재고수량을 비워 두면 재고를 세지 않습니다. 0을 넣으면 품절 처리됩니다."

0-2. 관리자 로그인 화면에 스타일이 전혀 없다.
  관리자 레이아웃과 같은 톤으로 정리하라. 화면 중앙 카드, 로고, 비밀번호 입력, 버튼.
  과하게 꾸미지 말고 깔끔하게.

────────────────────────────────
[1] DB 테이블 추가
────────────────────────────────
supabase/schema-1b.sql 파일을 만들어라. (내가 SQL Editor에서 직접 실행한다)

categories 테이블:
  id            uuid primary key default gen_random_uuid()
  slug          text unique not null
  label         text not null          -- 화면에 보이는 글자
  name_ko       text not null          -- h1·메타데이터용 한글
  parent_slug   text                   -- null이면 대분류
  display_order integer default 0
  is_visible    boolean default true
  description   text
  created_at    timestamptz default now()
  updated_at    timestamptz default now()

brands 테이블:
  id            uuid primary key default gen_random_uuid()
  slug          text unique not null
  label         text not null
  name          text not null          -- JSON-LD·alt용 정식 표기
  name_ko       text
  tagline       text
  story         text
  origin        text
  since         text
  image_url     text
  display_order integer default 0
  is_visible    boolean default true
  is_featured   boolean default false
  created_at    timestamptz default now()
  updated_at    timestamptz default now()

site_settings 테이블은 이미 있다. key 하나씩 추가해 쓴다.

인덱스: categories(parent_slug), categories(display_order),
       brands(display_order), brands(is_visible)

★ 기존 lib/categories.ts, lib/brands.ts의 데이터를 그대로 옮기는
  supabase/seed-1b.sql 도 함께 만들어라. (이것도 내가 실행한다)
★ 두 파일 실행 전에도 사이트가 안 죽게, DB가 비어 있으면
  기존 lib/*.ts 값을 임시로 쓰는 폴백을 넣어라.

────────────────────────────────
[2] 분류 관리 (/admin/categories)
────────────────────────────────
■ 대분류 목록
- 표: 순서 · 라벨(label) · 한글명 · 소분류 개수 · 노출 · 관리
- 드래그로 순서 변경 (display_order 자동 갱신)
- 노출 토글: 끄면 메뉴·사이트맵·카테고리 페이지에서 제외
- [+ 대분류 추가] 버튼

■ 대분류 추가/수정 폼
- slug (영문 소문자+하이픈, 자동 검증)
  ★ 이미 등록된 대분류의 slug는 수정 불가로 막아라.
    바꾸면 검색 색인이 초기화된다. 입력칸을 비활성화하고
    "주소에 쓰이므로 변경할 수 없습니다"를 표시하라.
- label (화면 표시용, 한글·영문 자유)
- name_ko (h1·메타데이터용)
- 설명 (카테고리 페이지 상단 문구, 선택)
- 노출 여부

■ 소분류 관리
- 대분류를 클릭하면 그 아래 소분류 목록이 펼쳐진다
- 소분류도 동일하게 추가·수정·순서변경·노출토글
- 소분류 slug도 등록 후 수정 불가

■ 삭제
- 해당 분류에 속한 상품이 있으면 삭제를 막고 개수를 알려줘라
- 상품이 없을 때만 삭제 가능, 확인 창 필수

────────────────────────────────
[3] 브랜드 관리 (/admin/brands)
────────────────────────────────
- 목록: 순서 · 이미지 · 라벨 · 정식명 · 상품수 · 노출 · 강조 · 관리
- 드래그로 순서 변경
- 추가/수정: slug(등록 후 수정 불가) · label · name(정식 표기) ·
  한 줄 소개 · 브랜드 스토리 · 원산지 · 설립연도 · 대표 이미지 업로드
- 노출 토글 / 강조(isFeatured) 토글
- 브랜드에 속한 상품이 있으면 삭제 불가

★ 브랜드 스토리는 SEO에 쓰인다. 브랜드 상세 페이지(/brand/{slug})에
  실제 텍스트로 출력되게 하라. "아미 브랜드" 같은 검색 유입 경로다.

────────────────────────────────
[4] 설정 (/admin/settings)
────────────────────────────────
탭 또는 섹션으로 나눠라.

4-1. 스토어 정보
  브랜드명 · 슬로건 · 브랜드 소개(3문장) · 고객센터 번호 ·
  카카오톡 채널 링크 · 이메일 · 운영시간
  사업자 정보: 상호 · 대표자 · 사업자등록번호 · 통신판매업신고번호 · 주소
  → 푸터와 법정 페이지에 즉시 반영

4-2. 브랜딩
  파비콘 업로드 (이미 만든 기능을 여기로 옮겨라)
  로고 이미지 업로드 (없으면 텍스트 로고 사용)

4-3. 배송·반품
  기본 배송비 (숫자)
  무료배송 기준 금액 (이 금액 이상이면 무료. 0이면 기능 끔)
  제주·도서산간 추가배송비
  반품 주소 (사업장 주소와 다를 수 있음)
  평균 배송 소요일 안내 문구

4-4. 데이터 내보내기
  상품 전체를 엑셀(xlsx 또는 csv)로 내려받기
  ★ 라이브러리 추가 없이 CSV로 만들어라. UTF-8 BOM을 넣어
    엑셀에서 한글이 깨지지 않게 하라.

────────────────────────────────
[5] 디자인 관리 (/admin/design)
────────────────────────────────
5-1. 메인 배너
  - 최대 5개 등록. 각 배너: 이미지 · 제목 · 부제 · 버튼문구 · 링크 · 노출여부
  - 드래그로 순서 변경
  - 자동 슬라이드 간격 설정 (기본 5000ms). 배너는 상품 이미지와 달리
    천천히 넘어가야 한다. 1초로 하지 마라.
  - 배너가 1개면 슬라이드하지 않는다
  - 모바일용 이미지를 따로 올릴 수 있게 하라 (가로형/세로형 비율이 다르다)
  - 권장 사이즈를 화면에 표시하라 (PC 1920x800, 모바일 1080x1350)

5-2. 사이트 문구
  아래 항목의 문구를 관리자에서 수정할 수 있게 하라.
  전부 site_settings에 저장하고, 각 페이지가 DB에서 읽도록 바꿔라.
  - 메인 히어로 문구 / 브랜드 스토리 섹션
  - 주문 방법 3스텝 안내
  - 배송·교환·반품 안내 (/guide)
  - 이용약관 (/terms)
  - 개인정보처리방침 (/privacy)
  - 브랜드 소개 (/about)
  - 장바구니·주문 안내 문구 (/order)
  - 404 페이지 문구

  ■ 편집기는 상품 상세 편집기와 같은 수준으로:
    소제목 · 본문 · 굵게 · 줄바꿈 · 링크 · 정렬(좌/중/우)
    법정 페이지는 문단이 길다. 여러 문단을 편하게 다룰 수 있어야 한다.
  ■ 각 항목에 "기본값으로 되돌리기" 버튼을 두어라.
    잘못 지웠을 때 복구할 수 있어야 한다.
  ■ 저장 시 revalidatePath로 해당 페이지 즉시 반영

────────────────────────────────
[6] 구글 애널리틱스 (GA4)
────────────────────────────────
- 설정 화면에 GA4 측정 ID(G-XXXXXXXXXX) 입력칸을 두어라
- 값이 있으면 app/layout.tsx에서 GA4 스크립트를 삽입하고,
  비어 있으면 아무것도 넣지 마라
- next/script의 strategy="afterInteractive" 사용
- 개발 환경(localhost)에서는 전송하지 마라
- 상품 상세 조회, 장바구니 담기 이벤트를 GA4로 전송하라
  (view_item, add_to_cart 표준 이벤트명 사용)

────────────────────────────────
[7] 사이드바 정리
────────────────────────────────
관리자 사이드바를 아래로 갱신하라. "준비 중" 표시는 없앤다.
  상품 관리
  분류 관리
  브랜드 관리
  디자인 관리
  설정
  ─────────
  (주문 관리 · 회원 관리 · 문의 관리 · 리뷰 관리 — 준비 중 회색 표시)

────────────────────────────────
[8] 마무리
────────────────────────────────
- 프론트 전 페이지가 DB의 카테고리·브랜드·설정을 읽도록 전환하되,
  ISR과 정적 생성을 깨뜨리지 마라. SEO가 이 프로젝트의 최우선 목표다.
- 저장 시 revalidatePath로 관련 페이지만 즉시 갱신
- npm run build 통과 (Type error·Failed to compile 0건)
- supabase/schema-1b.sql 과 supabase/seed-1b.sql 내용을 마지막에 보여줘라
- 깃허브에 올려라
- 마지막에 한국어로 알려줘라:
  1) 실행해야 할 SQL 파일과 순서
  2) 새로 생긴 관리자 화면과 각각 무엇을 할 수 있는지
  3) GA4 측정 ID를 어디서 발급받아 어디에 넣는지
  4) 이번에 만들지 않은 것 (다음 단계)
