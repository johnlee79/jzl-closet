# JZL CLOSET 3단계-K 작업 명세

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- 고객 화면 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #55524E / wine #6A2E3C
- 고객 화면 폰트: Pretendard(본문) · Gowun Batang(국문 제목) · Cormorant Garamond(영문 디스플레이)
- **그림자 금지, 이모지 금지** (아이콘은 SVG로 직접 그릴 것)
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role
- 새 SQL이 필요하면 `supabase/schema-3k.sql`로 만들고 실행 순서를 보고할 것.
  필요 없으면 억지로 만들지 말 것
- **`vercel.json`의 cron은 `0 19 * * *`(하루 1회)를 반드시 유지하세요.**
- 체험단 후기의 `※ 제품을 무상으로 제공받아 작성된 후기입니다` 문구는 표시광고법상 필수입니다. 제거 금지.
- 작업 후 `npm run build`와 `npm run check:schema`를 실행해 확인할 것

---

# [1] ⚠️ 전체화면 이미지 뷰어 버그 수정 (최우선)

## 증상

3-J에서 만든 전체화면 뷰어가 **PC에서 깨져 있습니다.** 모바일은 정상입니다.

운영자가 보낸 화면에서 확인된 것:

1. 이미지가 화면 위쪽 일부에만 나오고, **뷰어가 화면 전체를 덮지 못합니다**
2. 뒤에 있는 상품 상세의 **옵션 선택 박스(`컬러을(를) 선택해 주세요`, `사이즈을(를) 선택해 주세요`)가 뷰어 위로 뚫고 올라와 보입니다**
3. **닫기 X 버튼이 보이지 않습니다.** 로고를 눌러 페이지를 벗어나는 것 말고는 빠져나올 방법이 없습니다
4. 하단의 `1 / 8` 표시는 정상적으로 나옵니다

## 원인 조사

**추측하지 말고 실제로 확인하세요.** 아래를 순서대로 점검하세요.

1. 뷰어 컨테이너의 `position` · `inset` · `width` · `height` 가 뷰포트 전체를 덮는가
2. `z-index` 가 옵션 선택 영역보다 확실히 위인가.
   상품 상세의 다른 요소에 걸린 `z-index` 값을 전부 조사해서 가장 높은 값보다 위로 올리세요
3. 옵션 선택 영역이 `position: sticky` 나 `fixed` 를 쓰고 있어 **쌓임 맥락(stacking context)** 을 따로 만들고 있지는 않은가.
   부모에 `transform` · `filter` · `will-change` 가 걸려 있으면 자식의 `z-index`가 무력해집니다.
   이 경우 뷰어를 `createPortal` 로 `document.body` 바로 아래에 렌더하세요
4. 닫기 버튼이 DOM에는 있는데 화면 밖으로 나갔거나, 색이 배경과 같아 안 보이는 것은 아닌가

**데스크톱 폭(1440px 이상)에서 반드시 실제로 열어 보고 확인하세요.**
3-J 때 개발 서버 CSS가 깨진 상태에서 확인한 전례가 있으니, `.next` 를 지우고 새로 띄운 뒤 확인하세요.

## 닫기 버튼 사양

```
위치: 오른쪽 위
모양: 흰 바탕 원 또는 사각, 검정 X 아이콘
크기: 44×44px 이상 (손가락으로 누를 수 있는 최소 크기)
여백: 화면 가장자리에서 16~24px
```

- 아이콘은 SVG로 직접 그릴 것 (이모지·문자 `×` 금지)
- 배경 이미지가 밝든 어둡든 항상 보여야 합니다. 흰 바탕이 그 역할을 합니다
- `aria-label="닫기"`
- 마우스를 올리면 살짝 어두워지는 정도의 반응

## 함께 확인할 것

- `Esc` 키로 닫히는가
- 배경(이미지 바깥 어두운 영역)을 눌러도 닫히는가
- 닫으면 원래 스크롤 위치로 돌아오는가
- 열려 있는 동안 하단 구매 바와 맨 위로 버튼이 숨는가
- 좌우 화살표와 키보드 좌우키가 동작하는가

---

# [2] 메인 섹션 노출 켜고 끄기

## 배경

메인의 섹션 중 일부를 임시로 감추고 싶을 때가 있습니다.
지금은 코드를 고쳐야만 가능해서, 관리자가 스스로 조절할 수 없습니다.

예를 들어 카테고리 이미지를 아직 준비하지 못했으면 그 섹션만 잠시 끄고 싶습니다.

## 조치

`관리자 → 디자인 관리` 에 **메인 섹션 노출** 이라는 항목을 만드세요.
아래 섹션을 각각 켜고 끌 수 있어야 합니다.

| 섹션 | 기본값 |
|---|---|
| 메인 배너 | 켬 |
| 히어로 | 켬 |
| NEW ARRIVAL (이번 주 입고) | 켬 |
| SELECTION (고르는 기준) | 켬 |
| CATEGORY | 켬 |
| BRANDS (취급 브랜드) | 켬 |

- 껐을 때 해당 섹션이 **화면에서 완전히 사라져야** 합니다. 빈 여백이 남으면 안 됩니다
- 저장 시 `revalidatePath('/')` 로 즉시 반영
- 관리자 화면에서 각 항목 옆에 **그 섹션이 화면 어디에 있는지** 한 줄로 적어 주세요
  (예: `메인 중간, 분류 네 칸이 나오는 자리`)
  운영자가 이름만 보고는 어느 섹션인지 모릅니다

기존에 섹션 노출을 제어하는 구조가 이미 있으면 **그것을 확장**하세요. 새로 만들지 마세요.

---

# [3] 카테고리·브랜드 대표 이미지를 관리자에서 업로드

## 배경

현재 메인 CATEGORY 섹션의 네 칸이 **전부 회색 빈 화면**입니다.

원인은 코드가 `public/images/category/{slug}.jpg` 를 참조하는데 그 파일이 없기 때문입니다.
`SafeImage` 가 실패를 가려주고 있어 깨지지는 않지만, 아무 정보 없는 회색 상자가 큰 자리를 차지하고 있습니다.

운영자가 이미지를 직접 만들어 올릴 계획입니다. **관리자에 업로드 자리를 만들어야 합니다.**

## 3-1. 카테고리 관리에 이미지 업로드 추가

`관리자 → 분류 관리` 의 각 카테고리에 **대표 이미지** 항목을 추가하세요.

- Cloudflare R2 업로드 (기존 이미지 파이프라인·WebP 변환 그대로 재사용)
- 미리보기와 삭제 버튼
- 권장 크기 안내를 화면에 적어 주세요 (메인 카드 비율에 맞춰 실제 값을 적을 것)

`categories` 테이블에 이미지 URL 컬럼이 없으면 `supabase/schema-3k.sql` 로 추가하고,
**실행해야 할 SQL을 마지막에 반드시 보고**하세요.

## 3-2. 브랜드도 같은 상태인지 확인

`public/images/brands/{slug}.jpg` 도 같은 방식으로 참조되고 있습니다.
브랜드 관리에는 이미 이미지 업로드가 있는 것으로 보이는데, **화면이 그 값을 쓰고 있는지** 확인하세요.
`public/` 경로를 보고 있다면 관리자에 올린 R2 이미지를 쓰도록 고치세요.

## 3-3. 이미지가 없을 때

이미지를 아직 안 올린 카테고리는 **회색 빈 상자 대신** 아래처럼 처리하세요.

- 카드 자체는 유지하되, 이미지 영역을 **얇게 줄이거나 없애고** 이름·상품 수·하위 분류만 보여줄 것
- 지금처럼 세로로 크게 회색 상자가 남으면 안 됩니다
- 이미지를 올리면 자동으로 원래 카드 모양이 됩니다

## 3-4. `public/images` 옛 경로 정리

`public/images/main/hero.jpg` · `category/{slug}.jpg` · `brands/{slug}.jpg` 는
파일이 없고 `README.txt` 만 있는데 코드가 참조하고 있습니다.

관리자 업로드로 전환한 뒤, **코드에서 이 경로 참조를 제거**하세요.

**단, `public/images/products/*` 는 `supabase/seed.sql` 이 참조하므로 그대로 두세요.**

---

# [4] 확인 사항

작업이 끝나면 아래를 확인하고 보고하세요.

1. **데스크톱 폭(1440px)에서** 전체화면 뷰어가 화면 전체를 덮는가
2. 뒤쪽 옵션 선택 박스가 뷰어 위로 비쳐 보이지 않는가
3. 오른쪽 위 흰 바탕 X 버튼이 보이고 눌러서 닫히는가
4. `Esc` · 배경 클릭으로도 닫히는가
5. 닫으면 원래 스크롤 위치로 돌아오는가
6. 메인 섹션을 하나씩 꺼봤을 때 빈 여백 없이 사라지는가
7. 카테고리에 이미지를 올리면 메인 CATEGORY 카드에 나오는가
8. 이미지를 안 올린 카테고리가 회색 빈 상자로 남지 않는가
9. `npm run build` 통과 (Type error·Failed to compile 0건)
10. `npm run check:schema` 통과

---

# [5] 마무리

- 커밋하고 푸시하세요
- `vercel.json`의 cron이 `0 19 * * *`인지 커밋 전에 다시 확인하세요
- 마지막에 한국어로 알려주세요:
  1) **실행해야 할 SQL 파일명과 순서** (카테고리 이미지 컬럼이 생겼다면 반드시)
  2) 뷰어가 깨졌던 **실제 원인**과 어떻게 고쳤는지
  3) 관리자 어디에 새 항목이 생겼는지 (메뉴 경로를 정확히)
  4) 카테고리 이미지 **권장 크기** (가로×세로 픽셀)
  5) 명세대로 하지 않은 것이 있으면 그 이유
  6) 이번에 만들지 않은 것 (다음 단계)
