# 결제 하나를 끝까지 확인하는 SQL

**어디서 실행하나** — Supabase 대시보드 → 왼쪽 메뉴 **SQL Editor** → 새 쿼리에 붙여넣고 **Run**

**이 파일은 붙여넣기용 메모입니다.** 코드가 아니고, 실행 순서에도 들어가지 않습니다.
`supabase/` 폴더의 schema·rls 파일들과는 아무 상관이 없습니다. 그 파일들은 표를 만드는 것이고,
여기 있는 것은 **이미 있는 표를 읽기만** 합니다.

> **둘 다 `select` 뿐입니다. 아무것도 바꾸지 않습니다.**
> 주문번호만 확인하려는 것으로 바꿔서 쓰세요.

---

## ① 결제 기록 — 결제창이 우리 서버로 돌아왔는지

```sql
select created_at, kind, authyn, trno, note
  from public.payment_logs
 where order_no = 'ORD-20260825-0003'
 order by created_at;
```

### `kind` 열만 보면 됩니다

| `kind` | 무슨 뜻인가 |
|---|---|
| `return` | **결제창이 우리 서버로 돌아왔습니다** (손님 브라우저를 통해) |
| `approve` | 우리가 KSNET 에 물어 승인을 확인했습니다 |
| `notify` | **KSNET 서버가 직접 알려 왔습니다** (손님 브라우저와 무관) |
| `approve_retry` | 10분마다 도는 정리가 다시 물어봤습니다 |
| `mismatch` | 금액·주문번호가 우리 기록과 달랐습니다 |
| `error` | 통신·해석 실패 |

---

## ② 주문에 결제 Key 가 저장됐는지

```sql
select order_no, status, pg_comm_con_id, pg_tid, pg_auth_no
  from public.orders
 where order_no = 'ORD-20260825-0003';
```

`pg_comm_con_id` 가 **결제 Key** 입니다. KSNET 에 승인 재조회를 할 때 **이 값으로만** 됩니다.
주문번호로는 못 묻습니다.

---

## 왜 이게 "모바일 3초" 진단에 필요한가

모바일에서 결제가 끝나면 **완료 화면이 바로 떠야 합니다.**
그런데 `"결제 결과를 불러오는 중"` 이 3초쯤 뜬다는 것은,
**완료 화면이 열린 그 순간에 주문이 아직 `결제대기` 였다**는 뜻입니다.
그 뒤 완료 화면이 스스로 서버에 물어보고(2초마다) 결제완료로 바뀐 것입니다.

주문을 결제완료로 바꾸는 길은 **세 개**인데, 어느 길로 끝났느냐에 따라 원인이 다릅니다.

| 어느 길로 끝났나 | `kind` 에 나타나는 것 | 3초의 원인 | 결제 Key |
|---|---|---|---|
| **① 결제창 복귀** (`return` 라우트) | `return` + `approve` | 303 은 돌았고 **다른 이유**입니다. 더 파야 합니다 | **저장됨** |
| **② 노티** (KSNET 서버 → 우리 서버) | `notify` 만 있고 `return` 없음 | **결제창이 우리 서버로 안 돌아왔습니다.** 브라우저는 그와 별개로 완료 화면에 도착했고, 그때 주문은 아직 결제대기였습니다 | **저장 안 됨** |
| **③ 카드 정리** (10분마다 도는 크론) | `approve_retry` | ②와 비슷하지만 더 늦게 끝난 경우 | **저장 안 됨** |

### 두 증상이 한 원인일 수 있습니다

결제 Key 를 저장하는 `saveKsnetPaymentKey()` 는 **`return` 라우트에서만** 부릅니다.
노티(`notify/route.ts`)와 카드 정리(`card-sweep`)는 부르지 않습니다.

그래서 **`return` 이 없으면 → 결제 Key 도 없고 → 3초도 걸립니다.**
`return` 이 있으면 → 결제 Key 도 있고 → 3초는 별개의 문제입니다.

**②번 쿼리에서 `pg_comm_con_id` 가 비어 있다면 ②·③번 길일 가능성이 큽니다.**

---

## 결과를 알려 주실 때

`kind` 열에 무엇이 몇 줄 나왔는지와, `pg_comm_con_id` 가 있는지 없는지 —
이 두 가지만 알려 주시면 원인이 갈립니다.
