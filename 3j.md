# JZL CLOSET 3단계-J 작업 명세

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- 고객 화면 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #55524E / wine #6A2E3C
- 고객 화면 폰트: Pretendard(본문) · Gowun Batang(국문 제목) · Cormorant Garamond(영문 디스플레이)
- **그림자 금지, 이모지 금지** (아이콘은 SVG로 직접 그릴 것)
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role
- 새 SQL이 필요하면 `supabase/schema-3j.sql`로 만들고 실행 순서를 보고할 것.
  필요 없으면 억지로 만들지 말 것
- **`vercel.json`의 cron은 `0 19 * * *`(하루 1회)를 반드시 유지하세요.**
  Hobby 플랜은 하루 1회만 허용하며, `*/10 * * * *`로 되돌리면 **배포 자체가 거부됩니다.**
- 체험단 후기의 `※ 제품을 무상으로 제공받아 작성된 후기입니다` 문구는 표시광고법상 필수입니다. 제거 금지.
- 작업 후 `npm run build`와 `npm run check:schema`를 실행해 확인할 것

## 이 단계의 목적

곧 PG 카드결제를 붙여 오픈합니다. 그 전에 두 가지를 정리합니다.

1. **메인에서 필요 없는 섹션을 덜어낸다** — 손님이 상품에 집중하게
2. **검색과 공유에서 손해 보지 않게 기본기를 갖춘다** — 이 프로젝트는 SEO가 최우선 목표입니다

---

# [1] 메인 HOW TO ORDER 섹션 삭제

## 배경

곧 PG 카드결제가 붙습니다. 카드로 즉시 결제되는 구조에서 "주문 방법 3단계"를 메인에 크게 설명할 이유가 없습니다.
누구나 아는 흐름이고, 자리만 차지합니다.

## 조치

- 메인 화면에서 `HOW TO ORDER` 섹션을 **화면에서 제거**하세요
- **관리자의 문구 항목(`HOW TO ORDER — 주문 방법 3단계`)은 지우지 마세요.**
  저장된 문구와 `기본값으로 되돌리기` 값이 그대로 남아 있어야, 나중에 다시 쓰기로 하면 되살릴 수 있습니다
- 다만 그 항목이 **어디에도 쓰이지 않는 상태**가 되므로, 관리자 화면에서 해당 항목에
  `현재 화면에 노출되지 않습니다` 정도의 안내를 한 줄 붙여 주세요.
  운영자가 고쳐도 아무 변화가 없어 혼란스러워하는 것을 막기 위해서입니다
- 이 섹션만 쓰던 컴포넌트·스타일·아이콘이 있으면 함께 정리하세요.
  다른 곳에서도 쓰이면 남겨 두세요

## 삭제 후 메인 구성

```
히어로
NEW ARRIVAL     이번 주 입고
SELECTION       고르는 기준
CATEGORY        네 칸
BRANDS          취급 브랜드
```

섹션 사이 여백이 어색해지지 않는지 확인하고, 필요하면 간격을 조정하세요.

---

# [2] 메인 CATEGORY 설명 문구 삭제

## 배경

`무엇을 찾고 계신가요` 라는 문구가 있습니다. 바로 아래에 카테고리 네 칸이 있어 물어볼 이유가 없습니다.
아무 정보도 주지 않는 문장입니다.

## 조치

- 해당 설명 문구를 화면에서 제거하세요
- 관리자 문구 항목은 남기되, **비어 있으면 화면에 아무것도 나오지 않도록** 처리하세요
  (제목·설명 모두 동일하게. 운영자가 나중에 채우면 다시 나와야 합니다)
- 라벨 `CATEGORY`는 그대로 둡니다

---

# [3] 검색·공유 기본기

이 프로젝트는 **SEO가 최우선 목표**입니다. 아래가 갖춰지지 않으면 페이지를 아무리 잘 만들어도 검색에 잡히지 않습니다.

**현재 상태를 먼저 점검하고, 없는 것만 만드세요.**
이미 있으면 손대지 말고 "있음"이라고만 보고하세요. 중복 작업을 피하기 위해서입니다.

## 3-1. `sitemap.xml` · `robots.txt`

- `app/sitemap.ts` 로 동적 생성
  - 상품 · 브랜드 · 카테고리 · 정적 페이지(`/`, `/about`, `/brands`, `/terms`, `/privacy` 등) 전부 포함
  - **비노출 상품·브랜드는 반드시 제외**
  - `lastModified`에 실제 수정 시각을 넣을 것
- `app/robots.ts` 로 `robots.txt` 생성
  - `/admin` 이하는 `Disallow`
  - `sitemap` 위치를 명시
- 이미 있으면 **누락된 경로가 없는지만** 확인하고 보고

## 3-2. 상품 구조화 데이터 (Product JSON-LD)

상품 상세에 `Product` 스키마가 있는지 확인하고, 없거나 불완전하면 채우세요.

- `name` · `image`(여러 장) · `description` · `brand`
- `offers`
  - `price` (할인 중이면 할인가)
  - `priceCurrency: "KRW"`
  - `availability` — 재고 있으면 `InStock`, 품절이면 `OutOfStock`
  - `url` — 절대경로
- 리뷰가 있는 상품만 `aggregateRating` 을 넣으세요.
  **리뷰가 없는데 빈 값이나 0을 넣으면 안 됩니다.** 구글이 잘못된 데이터로 판단합니다

**가격과 재고 상태가 들어가야 검색 결과에 가격이 표시됩니다.** 클릭률에 직접 영향이 있습니다.

## 3-3. 카카오톡 공유 썸네일 (Open Graph)

손님 대부분이 카카오톡으로 링크를 주고받습니다. 지금 공유했을 때 썸네일이 제대로 나오는지 확인하세요.

- 모든 페이지에 `og:title` · `og:description` · `og:image` · `og:url` · `og:type`
- **상품 상세는 대표 이미지가 `og:image`로** 들어가야 합니다
- 브랜드 페이지는 브랜드 대표 이미지
- `og:image`는 **반드시 절대경로(https://)** 여야 합니다. 상대경로면 카카오톡에서 안 나옵니다
- 권장 크기 1200×630. R2 이미지에 리사이즈 파라미터를 쓸 수 있으면 그렇게 처리하세요
- 이미지가 없는 페이지는 사이트 기본 이미지로 대체
- `twitter:card` 도 함께 넣어 주세요 (`summary_large_image`)

**`lib/store.ts`의 `SITE_URL`을 기준으로 절대경로를 만드세요.**
도메인이 `jzl.kr`로 바뀔 때 한 곳만 고치면 되도록 하드코딩하지 마세요.

## 3-4. 이미지 대체 텍스트(alt)

상품·브랜드 이미지의 `alt`가 비어 있거나 `image`, `thumbnail` 같은 무의미한 값이면
**`브랜드명 상품명`** 형태로 채우세요. 이미지 검색 유입과 접근성 양쪽에 영향이 있습니다.

장식용 이미지는 `alt=""`로 두는 것이 맞습니다. 억지로 채우지 마세요.

## 3-5. 404 페이지

`app/not-found.tsx` 가 있는지 확인하세요. 없으면 사이트 디자인에 맞춰 만드세요.

- 짧은 안내 문구 (사과조로 길게 쓰지 말 것)
- `홈으로` · `전체 상품 보기` 링크
- 기본 Next.js 흑백 화면이 그대로 나오면 안 됩니다

---

# [4] 상품 목록 카드 — 마우스를 올리면 두 번째 이미지

## 배경

패션몰에서 효과가 큰 장치입니다. 정면 컷 위에 마우스를 올리면 착용 컷이 나오는 식이죠.
확대보다 정보량이 많고, **서버 부담이 전혀 없습니다.** 이미 받아둔 이미지를 바꿔 보여줄 뿐입니다.

## 조치

- 상품에 이미지가 **2장 이상이면** 마우스를 올렸을 때 두 번째 이미지로 부드럽게 전환
  (opacity 교차, 200~300ms)
- 이미지가 **1장뿐이면** `transform: scale(1.04)` 정도로 살짝 확대만
- 마우스를 떼면 원래대로

## 주의

- **모바일에는 마우스 올리기가 없습니다.** 터치 기기에서는 아무 일도 일어나지 않아야 합니다.
  `@media (hover: hover)` 로 감싸세요. 터치했을 때 이미지가 바뀌면 오히려 방해가 됩니다
- 두 번째 이미지를 **미리 다 불러오지 마세요.** 목록에 상품이 많으면 전송량이 커집니다.
  `loading="lazy"` 를 유지하거나, 마우스가 올라온 뒤 불러오게 하세요
- 카드 높이가 바뀌면 안 됩니다. 목록 전체가 흔들립니다
- 이미지가 바뀌는 동안 상품명·가격은 그대로 있어야 합니다

---

# [5] 상품 상세 — 눌러서 전체화면 이미지 보기

## 배경

옷은 소재와 디테일을 봐야 삽니다. 지금은 작은 이미지로만 판단해야 합니다.

**마우스를 올리면 돋보기로 확대되는 방식은 쓰지 마세요.**
모바일에는 마우스 올리기가 없어 손님 대부분이 쓸 수 없고, 큰 원본을 미리 받아야 해서 전송량만 늘어납니다.

## 조치

이미지를 **누르면 전체화면으로 열리는 뷰어**를 만드세요.

- 상품 상세의 이미지를 누르면 전체화면으로 열림
- 배경은 어둡게, 닫기 버튼은 오른쪽 위
- 좌우로 넘겨 다른 이미지 보기 (모바일은 스와이프, 데스크톱은 화살표 버튼 + 키보드 좌우키)
- `Esc` 키로 닫기, 배경을 눌러도 닫기
- 하단에 `3 / 8` 처럼 현재 위치 표시
- **모바일** — 손가락으로 벌려서 확대(핀치 줌)
- **데스크톱** — 휠로 확대, 확대된 상태에서 끌어서 이동

## 성능

- **뷰어를 열었을 때만 큰 이미지를 불러오세요.** 페이지 로드 시점에 미리 받지 마세요
- 열려 있는 동안 배경 스크롤을 막으세요 (`overflow: hidden`)
- 닫으면 원래 스크롤 위치로 돌아와야 합니다

## 접근성

- 열렸을 때 포커스가 뷰어 안에 머물게 하세요
- 닫으면 원래 눌렀던 이미지로 포커스 복귀
- 닫기 버튼에 `aria-label="닫기"`

## 하단 구매 바와의 관계

3-I에서 만든 모바일 하단 구매 바가 있습니다.
**뷰어가 열려 있는 동안에는 구매 바와 맨 위로 버튼을 숨기세요.** 이미지 위에 겹치면 안 됩니다.

---

# [6] 메인 히어로에 보조 버튼 추가

## 배경

지금은 `컬렉션 보기` 버튼 하나뿐입니다.
바로 위 문장이 "편집숍입니다"라고 소개하는데, **그 편집숍이 어떤 곳인지 볼 길이 없습니다.**
`/about` 페이지를 새로 다듬어 놓았으니 메인에서 바로 들어갈 수 있어야 합니다.

## 조치

`컬렉션 보기` 버튼 **오른쪽에** 두 번째 버튼을 나란히 두세요.

```
[ 컬렉션 보기 ]   [ 편집숍 소개 ]
   ink 배경          paper 배경
   흰 글씨           ink 글씨 + ink 테두리
```

- 문구: `편집숍 소개`
- 링크: `/about`
- **흰 바탕(paper)에 검정 글씨(ink), 1px 테두리(ink)**
- 첫 번째 버튼과 **높이·좌우 여백·글자 크기가 완전히 같아야 합니다.** 나란히 놓았을 때 어긋나면 어색합니다
- 그림자 없음
- 마우스를 올리면 배경이 ink, 글씨가 paper로 반전 (200ms)

## 모바일

화면이 좁으면 두 버튼이 붙어서 답답해집니다.

- 좁은 화면에서는 **위아래로 쌓되 각각 가로 전체를 차지하지 않게** 하세요
- 또는 나란히 두되 각각 절반 너비
- 어느 쪽이든 글자가 줄바꿈되지 않아야 합니다

## 관리자 문구 연동

두 버튼 모두 문구와 링크를 관리자에서 고칠 수 있어야 합니다.
`디자인 관리 → 사이트 문구 → 메인 화면 → 메인 히어로 문구` 에 항목을 추가하세요.

| 항목 | 기본값 |
|---|---|
| 첫 번째 버튼 문구 | `컬렉션 보기` |
| 첫 번째 버튼 링크 | `/products` |
| 두 번째 버튼 문구 | `편집숍 소개` |
| 두 번째 버튼 링크 | `/about` |

**두 번째 버튼 문구를 비우면 버튼 자체가 안 나오게** 하세요. 운영자가 원할 때 뺄 수 있어야 합니다.

---

# [7] 성능·군더더기 정리

오픈이 가까워졌습니다. 지금 정리해 두어야 나중에 손대기 어려워집니다.

## ⚠️ 삭제할 때 지킬 것

**확실한 것만 지우세요.** 조금이라도 애매하면 지우지 말고 목록으로 보고하세요.
`혹시 쓰일지도 모르니 지웠다`는 판단은 하지 마세요. 지금 잘 돌아가는 것을 망가뜨리는 것이 가장 나쁩니다.

특히 아래는 **절대 건드리지 마세요.**

- Supabase 테이블·컬럼·RLS 정책
- `supabase/` 폴더의 기존 SQL 파일
- `vercel.json`의 cron (`0 19 * * *` 유지)
- 결제·주문·회원 관련 로직
- 체험단 후기 고지 문구

## 7-1. 클라이언트 번들 점검

- `'use client'` 가 붙어 있지만 **실제로 상태나 이벤트를 쓰지 않는 컴포넌트**를 찾아 서버 컴포넌트로 되돌리세요.
  이게 첫 화면 속도에 가장 크게 영향을 줍니다
- 무거운 라이브러리가 클라이언트 번들에 딸려 들어오는 곳이 있는지 확인하세요
- `next build` 결과의 First Load JS 크기를 **작업 전후로 비교해서 보고**하세요

## 7-2. 이미지

- 모든 이미지가 `next/image` 를 쓰고 있는지 확인하세요. `<img>` 가 남아 있으면 바꾸세요
- `sizes` 속성이 빠진 곳을 채우세요. 없으면 필요 이상으로 큰 이미지를 내려받습니다
- 첫 화면에 보이는 이미지(히어로·상품 목록 첫 줄)에만 `priority`, 나머지는 `lazy`
- R2 WebP 변환이 모든 업로드 경로에 걸려 있는지 확인하세요

## 7-3. 폰트

Pretendard · Gowun Batang · Cormorant Garamond 세 종류를 씁니다.

- 안 쓰는 굵기(weight)를 불러오고 있으면 빼세요
- `display: swap` 이 걸려 있는지 확인
- 한글 폰트는 용량이 큽니다. 서브셋이 가능하면 적용하세요
- **다만 화면에서 글자 모양이 바뀌면 안 됩니다.** 바꾼 뒤 눈으로 확인하세요

## 7-4. 데이터 조회

- `select *` 로 필요 없는 컬럼까지 가져오는 곳을 찾아 필요한 컬럼만 지정하세요.
  (3-F에서 `LIST_COLUMNS` 가 실제 컬럼과 어긋나 목록 필터가 깨진 적이 있습니다. **컬럼명을 반드시 실제 스키마와 대조**하고, 작업 후 `npm run check:schema` 를 돌리세요)
- 목록 화면에서 상품마다 따로 조회하는 N+1 패턴이 있으면 한 번에 가져오도록 고치세요
- 정적 생성(ISR)이 걸려 있어야 할 페이지가 매번 서버에서 도는 상태는 아닌지 확인하세요.
  `npm run build` 출력의 ○(정적) / ƒ(동적) 표시를 보고 **예상과 다른 페이지가 있으면 보고**하세요

## 7-5. 군더더기

- 쓰이지 않는 파일·컴포넌트·유틸 함수 (import 되는 곳이 한 군데도 없는 것만)
- `package.json` 에서 실제로 안 쓰는 의존성
- 운영 코드에 남은 `console.log` (오류 로그는 남겨 두세요)
- 주석 처리된 채 방치된 코드 덩어리
- `/images/main/about.jpg` 처럼 더 이상 참조되지 않는 정적 파일

**각 항목을 지운 이유와 함께 목록으로 보고하세요.**

## 7-6. 관리자 화면 속도

관리자 화면이 느립니다. 원인은 **Vercel 미국 리전 ↔ Supabase 서울 왕복**입니다.
리전 전환은 유료 플랜이 필요해 이번 범위 밖입니다.

다만 코드로 줄일 수 있는 부분이 있으면 처리하세요.

- 한 화면에서 순차적으로 여러 번 왕복하는 곳을 병렬(`Promise.all`)로
- 화면에 안 쓰는 데이터를 미리 불러오는 곳 제거
- 목록 화면의 페이지당 건수가 과한 곳 조정

**구조를 크게 바꾸지는 마세요.** 명백한 것만 손보고, 나머지는 "이렇게 하면 더 빨라진다"고 보고만 하세요.

---

# [8] 확인 사항

작업이 끝나면 아래를 확인하고 보고하세요.

1. 메인에서 HOW TO ORDER 섹션이 사라졌고, 섹션 간 여백이 어색하지 않은가
2. 관리자에 HOW TO ORDER 문구 항목이 **남아 있고**, 노출되지 않는다는 안내가 붙었는가
3. CATEGORY 설명이 사라졌고, 관리자에서 다시 채우면 나오는가
4. `sitemap.xml`에 비노출 상품·브랜드가 들어가 있지 않은가
5. `robots.txt`에서 `/admin`이 막혀 있는가
6. 상품 상세의 JSON-LD에 가격과 재고 상태가 들어갔는가
7. 상품 상세 링크를 카카오톡에 보냈을 때 썸네일과 제목이 나오는가
8. 목록 카드 호버가 **데스크톱에서만** 동작하는가 (터치 기기에서는 반응 없음)
9. 이미지가 1장인 상품에서 호버가 어색하지 않은가
10. 전체화면 뷰어가 모바일·데스크톱 양쪽에서 열리고 닫히는가
11. 뷰어가 열려 있을 때 하단 구매 바와 맨 위로 버튼이 숨는가
12. `npm run build` 통과 (Type error·Failed to compile 0건)
13. `npm run check:schema` 통과
14. 메인 히어로에 버튼 두 개가 높이·여백 어긋남 없이 나란히 있는가
15. 모바일에서 두 버튼이 답답하지 않고 글자가 줄바꿈되지 않는가
16. 두 번째 버튼 문구를 비우면 버튼이 사라지는가
17. 정리 작업 후에도 화면이 **작업 전과 똑같이** 보이는가 (글자 모양·이미지·간격 포함)
18. First Load JS 크기가 작업 전보다 줄었는가 (숫자로 보고)

---

# [9] 마무리

- 커밋하고 푸시하세요
- `vercel.json`의 cron이 `0 19 * * *`인지 커밋 전에 다시 확인하세요
- 마지막에 한국어로 알려주세요:
  1) 실행해야 할 SQL이 있으면 파일명과 순서 (없으면 없다고)
  2) [3]번 항목 각각에 대해 **이미 있었던 것 / 새로 만든 것**을 구분해서
  3) 명세대로 하지 않은 것이 있으면 그 이유
  4) **정리하면서 지운 것 목록과 각각의 이유**
  5) **지울까 고민했지만 확신이 없어 남겨 둔 것 목록** (제가 판단하겠습니다)
  6) First Load JS 크기 — 작업 전 / 작업 후
  7) 이번에 만들지 않은 것 (다음 단계)
