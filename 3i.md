# JZL CLOSET 3단계-I 작업 명세

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- 고객 화면 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #55524E / wine #6A2E3C
- 고객 화면 폰트: Pretendard(본문) · Gowun Batang(국문 제목) · Cormorant Garamond(영문 디스플레이)
- **그림자 금지, 이모지 금지** (아이콘은 SVG로 직접 그릴 것)
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role
- 새 SQL이 필요하면 `supabase/schema-3i.sql`로 만들고 실행 순서를 보고할 것
- **`vercel.json`의 cron은 `0 19 * * *`(하루 1회)를 반드시 유지하세요.**
  Hobby 플랜은 하루 1회만 허용하며, `*/10 * * * *`로 되돌리면 **배포 자체가 거부됩니다.**
- 체험단 후기의 `※ 제품을 무상으로 제공받아 작성된 후기입니다` 문구는 표시광고법상 필수입니다. 제거 금지.
- 작업 후 `npm run build`와 `npm run check:schema`를 실행해 확인할 것

## 사업 구조 (설계 판단의 전제)

JZL CLOSET은 **자체 제작 브랜드가 아니라 병행수입 편집숍**입니다.

```
/about              편집숍 JZL CLOSET 소개    ← 이번 작업 대상
/brands             취급 브랜드 목록
/brand/{slug}       개별 브랜드 소개 + 해당 브랜드 상품 목록
/brand/jzl-closet   자체 기획 라인 (현재 비노출)
```

`/about`과 `/brand/jzl-closet`은 성격이 다른 페이지입니다. **통합하지 마세요.**

---

# [1] `/about` 편집숍 소개 — 구조 개편 + 관리자 편집 기능

## 1-1. 현재 문제

두 가지입니다.

**첫째, 관리자에서 손댈 수 없습니다.**
3-H에서 사이트 문구를 관리자로 옮겼는데 `/about`만 빠졌습니다. 지금은 코드에 하드코딩되어 있습니다.

**둘째, 페이지가 너무 깁니다.**
이미지가 크고 글이 길어 손님이 끝까지 내려가지 않습니다. 읽히지 않으면 없는 것과 같습니다.

## 1-2. 새 구조

아래 순서로 다시 짜세요.

```
① 대표 이미지 + 제목 + 부제
② 소개 본문        (짧게 — 2~3문단)
③ 고르는 기준      (짧게 — 2문단)
④ 취급 브랜드      ← 새로 만드는 부분 (핵심)
⑤ 문의 안내        (카카오 오픈채팅 버튼)
```

**기존의 `취급 품목` 텍스트 섹션은 ④ 취급 브랜드로 대체합니다.**
글로 "의류·가방·슈즈·액세서리를 취급합니다"라고 쓰는 것보다, 실제 취급 브랜드를 보여주는 편이 훨씬 설득력 있습니다.

## 1-3. ④ 취급 브랜드 섹션 (새로 만들 것)

`brands` 테이블에서 **노출 ON인 브랜드를 전부 읽어** 격자로 나열하세요.

- 브랜드 로고가 있으면 로고, 없으면 브랜드명을 Cormorant Garamond로 표시
- 각 항목을 누르면 **`/brand/{slug}`** 로 이동
  (이 페이지에 브랜드 소개와 해당 브랜드 상품 목록이 함께 있으므로 판매 페이지 역할을 합니다)
- 데스크톱 4~5열, 모바일 2~3열
- 섹션 맨 아래에 `브랜드 전체 보기 → /brands` 링크
- 브랜드가 하나도 없으면 섹션 자체를 숨길 것

`/brands` 페이지에 이미 브랜드 카드 컴포넌트가 있으면 **그것을 재사용**하세요. 새로 만들지 마세요.

## 1-4. 이미지 크기 조정

대표 이미지가 화면을 너무 차지하고 있습니다.

- 모바일에서 이미지 높이가 화면의 40%를 넘지 않게 하세요
- 데스크톱은 좌우로 넓게, 세로로는 짧게 (배너 비율)
- 이미지가 등록되지 않았으면 이미지 영역 자체를 숨기고 제목부터 시작할 것

## 1-5. 관리자 항목 신설

`관리자 → 디자인 관리 → 사이트 문구` 안에 **`편집숍 소개`** 그룹을 만드세요.
3-H에서 만든 문구 관리 구조(`getCachedCopy` 등)를 그대로 재사용합니다.

| 항목 | 형식 |
|---|---|
| 대표 이미지 | 이미지 업로드 (R2, 기존 파이프라인 재사용) |
| 제목 | 한 줄 |
| 부제 | 한 줄 |
| 소개 본문 | 여러 문단 |
| 고르는 기준 제목 | 한 줄 |
| 고르는 기준 본문 | 여러 문단 |
| 취급 브랜드 제목 | 한 줄 (기본값: `취급 브랜드`) |
| 취급 브랜드 안내문 | 한 줄 (선택, 비우면 안 나옴) |
| 문의 안내 문구 | 여러 문단 |

**중요 — 문구를 새로 지어내지 마세요.**
`app/(shop)/about/page.tsx`(실제 경로 확인)를 열어 지금 있는 문구를 읽고, **그것을 기본값으로 삼으세요.**
운영자가 아직 저장하지 않은 상태에서는 문구가 그대로 남아 있어야 합니다.

기존 `취급 품목` 텍스트는 섹션이 사라지지만, 그 내용을 **소개 본문 기본값 끝에 한 문단으로 합쳐** 두세요. 정보가 사라지면 안 됩니다.

## 1-6. 각 항목에 `기본값으로 되돌리기` 버튼

3-H와 동일하게 항목마다 되돌리기 버튼을 두세요.

## 1-7. 문의 영역

`/about` 하단 문의 안내에 전화 버튼이 있으면 **카카오톡 실시간 문의 버튼(오픈채팅)** 으로 교체하세요.
관리자 설정의 오픈채팅 링크를 읽어 씁니다. 3-G에서 만든 컴포넌트를 재사용하세요.

푸터 고객센터 전화번호는 그대로 둡니다(법적 표시 의무).

## 1-8. 저장 시 즉시 반영 · SEO

- 저장하면 `revalidatePath('/about')`으로 즉시 반영
- `generateMetadata`로 title·description을 관리자 문구에서 가져올 것
- 대표 이미지를 `og:image`로 연결
- 정적 생성(ISR)을 깨뜨리지 말 것

---

# [2] HOW TO ORDER 문구 — 관리자에서 수정 가능한지 확인

## 배경

운영자가 메인의 `HOW TO ORDER` 문구를 바꾸려는데 관리자에서 찾지 못하고 있습니다.

## 조치

1. 이 섹션의 문구가 **관리자에서 편집 가능한 상태인지 확인**하세요.
2. **가능하다면** — 관리자 화면 어느 메뉴 어느 항목인지 정확한 경로를 마지막 보고에 적으세요.
   (예: `디자인 관리 → 사이트 문구 → 메인 화면 → HOW TO ORDER 01 제목`)
3. **불가능하다면** — 위 `편집숍 소개`와 같은 방식으로 관리자 항목을 신설하세요.
   - 라벨 / 제목
   - 01·02·03 각각의 제목과 본문
   - 각 항목에 `기본값으로 되돌리기` 버튼
   - 현재 문구를 기본값으로 삼을 것

어느 쪽이든 **찾기 쉬운 자리**에 두세요. 관리자가 항목을 못 찾으면 없는 기능과 같습니다.

---

# [3] 모바일 하단 고정 구매 바 (신설)

## 배경

모바일에서 상품 상세를 아래로 내리면 구매 버튼이 화면 밖으로 사라집니다.
상세 설명이 긴 상품은 다 읽고 나서 다시 맨 위로 올라가야 합니다.

## 3-1. 동작

**모바일에서만** 표시합니다. 데스크톱은 지금 그대로 두세요.
(데스크톱은 우측 구매 영역 높이가 1338px로 뷰포트를 넘어 sticky가 부적절하다는 것이 이미 확인되었습니다.)

- 화면 하단에 고정
- 상단의 원래 구매 버튼이 **화면에서 벗어나면 나타나고**, 다시 보이면 사라짐
  (스크롤 이벤트 대신 `IntersectionObserver` 사용)
- 나타나고 사라질 때 부드럽게 (transform + opacity, 200ms 내외)

## 3-2. 구성

```
┌─────────────────────────────────────┐
│  39,000원      [장바구니]  [바로구매]  │
└─────────────────────────────────────┘
```

- 왼쪽에 판매가 (할인 중이면 할인가 기준)
- `장바구니` — 외곽선 버튼
- `바로구매` — 채운 버튼 (ink 배경)
- 두 버튼 모두 **기존 구매 버튼과 완전히 동일한 동작**을 해야 합니다. 로직을 새로 쓰지 말고 기존 핸들러를 재사용하세요.

## 3-3. 옵션 미선택 처리

옵션(사이즈·색상)을 아직 안 고른 상태에서 누르면,
**옵션 선택 영역으로 스크롤을 올리고 안내를 표시**하세요. 그냥 실패시키지 마세요.

## 3-4. 품절 상품

품절이면 버튼을 비활성으로 두고 `품절`이라고 표시하세요.

## 3-5. 아이폰 하단 여백

`padding-bottom: env(safe-area-inset-bottom)` 을 반드시 적용하세요.
홈 인디케이터 막대와 버튼이 겹치면 안 됩니다.

## 3-6. ⚠️ 맨 위로 버튼과의 충돌 (중요)

`0aff4e4`에서 만든 `components/ScrollToTop.tsx` 버튼이 같은 자리에 있습니다.

**하단 구매 바가 떠 있는 동안에는 맨 위로 버튼을 그 위로 밀어 올리세요.**
겹치거나 가려지면 안 됩니다.

- 구매 바 높이만큼 `bottom` 값을 올릴 것
- 구매 바가 사라지면 원래 위치로 복귀
- 값을 하드코딩하지 말고 CSS 변수나 상태로 연동하세요

상품 상세 이외의 페이지에서는 맨 위로 버튼이 지금 그대로 동작해야 합니다.

---

# [4] 확인 사항

작업이 끝나면 아래를 확인하고 보고하세요.

1. 관리자에서 아무것도 저장하지 않은 상태에서 `/about`의 문구가 **작업 전과 동일한가** (구조는 바뀌어도 글이 사라지면 안 됨)
2. `/about` 취급 브랜드에서 브랜드를 누르면 `/brand/{slug}`로 가는가
3. 노출 OFF인 브랜드가 `/about`에 나오지 않는가
4. 관리자에서 문구를 바꾸고 저장하면 즉시 반영되는가
5. `기본값으로 되돌리기`가 정상 작동하는가
6. 모바일에서 상품 상세를 내리면 하단 구매 바가 나타나는가
7. 하단 구매 바가 떠 있을 때 맨 위로 버튼이 가려지지 않는가
8. `npm run build` 통과 (Type error·Failed to compile 0건)
9. `npm run check:schema` 통과

---

# [5] 마무리

- 커밋하고 푸시하세요
- `vercel.json`의 cron이 `0 19 * * *`인지 커밋 전에 다시 확인하세요
- 마지막에 한국어로 알려주세요:
  1) 실행해야 할 SQL 파일이 있으면 파일명과 순서
  2) 관리자 어디에 새 항목이 생겼는지 (메뉴 경로를 정확히)
  3) **HOW TO ORDER 문구를 수정하는 정확한 위치**
  4) `/about`에서 항목화하지 않은 것이 있다면 그 이유
  5) 이번에 만들지 않은 것 (다음 단계)
