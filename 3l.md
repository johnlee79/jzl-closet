# JZL CLOSET 3단계-L 작업 명세

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- 고객 화면 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #55524E / wine #6A2E3C
- 고객 화면 폰트: Pretendard(본문) · Gowun Batang(국문 제목) · Cormorant Garamond(영문 디스플레이)
- **그림자 금지, 이모지 금지** (아이콘은 SVG로 직접 그릴 것)
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role
- 새 SQL이 필요하면 `supabase/schema-3l.sql`로 만들고 실행 순서를 보고할 것.
  필요 없으면 억지로 만들지 말 것
- **`vercel.json`의 cron은 `0 19 * * *`(하루 1회)를 반드시 유지하세요.**
- 체험단 후기의 `※ 제품을 무상으로 제공받아 작성된 후기입니다` 문구는 표시광고법상 필수입니다. 제거 금지.
- 작업 후 `npm run build`와 `npm run check:schema`를 실행해 확인할 것

---

# [1] 메인 CATEGORY · BRANDS 가로 스크롤

## 배경

지금은 격자 배치라 항목이 늘어나면 무너집니다.
분류가 다섯 개가 되면 한 줄에 네 개, 다음 줄에 하나가 덩그러니 남습니다.
브랜드는 이미 14개라 세로로 길게 늘어집니다.

**자동으로 흐르는 방식은 쓰지 마세요.**
손님이 누르려는 순간 이동해버려 오히려 방해가 되고, 읽는 속도는 사람마다 다릅니다.
직접 밀어서 넘기는 방식으로 만드세요.

## 1-1. CATEGORY 섹션

- 노출 중인 분류가 **4개 이하면 지금처럼 격자** 그대로 두세요
- **5개 이상이면 가로 스크롤**로 전환
- 오른쪽 끝 항목이 살짝 잘려 보이게 해서 "더 있다"는 것이 전달되게 하세요
- 모바일 — 손가락으로 밀기
- 데스크톱 — 좌우 화살표 버튼. 맨 처음·맨 끝에서는 해당 방향 화살표를 감출 것
- 스크롤바는 숨기되 **키보드 조작은 살려 두세요** (`overflow-x: auto` 유지)

## 1-2. BRANDS 섹션

같은 방식으로 가로 스크롤을 적용하세요. 브랜드는 지금도 14개라 바로 적용됩니다.

- 브랜드 카드 크기는 분류보다 작게 (로고 또는 브랜드명 위주)
- 항목이 적을 때(4개 이하) 왼쪽으로 몰리지 않고 자연스럽게 보이도록 처리

## 1-3. 공통 주의

- **한 손가락으로 세로 스크롤이 막히면 안 됩니다.** 가로 영역 위에서 위아래로 밀면 페이지가 스크롤돼야 합니다
- 스크롤 위치가 항목 경계에 맞게 멈추면 보기 좋습니다 (`scroll-snap`)
- 카드 높이가 서로 달라 들쭉날쭉하지 않게 하세요
- 기존에 가로 스크롤을 쓰는 컴포넌트가 있으면 **그것을 재사용**하세요. 새로 만들지 마세요

---

# [2] `/order` · 장바구니 하드코딩 문구를 관리자로

## 배경

앞선 조사에서 `/order` 페이지와 장바구니의 문구 상당수가 코드에 박혀 있는 것이 확인되었습니다.
가장 눈에 띄는 헤더와 가장 중요한 버튼 옆 안내가 모두 관리자 밖에 있습니다.

**곧 PG 카드결제가 붙습니다.** 지금 무통장입금이라고 적힌 문구들이 그대로 남으면 손님에게 거짓말이 됩니다.
그때 코드를 고치지 않고 관리자에서 문구만 바꿀 수 있어야 합니다.

## 2-1. 옮길 것

| 파일 | 줄 | 문구 |
|---|---|---|
| `app/(shop)/order/page.tsx` | 36 | `장바구니 · 주문 문의` (h1) |
| 〃 | 39–40 | `담으신 상품을 확인하고 주문서를 작성해 주세요…` |
| 〃 | 43–47 | `이미 주문하셨다면 주문 조회에서…` (링크 포함) |
| `components/CartPanel.tsx` | 86–88 | 빈 장바구니 안내 |
| 〃 | 258–261 | `무통장입금(계좌이체)으로 결제합니다…` |
| 〃 | 269–271 | `온라인 주문이 어려우시면 위 내용을 복사해…` |

기존 문구 관리 구조(`getCachedCopy` 등)를 그대로 재사용하고,
**지금 화면에 나오는 문구를 그대로 기본값으로** 삼으세요. 새로 지어내지 마세요.

관리자 위치는 `디자인 관리 → 사이트 문구 → 주문·배송 안내` 그룹 안에 두세요.
각 항목에 **화면 어디에 나오는 문구인지** 한 줄로 적어 주세요.

## 2-2. ⚠️ 중복 제거 (이번 작업의 핵심)

`CartPanel.tsx` 258–261의 무통장입금 안내와,
관리자에서 이미 고칠 수 있는 `copy.order` 의 `결제 안내` 가 **사실상 같은 말을 두 곳에서 하고 있습니다.**

한쪽만 고치면 같은 화면에서 서로 다른 말을 하게 됩니다. **PG를 붙일 때 반드시 사고가 납니다.**

**하나로 합치세요.** 어느 쪽을 남길지는 화면 흐름을 보고 판단하되,
결과적으로 **결제 수단 안내가 관리자 한 곳에서만 관리되어야 합니다.**
어떻게 합쳤는지 마지막에 보고하세요.

## 2-3. 옮기지 않을 것

아래는 **그대로 두세요.** 관리자 항목이 많아지면 운영자가 오히려 못 찾습니다.

- UI 라벨 — `삭제` `합계` `주문 요약` `무료` `주문하기` `장바구니 비우기`
- 영문 라벨 — `CART & ORDER` `HOW TO ORDER` `RECENTLY VIEWED`
- 로딩·오류 문구 — `장바구니를 불러오는 중입니다` `자동 복사가 되지 않는 브라우저입니다`
- 복사용 주문서 서식 — 화면 문구가 아니라 카카오톡으로 나가는 양식입니다. 별도 설계가 필요하므로 이번 범위 밖

## 2-4. 메타데이터

`/order` 페이지의 `title` · `description` 도 관리자 문구를 쓰도록 연결하세요.

---

# [3] 확인 사항

작업이 끝나면 아래를 확인하고 보고하세요.

1. 분류가 4개일 때 격자, 5개 이상일 때 가로 스크롤로 바뀌는가
2. 브랜드 14개가 가로 스크롤로 나오는가
3. 가로 스크롤 영역 위에서 위아래로 밀었을 때 페이지가 정상 스크롤되는가
4. 데스크톱에서 화살표가 맨 처음·맨 끝에서 알맞게 감춰지는가
5. 관리자에서 아무것도 저장하지 않은 상태에서 `/order`·장바구니 문구가 **작업 전과 동일한가**
6. 결제 수단 안내가 **한 곳에서만** 관리되는가 (같은 말이 두 번 나오지 않는가)
7. 관리자에서 문구를 바꾸고 저장하면 즉시 반영되는가
8. `npm run build` 통과 (Type error·Failed to compile 0건)
9. `npm run check:schema` 통과

---

# [4] 마무리

- 커밋하고 푸시하세요
- `vercel.json`의 cron이 `0 19 * * *`인지 커밋 전에 다시 확인하세요
- 마지막에 한국어로 알려주세요:
  1) 실행해야 할 SQL이 있으면 파일명과 순서 (없으면 없다고)
  2) 관리자 어디에 새 항목이 생겼는지 (메뉴 경로를 정확히)
  3) **결제 수단 안내 중복을 어떻게 합쳤는지**
  4) 명세대로 하지 않은 것이 있으면 그 이유
  5) 이번에 만들지 않은 것 (다음 단계)
