# JZL CLOSET 3단계-D 작업 명세 — 셀스타 상품 가져오기

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- 고객 화면 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #55524E / wine #6A2E3C
- 관리자 화면은 Pretendard 단일 폰트, 다크모드 지원 (3-C에서 구현됨)
- **그림자 금지, 이모지 금지**
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role
- 새 SQL은 `supabase/schema-3d.sql`, 필요시 `supabase/rls-3d.sql`로 만들고 실행 순서 보고
- 새 DB 함수를 만들면 rls-3b.sql의 pg_proc 조회 방식으로 service_role 전용 잠금
- **`vercel.json`의 cron은 `0 19 * * *`(하루 1회)를 유지하세요.** Hobby 플랜은 하루 1회만 허용하며,
  `*/10 * * * *`로 되돌리면 **배포 자체가 거부됩니다.** Pro 전환 후에 변경합니다.

---

# 배경

JZL CLOSET 운영자는 도매 플랫폼 **셀스타(sellstar.kr)의 운영자**이며,
리셀러 스토어 **마르크앤코(marquenco)** 도 직접 운영합니다.
이미지·상품정보의 공동 사용이 허가된 상태이므로 권리 문제는 없습니다.

현재 상품을 한 건씩 손으로 등록하고 있어 시간이 오래 걸립니다.
셀스타 상품 주소를 붙여넣으면 정보와 이미지를 자동으로 가져오는 기능을 만듭니다.

---

# [1] 셀스타 API 연동

## 확인된 API

```
GET https://api.i-one.kr/api/front/v1/oneeasy/store/product/{id}
```

- 인증 불필요, 응답은 JSON
- `{id}`는 셀스타 상품번호 (예: 188)
- 화면 주소 `https://sellstar.kr/marquenco/product/188` 의 맨 뒤 숫자와 동일

## ★ 반드시 서버에서 호출할 것

응답 헤더가 `Access-Control-Allow-Origin: https://sellstar.kr` 로 제한되어 있습니다.
**브라우저에서 직접 fetch하면 CORS로 차단됩니다.**
반드시 Next.js 서버 라우트(예: `/api/admin/import/sellstar`)를 거쳐 호출하세요.

## 응답 구조 (실제 확인된 예시)

```json
{
  "code": 200,
  "message": "성공",
  "result": {
    "id": 188,
    "resellerId": 2,
    "productId": 113,
    "name": "가니 레오파드 반팔티",
    "content": "![](이미지URL)\n![](이미지URL)\n...",
    "price": 160000,
    "salePrice": 96000,
    "deliveryFee": 0,
    "hasOptions": "Y",
    "status": "ACTIVE",
    "mainImage": { "id": 4183, "path": "...", "width": 1000, "height": 1000 },
    "images": [ { "id": 4188, "path": "...", "width": 1000, "height": 1000 }, ... ],
    "detailImages": [],
    "optionGroups": [
      { "id": 586, "groupName": "컬러", "sortOrder": 1,
        "optionValues": [ { "id": 1641, "valueName": "화이트", "sortOrder": 1 } ] },
      { "id": 587, "groupName": "사이즈", "sortOrder": 2,
        "optionValues": [ { "id": 1642, "valueName": "XS", "sortOrder": 1 }, ... ] }
    ],
    "options": [
      { "id": 799, "productVariantId": 567, "optionLabel": "화이트 / XS",
        "salePrice": 96000, "optionValueIds": [1641, 1642] }, ...
    ],
    "variants": [
      { "id": 567, "status": "ACTIVE", "optionLabel": "화이트 / XS",
        "availableStock": 999,
        "variantOptions": [ { "optionValueId": 1641, "groupName": "컬러", "valueName": "화이트" }, ... ] }
    ],
    "store": { "storeId": "marquenco", "storeName": "MARQUE & CO. | 마르크앤코" },
    "shipping": {
      "policyType": "FIXED", "baseFee": 0,
      "extraJeju": 5000, "extraIsolated": 5000,
      "returnShippingFee": 3000, "exchangeShippingFee": 6000,
      "returnGuide": "...", "exchangeGuide": "...",
      "defaultCourier": "CJ", "cutoffTime": "14:00:00", "avgDeliveryDays": 2
    }
  }
}
```

## 파싱 시 주의점

1. **`content` 필드가 상세설명 이미지 목록입니다.**
   마크다운 이미지 문법 `![](URL)` 이 줄바꿈(`\n`)으로 구분되어 나열됩니다.
   `detailImages` 배열은 비어 있으니 `content`를 파싱해서 URL을 추출하세요.
   앞으로 텍스트가 섞여 들어올 수도 있으니, 이미지가 아닌 줄은 텍스트 블록으로 처리해 주세요.

2. **`.gif` 파일이 섞여 있습니다.** (예시 데이터의 6번째 이미지)
   이미지 처리 시 gif가 깨지거나 정지 이미지로 변환되지 않게 주의하세요.

3. **응답 구조가 바뀔 수 있습니다.**
   필드가 없거나 형태가 다를 때 에러로 죽지 말고, 가져온 것만 채우고
   "이 항목은 가져오지 못했습니다"로 안내한 뒤 수동 입력할 수 있게 하세요.

4. `code`가 200이 아니거나 네트워크 실패 시 명확한 에러 메시지를 화면에 표시하세요.

---

# [2] 관리자 → 상품 가져오기 화면

## 위치
관리자 사이드바 `상품 관리` 그룹에 **`상품 가져오기`** 메뉴 추가

## 2-1. 주소 입력

```
셀스타 상품 주소 또는 상품번호

[ https://sellstar.kr/marquenco/product/188        ]  [ 불러오기 ]

여러 개를 한 번에 가져오려면 줄바꿈으로 구분해 입력하세요.
```

- 전체 주소(`https://sellstar.kr/{store}/product/188`)와 숫자만(`188`) 둘 다 인식
- 여러 줄 입력 시 순차 처리, 각 건의 성공·실패를 목록으로 표시
- **이미 가져온 상품이면 경고** — 셀스타 상품번호를 저장해두고 중복 확인
  ("이미 등록된 상품입니다. 덮어쓰시겠습니까?" / 건너뛰기 선택)

## 2-2. 불러온 뒤 편집 화면

자동으로 채워지고, 모두 수정 가능해야 합니다.

**기본 정보**
- 상품명 (수정 가능)
- 셀스타 정가 / 판매가 → **참고용으로 표시**
- **JZL 판매가 입력란** — 기본값은 셀스타 판매가. 운영자가 보고 직접 결정합니다
- 정가(할인 전 가격) 입력란
- 분류·브랜드 선택 (기존 관리자 값에서 선택)

**옵션·재고**
- `optionGroups` → 옵션 그룹(컬러·사이즈)으로 자동 변환
- `variants` → 조합별 재고로 자동 변환 (availableStock 사용)
- `status`가 `ACTIVE`가 아닌 variant는 품절 처리
- 표로 보여주고 재고·판매가를 조합별로 수정 가능하게

**배송·반품 정보**
- `shipping`의 배송비·제주/도서 추가비·반품비·교환비·택배사·평균 배송일 표시
- 이 값을 상품별로 저장할지, 관리자 설정의 공통값을 쓸지 **선택할 수 있게** 하세요
  (기본은 공통값 사용 — 3-B에서 만든 판매정보 설정)

## 2-3. 상세페이지 구성 편집기 (핵심 기능)

가져온 상세 이미지를 세로로 나열하고, 자유롭게 편집합니다.

```
┌─────────────────────────────────────────┐
│ ☐  [이미지]  마르크앤코 브랜드 배너      │  ← 자동 체크 해제
│    ⓘ 리셀러 브랜드 이미지                │
├─────────────────────────────────────────┤
│              [ + 추가 ]                  │
├─────────────────────────────────────────┤
│ ☑  [이미지]  상품컷 1                    │
│    ⋮⋮ 드래그  ↑ ↓  🗑                    │
├─────────────────────────────────────────┤
│              [ + 추가 ]                  │
└─────────────────────────────────────────┘
```

**필수 기능**
1. **각 이미지에 체크박스** — 해제하면 등록에서 제외
2. **`RESELLER_PRODUCT` 경로 이미지는 자동으로 체크 해제**
   - 셀스타 이미지 URL에 `/RESELLER_PRODUCT/`가 포함되면 리셀러(마르크앤코) 브랜드 이미지입니다
   - `/PRODUCT/`는 실제 상품컷입니다
   - 자동 해제하되 운영자가 다시 켤 수 있어야 합니다
   - 해당 이미지에 "리셀러 브랜드 이미지" 표시를 달아 주세요
3. **이미지 사이마다 `+ 추가` 버튼** — 누르면 선택지:
   - **글 넣기** — SEO용 텍스트 블록 (줄바꿈·굵게·링크 정도의 간단한 편집)
   - **이미지 넣기** — 운영자가 직접 만든 배너·사이즈표 등 업로드
4. **드래그로 순서 변경**, 개별 삭제
5. 미리보기 — 실제 상품 페이지에서 어떻게 보일지 확인

## 2-4. 등록

- **임시저장 상태로 등록** (사이트에 노출되지 않음)
- 확인 후 관리자가 `판매중`으로 전환
- 등록 후 해당 상품 편집 화면으로 이동

---

# [3] 이미지 처리

## 3-1. R2로 복사 저장 (중요)

셀스타 이미지 주소를 그대로 쓰지 말고, **우리 R2 버킷에 복사해서 저장**하세요.

이유:
- 셀스타에서 이미지가 교체·삭제되면 JZL 상품 페이지가 깨집니다
- 외부 도메인 이미지는 Next.js 이미지 최적화가 제한됩니다
- 트래픽이 셀스타 쪽으로 발생합니다

## 3-2. 처리 방식

- 서버에서 이미지를 내려받아 R2에 업로드
- 원본 크기(width·height)를 함께 저장 — **CLS 방지에 필요합니다** (3-C에서 이미 적용된 방식)
- `.gif`는 원본 그대로 유지 (애니메이션 보존)
- 이미지가 21장 이상일 수 있으므로 **진행률 표시** (`7/21 처리 중...`)
- 일부 실패해도 나머지는 계속 진행하고, 실패한 것만 목록으로 안내

## 3-3. 대체텍스트 자동 생성

SEO에 중요하지만 손으로 넣기 번거롭습니다.
- 대표 이미지: `{상품명}`
- 갤러리: `{상품명} 상세 이미지 {번호}`
- 운영자가 수정할 수 있게 입력란 제공

---

# [4] 편의 기능

운영자가 매번 반복하는 작업을 줄입니다.

## 4-1. JZL 공통 블록 자동 삽입

관리자 설정에서 등록해두면, 가져오기 할 때마다 자동으로 붙습니다.

- **상단 블록** — 브랜드 배너 등 (이미지 또는 글)
- **하단 블록** — 배송·교환 안내, 브랜드 소개 등
- 각각 on/off, 상품마다 개별 해제 가능

## 4-2. 글 템플릿

자주 쓰는 SEO 문구를 저장해두고 클릭 한 번에 삽입합니다.

- 관리자 설정에서 템플릿 이름 + 내용 등록
- `+ 글 넣기` 클릭 시 저장된 템플릿 목록에서 선택 가능
- `{상품명}` 같은 치환 변수 지원

## 4-3. 마진 계산 보조

JZL 판매가를 정할 때 참고용으로 표시합니다.

- 셀스타 판매가 대비 `+10% / +20% / +30%` 빠른 계산 버튼
- 누르면 판매가 입력란에 자동 반영 (100원 단위 반올림)
- **자동 적용이 아니라 보조 도구입니다.** 운영자가 최종 결정합니다

## 4-4. 셀스타 원본 연결 유지

- 등록된 상품에 **셀스타 상품번호를 저장**
- 상품 편집 화면에 `셀스타에서 다시 불러오기` 버튼
- 누르면 가격·재고·옵션의 **현재 값과 셀스타 값을 나란히 비교**해서 보여주고,
  갱신할 항목만 선택해서 반영 (전체 덮어쓰기 금지 — 우리가 수정한 내용이 날아갑니다)

---

# [5] DB 변경

`supabase/schema-3d.sql`에 아래를 포함하세요.

- `products`에 셀스타 연동 정보 컬럼 추가
  - 셀스타 상품번호 (중복 확인·재조회용)
  - 마지막 동기화 시각
  - 셀스타 원본 가격(정가·판매가) — 마진 비교용
- 상세설명 블록에 이미지 원본 크기 저장 (3-C에서 도입한 구조 재사용)
- 글 템플릿 저장 테이블 (또는 site_settings에 JSON으로)
- 필요시 인덱스 추가

기존 상품에 영향이 없어야 하고, 여러 번 실행해도 안전해야 합니다.

---

# 작업 완료 후 보고할 것

1. 새 SQL 파일과 실행 순서
2. CORS 우회를 어떤 방식으로 처리했는지
3. 이미지 R2 복사가 21장 기준 얼마나 걸리는지 (Vercel 함수 시간 제한에 걸리지 않는지)
4. 관리자에서 새로 설정해야 하는 값 목록
5. 빌드 통과 여부와 커밋 해시
6. **`vercel.json`의 cron이 `0 19 * * *`로 유지되고 있는지 확인**

---

# 이번 작업에서 하지 않을 것

- CSV 대량 등록 (이 기능으로 대체)
- PG 결제 연동
- 카카오·네이버 로그인
- 셀스타 주문 연동 (지금은 상품 가져오기만)
