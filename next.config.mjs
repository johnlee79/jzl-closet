/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  experimental: {
    /**
     * ============================================================
     * 클라이언트 라우터 캐시를 끕니다 (동적 페이지)
     * ============================================================
     *
     * ★★ 왜 끄는가
     *   Next 14.2 는 링크로 옮겨 다닐 때 받아 둔 화면을 브라우저 안에 잠깐
     *   보관합니다. 동적 페이지의 기본값이 30초입니다.
     *   그래서 관리자가 사이드바에서 [주문 관리] 를 눌러 들어가면, 30초 안에
     *   한 번이라도 들렀던 적이 있으면 그때 받아 둔 목록을 그대로 보여 줍니다.
     *   방금 들어온 주문이 목록에 없습니다. 서버는 멀쩡히 최신 값을 주는데
     *   브라우저가 옛 화면을 다시 그리는 것이라, 서버 쪽을 아무리 뒤져도
     *   원인이 안 보입니다. (실제로 그렇게 한참 헤맸습니다)
     *
     *   force-dynamic 은 서버가 매번 새로 그리게 할 뿐, 이 보관함과는 무관합니다.
     *
     * ★ 0 으로 두면 링크로 옮길 때마다 서버에서 새로 받아 옵니다.
     *   정적 페이지(static)는 그대로 둡니다. 상품·분류처럼 미리 구워 두는 화면은
     *   보관해 두는 편이 빠르고, 내용이 바뀌면 revalidate 가 알아서 갈아 끼웁니다.
     *
     * ★ 이 설정이 필요한 화면
     *   관리자 전 화면 · 주문 조회 · 마이페이지 — 전부 "지금 이 순간" 이 중요한 곳입니다.
     *   손님 상품 화면은 정적이라 영향을 받지 않습니다.
     */
    staleTimes: {
      dynamic: 0,
    },
  },
};

export default nextConfig;
