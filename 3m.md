# JZL CLOSET 3단계-M 작업 명세

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- **`vercel.json`의 cron은 `0 19 * * *`(하루 1회)를 반드시 유지하세요.**
- 작업 후 `npm run build`와 `npm run check:schema`를 실행해 확인할 것
- 새 SQL이 필요하면 `supabase/schema-3m.sql`로 만들고 보고할 것. 필요 없으면 만들지 말 것

## 이 단계의 목적

도메인이 `https://jzl.kr` 로 확정되었습니다.
이제 검색엔진에 사이트를 등록하고 방문 통계를 수집합니다.

지금까지 만든 `sitemap.xml`, 구조화 데이터, 메타태그는 **검색엔진이 우리 사이트를 알아야 비로소 쓸모가 생깁니다.**

---

# [1] 검색엔진 소유확인 메타태그

## 1-1. 넣을 값

아래 두 개를 **모든 페이지의 `<head>`** 에 넣으세요. 루트 레이아웃에 두면 됩니다.

```html
<meta name="google-site-verification" content="oGI1IKtH999GKbaBoI8iDqW7XM_JBZ34UXpwR7vN-Gc" />
<meta name="naver-site-verification" content="61c6f3ea756a2dac39a6313e0f0e6baed34c2305" />
```

## 1-2. 구현 방식

Next.js 14 App Router에서는 `metadata` 객체의 `verification` 필드를 쓰는 것이 정석입니다.

```ts
verification: {
  google: '...',
  other: { 'naver-site-verification': '...' },
}
```

**다만 이 방식이 실제로 `<head>`에 태그를 내보내는지 반드시 빌드 산출물로 확인하세요.**
안 나오면 다른 방법을 쓰세요. 검증되지 않은 채로 넘어가면 인증이 실패합니다.

## 1-3. 값을 어디에 둘 것인가

**환경변수가 아니라 코드에 상수로 두세요.**

이유가 있습니다. 이 값들은 비밀이 아니라 **어차피 HTML에 공개되는 값**입니다.
환경변수로 빼면 Vercel 설정을 따로 건드려야 해서 오히려 번거롭고, 나중에 값이 왜 없는지 찾기 어려워집니다.

`lib/store.ts` 처럼 사이트 상수를 모아둔 곳에 두고, **어디에 쓰이는 값인지 주석**을 남기세요.

## 1-4. 확인

빌드 후 실제 산출물에서 두 태그가 `<head>` 안에 나오는지 확인하고 보고하세요.
`/`, `/products`, `/about` 등 여러 페이지에서 확인할 것 —
3-J에서 **페이지가 자체 `openGraph`를 정의하면 부모 메타데이터가 통째로 갈아 끼워지는 함정**이 있었습니다.
같은 일이 `verification`에도 일어나는지 반드시 확인하세요.

---

# [2] Google Analytics 4 연동

## 2-1. 측정 ID

```
G-H0XVMGX03D
```

## 2-2. 먼저 현재 상태를 확인할 것

**GA4 관련 코드가 이미 들어 있는지 먼저 확인하세요.**
과거에 넣어둔 흔적이 있으면 그것을 살려 쓰고, 중복으로 두 번 심지 마세요.
같은 페이지에서 두 번 집계되면 방문자 수가 두 배로 잡힙니다.

## 2-3. 구현

- Next.js의 `@next/third-parties/google` 의 `GoogleAnalytics` 컴포넌트를 쓰는 것이 가장 간단합니다.
  이미 의존성에 있으면 그것을 쓰고, 없으면 설치하거나 직접 `<Script>` 로 넣으세요
- 스크립트는 **`afterInteractive`** 로 불러 첫 화면 속도를 해치지 않게 하세요
- **개발 환경(`localhost`)에서는 집계되지 않게** 하세요. 실제 손님 데이터에 개발 중 방문이 섞이면 통계가 무의미해집니다
- App Router는 페이지 이동 시 새로고침이 없으므로, **화면 전환이 방문으로 집계되는지** 확인하세요

## 2-4. 전자상거래 이벤트는 이번 범위 밖

`view_item`, `add_to_cart`, `purchase` 같은 전자상거래 이벤트는 **이번에 넣지 마세요.**
PG 결제가 붙은 뒤에 실제 결제 흐름에 맞춰 넣어야 정확합니다.

지금은 **방문자 수와 유입 경로만** 잡히면 충분합니다.

## 2-5. 확인

- 빌드 산출물에 `G-H0XVMGX03D` 가 들어갔는지
- 개발 환경에서는 스크립트가 안 나가는지

---

# [3] robots.txt · sitemap.xml 재확인

도메인이 바뀌었으니 다시 한 번 확인하세요.

- `https://jzl.kr/robots.txt` 가 정상 응답하고 `Sitemap:` 줄이 `https://jzl.kr/sitemap.xml` 인지
- `https://jzl.kr/sitemap.xml` 이 정상 응답하고 모든 주소가 `https://jzl.kr` 로 시작하는지
- 비노출 상품·브랜드가 들어가 있지 않은지
- `/admin` 이 `Disallow` 되어 있는지

**이미 맞으면 손대지 말고 확인 결과만 보고하세요.**

---

# [4] 확인 사항

작업이 끝나면 아래를 확인하고 보고하세요.

1. 빌드 산출물의 `<head>` 에 google-site-verification 태그가 있는가
2. 빌드 산출물의 `<head>` 에 naver-site-verification 태그가 있는가
3. **여러 페이지에서** 두 태그가 모두 나오는가 (openGraph 정의 페이지 포함)
4. GA4 스크립트가 들어갔는가, 중복 삽입은 없는가
5. 개발 환경에서는 GA4가 집계되지 않는가
6. `robots.txt` · `sitemap.xml` 이 새 도메인으로 정상인가
7. `npm run build` 통과 (Type error·Failed to compile 0건)
8. `npm run check:schema` 통과

---

# [5] 마무리

- 커밋하고 푸시하세요
- `vercel.json`의 cron이 `0 19 * * *`인지 커밋 전에 다시 확인하세요
- 마지막에 한국어로 알려주세요:
  1) 실행해야 할 SQL이 있으면 파일명 (없으면 없다고)
  2) 소유확인 태그를 **어떤 방식으로** 넣었는지, 빌드 산출물에서 확인한 결과
  3) GA4가 이미 있었는지 새로 넣었는지
  4) 명세대로 하지 않은 것이 있으면 그 이유
  5) 배포 후 운영자가 해야 할 일 (인증 버튼 누르기 등)
