# JZL CLOSET 3단계-H 작업 명세

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- 고객 화면 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #55524E / wine #6A2E3C
- 고객 화면 폰트: Pretendard(본문) · Gowun Batang(국문 제목) · Cormorant Garamond(영문 디스플레이)
- **그림자 금지, 이모지 금지** (아이콘은 SVG로 직접)
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role
- 새 SQL은 `supabase/schema-3h.sql`로 만들고 실행 순서 보고
- **`vercel.json`의 cron은 `0 19 * * *`(하루 1회)를 반드시 유지하세요.**
  Hobby 플랜은 하루 1회만 허용하며, `*/10 * * * *`로 되돌리면 **배포 자체가 거부됩니다.**
- 체험단 후기의 `※ 제품을 무상으로 제공받아 작성된 후기입니다` 문구는 표시광고법상 필수입니다. 제거 금지.
- 작업 후 `npm run check:schema`를 실행해 확인해 주세요.

## 사업 구조 (설계 판단의 전제)

JZL CLOSET은 **자체 제작 브랜드가 아니라 병행수입 편집숍**입니다.
GANNI, COMME des GARÇONS, MAISON KITSUNÉ, ARC'TERYX 등 14개 브랜드를 직접 골라 들여옵니다.
따라서 페이지 구조는 아래와 같이 구분됩니다.

```
/about              편집숍 JZL CLOSET 소개        ← 쇼핑몰 자체 소개
/brands             취급 브랜드 목록
/brand/{slug}       개별 브랜드 소개              ← GANNI 등
/brand/jzl-closet   자체 기획 라인 (상품 생기면)
```

**`/about`과 `/brand/jzl-closet`을 통합하지 마세요.** 성격이 다른 페이지입니다.

---

# A. 문구·링크 정리

## [A-1] 푸터 INFORMATION 정리

현재 `브랜드 소개`가 `/brand/jzl-closet`으로 연결되어 있습니다.
손님은 "JZL이 어떤 곳인가"를 기대하는데 개별 브랜드 페이지가 나와 어긋납니다.

```
현재: 브랜드 소개 → /brand/jzl-closet
수정: 편집숍 소개 → /about
```

`브랜드 목록 → /brands`는 그대로 둡니다.

## [A-2] HOW TO ORDER 문구 수정

03단계에 사실과 다른 내용이 있습니다.

```
현재: 확인 후 발송하고, 송장번호를 문자로 알려드립니다.
수정: 확인 후 발송하고, 송장번호를 알려드립니다.
```

**문자(SMS) 발송 기능이 없습니다.** 사이트 전체에서 "문자로 알려드립니다" 류의 문구를 찾아 같은 방향으로 수정하세요.

## [A-3] 푸터 CATEGORY 중복

`전체`(`/category/all`)와 `전체 상품`(`/products`)이 둘 다 있습니다.
같은 곳으로 가는지 확인하고 하나로 정리하세요.

## [A-4] `/brand/jzl-closet` 처리

자체 기획 상품이 아직 없는데 `/brands` 목록에 상품 0개로 노출됩니다.
브랜드 스토리도 "자체 기획으로 만드는 라인입니다"라 실제와 맞지 않습니다.

- **관리자에서 비노출 처리하면 `/brands` 목록과 필터에서 빠지는지** 확인
- 비노출 상태에서 `/brand/jzl-closet` 직접 접근 시 404가 되는지 확인 (3-G에서 처리했을 것)
- 코드로 삭제하지는 마세요. 자체 상품이 생기면 다시 켤 예정입니다

---

# B. 개인정보처리방침 문구 수정

## 현재 문제

`lib/default-copy.ts`의 기본 문구에 **사실과 다른 문장**이 남아 있습니다.

```
"전화 또는 문자로 주문 내역을 전달하는 과정에서 수집"
"회원가입 기능을 제공하지 않으며"
```

지금은 회원가입·소셜로그인·포인트·리뷰·추천 기능이 모두 있고, 전화 주문은 받지 않습니다.
**개인정보처리방침은 법적 문서라 실제와 어긋나면 문제가 됩니다.**

## 조치

현재 서비스 구조에 맞게 기본 문구를 새로 작성하세요. 아래 내용이 반영되어야 합니다.

**수집하는 개인정보**
- 회원가입: 이메일, 이름, 비밀번호(이메일 가입 시)
- 소셜 로그인: 구글·카카오에서 제공하는 이메일, 닉네임
- 주문: 주문자·수령인 이름, 연락처, 배송지 주소, 배송 요청사항
- 선택 입력: 생년월일(생일 포인트), 연락처
- 자동 수집: 접속 기록, 쿠키(장바구니·추천 코드 추적)

**이용 목적**
- 회원 관리, 주문 처리 및 배송, 포인트 적립·사용, 리뷰 관리, 문의 응대, 추천인 실적 확인

**제3자 제공**
- 배송을 위해 공급 파트너에게 수령인 정보(이름·연락처·주소) 제공
- 위탁배송 구조임을 명시

**보유 기간**
- 회원 탈퇴 시까지, 단 전자상거래법에 따라 계약·결제 기록 5년, 소비자 불만·분쟁 처리 기록 3년

**이용자 권리**
- 열람·정정·삭제·처리정지 요구 가능, 마이페이지 또는 1:1 문의로 신청

**개인정보 보호책임자**
- 관리자 설정의 사업자 정보를 사용 (하드코딩 금지)

## 주의

- 관리자에서 수정 가능한 구조를 유지하세요 (디자인 관리 > 문구)
- **이용약관에도 같은 문제가 있는지 확인**하고 함께 점검해 주세요
- 법률 자문을 대체할 수 없다는 점을 보고에 명시해 주세요.
  운영자가 최종 검토 후 사용해야 합니다

---

# C. 이탈 방지 기능

## [C-1] 최근 본 상품

### 배경
손님이 여러 상품을 보다가 "아까 그거"를 다시 찾지 못해 이탈합니다.

### 요구사항
- 상품 상세를 열면 **브라우저에 기록** (localStorage)
- **최대 20개**, 오래된 것부터 밀어냄
- 같은 상품을 다시 보면 맨 앞으로
- **DB에 저장하지 마세요.** 비회원도 동작해야 하고 조회 부담을 늘리지 않습니다

### 표시 위치
1. **상품 상세 하단** — "함께 보면 좋은 상품" 아래
2. **메인 하단** — 기록이 있을 때만
3. **장바구니가 비어 있을 때** — 담긴 게 없으면 최근 본 상품을 보여줘 다시 둘러보게

### 디자인
- 가로 스크롤, 작은 썸네일 + 상품명 + 가격
- 라벨: `RECENTLY VIEWED` / 제목: `최근 본 상품`
- 기록이 없으면 영역 자체를 숨김
- 개별 삭제 · 전체 지우기 버튼

---

## [C-2] 함께 보면 좋은 상품

### 배경
상품 하나만 보고 나가는 것을 막습니다.

### 표시 위치
상품 상세 맨 아래 (판매정보 탭 아래, BRAND 섹션 근처)

### 추천 순서
1. **같은 브랜드 + 같은 소분류** (가장 유사)
2. 같은 브랜드
3. 같은 소분류
4. 부족하면 같은 대분류

- 현재 보고 있는 상품은 제외
- 품절·비노출 상품 제외
- **최대 8개**, 무작위가 아니라 위 우선순위대로
- 추천할 게 하나도 없으면 영역 숨김

### 성능
- **DB 조회를 1회로 끝내세요.** 우선순위별로 여러 번 조회하지 말고 한 번에 가져와 정렬
- 상품 상세는 정적 생성(ISR)이므로 그 안에서 함께 생성되게

### 디자인
- 라벨: `YOU MAY ALSO LIKE` / 제목: `함께 보면 좋은 상품`
- 기존 상품 카드 컴포넌트 재사용
- PC 4개, 모바일 2개씩 배치 또는 가로 스크롤

---

## [C-3] 메인에 브랜드 목록 추가

### 배경
편집숍의 핵심은 "어떤 브랜드를 다루는가"입니다.
현재 메인에는 브랜드가 전혀 드러나지 않습니다.

### 위치
**HOW TO ORDER 섹션 자리**에 넣고, HOW TO ORDER는 그 아래로 내립니다.

주문 방법은 손님이 거의 읽지 않는 반면, 취급 브랜드는 첫 방문자가 가장 궁금해하는 정보입니다.

### 구성
```
BRANDS
취급 브랜드                              [ 전체 보기 → ]

GANNI    COMME des GARÇONS    MAISON KITSUNÉ    ARC'TERYX
alo      AMI PARIS            STÜSSY            Vivienne Westwood
POLO RALPH LAUREN    PALACE   Calvin Klein      patagonia    LACOSTE
```

### 요구사항
- **로고가 아니라 브랜드명 텍스트** (Cormorant Garamond, 넓은 자간)
  브랜드 로고는 각 브랜드사의 등록상표라 사용에 제약이 있습니다
- 관리자에 로고를 등록한 브랜드는 로고가 우선 표시 (3-E 방식과 동일)
- 노출 설정된 브랜드만, 관리자 지정 순서대로
- **상품이 0개인 브랜드는 제외** (빈 페이지로 보내지 않기)
- 클릭 시 `/brand/{slug}`로 이동
- `전체 보기` → `/brands`
- 모바일에서는 2~3열로 자연스럽게

### 디자인
- 여백을 넉넉히 준 절제된 나열 (하이엔드 편집숍 느낌)
- 호버 시 밑줄 또는 색 변화

---

# 작업 완료 후 보고할 것

1. 새 SQL 파일과 실행 순서 (없으면 없다고)
2. "함께 보면 좋은 상품"이 DB를 몇 번 조회하는지
3. 최근 본 상품이 비회원·회원 모두에서 동작하는지
4. 개인정보처리방침·이용약관에서 수정한 문장 목록
5. `npm run check:schema` 결과
6. 빌드 통과 여부와 커밋 해시
7. **`vercel.json`의 cron이 `0 19 * * *`로 유지되고 있는지 확인**

---

# 이번 작업에서 하지 않을 것

- 사이즈 가이드 (브랜드별 실측 데이터 확보 후 별도 작업)
- 품절 알림 신청
- 정품 보증 배지
- PG 결제 연동
- 네이버 로그인
- SMS 발송
