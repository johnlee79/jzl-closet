# JZL CLOSET 3단계-F 작업 명세

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- 고객 화면 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #55524E / wine #6A2E3C
- 고객 화면 폰트: Pretendard(본문) · Gowun Batang(국문 제목) · Cormorant Garamond(영문 디스플레이)
- 관리자 화면은 Pretendard 단일 폰트, 다크모드 지원 (3-C 구현분 유지)
- **그림자 금지, 이모지 금지**
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role
- 새 SQL은 `supabase/schema-3f.sql` · `supabase/rls-3f.sql`로 만들고 실행 순서 보고
- 새 DB 함수를 만들면 rls-3b.sql의 pg_proc 조회 방식으로 **service_role 전용 잠금**
- **`vercel.json`의 cron은 `0 19 * * *`(하루 1회)를 반드시 유지하세요.**
  Hobby 플랜은 하루 1회만 허용하며, `*/10 * * * *`로 되돌리면 **배포 자체가 거부됩니다.**
- 체험단 후기의 `※ 제품을 무상으로 제공받아 작성된 후기입니다` 문구는 표시광고법상 필수입니다. 제거 금지.

---

# A. 화면 수정 (먼저 처리)

## [A-1] 브랜드·카테고리 필터 스타일 통일

### 현재 문제
3-E에서 브랜드는 세리프체 + 밑줄로 바뀌었으나, 카테고리는 네모 버튼 그대로입니다.
두 줄의 스타일이 달라 정돈되어 보이지 않고, `CATEGORY` 라벨이 첫 항목과 겹쳐 보입니다.

### 조치
1. **카테고리를 브랜드와 동일한 스타일로 통일**
   - 네모 버튼(테두리·검정 배경) 제거
   - Cormorant Garamond + 넓은 자간
   - 선택 안 됨: muted / 선택됨: ink + 아래 밑줄
2. `BRAND` · `CATEGORY` 라벨과 첫 항목 사이 **간격을 넉넉히** (현재 겹쳐 보임)
3. 두 줄 사이 **세로 간격**도 조금 더
4. 카테고리의 `전체`를 브랜드와 같이 **`ALL`** 로 통일
5. 모바일 가로 스크롤 동작은 3-E 구현 그대로 유지

---

## [A-2] 상품 상세 구매 영역 높이 조정

### 현재 문제
오른쪽 구매 영역이 왼쪽 이미지보다 길어서 아래에 큰 빈 공간이 생깁니다.

### 조치 (아래 세 가지만. 다른 요소는 건드리지 마세요)
1. **한 줄 소개와 적립 포인트 배지를 같은 줄에 배치**
   ```
   남여 공용 상의 셔츠              구매 시 5,000P 적립
   ```
   - 왼쪽에 소개, 오른쪽에 적립 배지
   - 모바일에서 폭이 좁으면 자동으로 두 줄이 되게
2. **상품 정보 표에서 `문의` 행 삭제** — 아래 카카오톡 문의 버튼과 중복입니다
3. **왼쪽 대표 이미지를 조금 키워** 양쪽 높이 차이를 줄이기
   - 비율은 유지
   - 오른쪽이 여전히 더 길면 자연스럽게 흐르게 (억지로 맞추지 마세요)

---

## [A-3] 관리자 상품 관리 화면 개선

### 현재 문제
- 화면 진입 시 분류 필터가 `의류`로 걸린 채 열려서 다른 분류 상품이 안 보입니다
- 상단에 `전체 1개`라고 나오는데 목록은 비어 있습니다 (필터 조건과 조회가 어긋남)

### 조치
1. **진입 시 필터가 걸리지 않은 전체 상태로 시작**
   - 분류 · 노출 · 품절 셀렉트의 기본값을 모두 `전체`로
2. **건수와 목록이 어긋나는 문제 확인 후 수정**
3. **건수 표시를 명확하게**
   - 필터 없음: `전체 N개`
   - 필터 있음: `조건에 맞는 상품 N개 · 전체 M개`
4. **결과 없을 때** `조건에 맞는 상품이 없습니다` 옆에 **`조건 지우기`** 버튼 함께 노출
5. **검색이 상품명·브랜드명 모두에서 동작하는지 확인**
   - `가니` 입력 → GANNI 브랜드 상품이 나와야 함
   - 영문·한글 모두 매칭되게 (브랜드의 라벨 · 정식표기 · 한글명 전부 대상)

---

# B. 추천 코드 기능 (핵심)

## 배경

회원이 상품을 공유해 친구를 데려오면 보상을 주는 기능입니다.
3-E에서 만든 공유 버튼에 추천 코드를 붙여 확장합니다.

**설계 원칙 — 반드시 지킬 것**

- **방문·가입 자체로는 포인트를 지급하지 않습니다.** 숫자만 집계해 보여줍니다.
- **보상은 "목표 달성" 시에만** 지급합니다. (예: 친구 5명이 구매하면 캠핑테이블)
- 이유: 방문·가입만으로 지급하면 어뷰징에 취약하고 매출로 이어지지 않습니다.

---

## [B-1] 추천 코드 발급

- 회원가입 시 **고유 코드 자동 발급** (예: `A3F9K2`)
  - 6자리, 영문 대문자 + 숫자
  - **혼동되는 문자 제외**: `0` `O` `1` `I` `L` — 손으로 옮겨 적을 때 헷갈립니다
  - 중복 불가
- 기존 회원에게도 일괄 발급 (SQL에서 처리)
- 추천 링크 형식
  ```
  https://jzl-closet.vercel.app/products/{slug}?ref=A3F9K2
  https://jzl-closet.vercel.app/?ref=A3F9K2
  ```

## [B-2] 공유 버튼 확장

3-E에서 만든 공유 버튼을 확장합니다. (URL 생성 함수를 분리해 두었을 것)

- **로그인 회원**: 공유 URL에 `?ref={본인코드}` 자동 부착
- **비회원**: 기존대로 코드 없이 공유
- 공유 문구에도 안내를 넣어 주세요
  ```
  {상품명}
  JZL CLOSET에서 확인해 보세요
  {URL}
  ```

## [B-3] 유입 추적

### 링크 접속 시
- `?ref=코드`로 접속하면 **쿠키에 저장** (유효기간 30일)
- 접속 기록 저장: 추천인 · 접속 시각 · 유입 상품
- **같은 방문자의 중복 접속은 1회로 집계** (쿠키·IP 기준)
- **본인 코드로 본인이 접속한 경우 집계 제외**

### 회원가입 시
- 쿠키에 추천 코드가 있으면 **추천 관계를 기록**
- 가입 화면에 **추천 코드 직접 입력란**도 제공
  ```
  추천 코드가 있으신가요? (선택)
  [ A3F9K2 ]
  ```
  - 링크로 들어온 경우 자동으로 채워지고 수정 가능
  - 오프라인·구두로 코드를 받은 경우를 위한 통로입니다
- **자기 자신을 추천인으로 지정할 수 없게** 차단

### 첫 구매 시
- 추천으로 가입한 회원이 **첫 주문을 완료(배송완료 또는 구매확정)** 하면 기록
- **주문 취소·반품 시 해당 실적에서 제외** (되돌리기)

---

## [B-4] 목표 이벤트 (관리자 설정)

### 요구사항
관리자가 목표와 보상을 직접 만듭니다. **여러 개를 동시에 운영**할 수 있어야 합니다.

```
목표 1   친구 3명 가입      →  포인트 5,000P
목표 2   친구 5명 첫 구매    →  사은품: 캠핑 테이블
목표 3   친구 10명 첫 구매   →  사은품: 상품 증정
```

### 목표 항목
- 목표 이름
- **달성 기준: `가입` 또는 `첫 구매` 중 선택** ← 둘 다 지원해야 합니다
- 목표 인원 수
- **보상 종류: `포인트` 또는 `사은품`**
  - 포인트: 금액 입력
  - 사은품: 아래 [B-5] 참조
- 이벤트 기간 (시작일·종료일, 날짜만. 3-C 팝업과 같은 방식)
- 반복 여부 — 달성할 때마다 계속 지급 / 1회만
- on·off

### 지급 처리
- **포인트 보상**: 달성 즉시 자동 지급 (`apply_point_change` 사용, 앱에서 잔액 직접 수정 금지)
- **사은품 보상**: 자동 지급하지 않고 **관리자 확인 후 발송 처리** ([B-6] 참조)
- 지급 시 텔레그램 알림

---

## [B-5] 사은품 등록

관리자가 사은품을 등록해두고 목표에 연결합니다.

### 등록 항목
- **사은품 이미지** (R2 저장)
- 사은품 이름 (예: `캠핑 테이블`)
- 설명 문구
- **링크** (선택) — 상품 페이지나 외부 소개 페이지로 연결

### 표시
마이페이지 친구 초대 화면에 **이미지와 함께** 보여줍니다.
회원이 뭘 받을 수 있는지 눈으로 보게 하는 것이 목적입니다.

---

## [B-6] 마이페이지 → 친구 초대

### 화면 구성

```
내 추천 코드
A3F9K2                         [ 복사 ]

내 초대 링크
jzl-closet.vercel.app/?ref=a3f9k2    [ 복사 ]  [ 공유 ]

───────────────────────────────

현재까지
방문 12명 · 가입 3명 · 구매 1명

───────────────────────────────

진행 중인 목표

[사은품 이미지]
친구 5명이 구매하면 캠핑 테이블을 드립니다

■■■□□   3 / 5
2명 더 모으면 받을 수 있어요

───────────────────────────────

초대 내역
김O수    구매 완료      2026.08.15
박O영    가입 완료      2026.08.14
이O민    방문           2026.08.13
```

### 주의사항
- **초대 내역의 이름은 반드시 마스킹** (`김O수`) — 개인정보입니다
- 진행률 막대는 목표별로 각각 표시
- 달성한 목표는 `달성 완료` 표시와 함께 처리 상태 노출
  (포인트: `5,000P 지급 완료` / 사은품: `발송 준비 중` → `발송 완료`)
- **DB 조회를 늘리지 마세요.** 집계값은 회원 행에 미리 저장해두고 그것만 읽습니다.
  (3-B 포인트 잔액과 같은 방식)

---

## [B-7] 어뷰징 방지 (필수)

이 기능은 악용 여지가 큽니다. 아래를 반드시 구현하세요.

1. **본인 추천 차단** — 자기 코드로 자기가 가입 불가
2. **같은 기기·IP 감지** — 동일 기기나 IP에서 온 가입은 **실적에 자동 반영하지 않고 보류**,
   관리자가 확인 후 인정/거절
3. **중복 집계 방지** — 같은 방문자의 반복 접속은 1회로만
4. **취소·반품 시 실적 회수** — 구매 실적에서 제외하고 진행률 재계산
5. **월 지급 한도** — 관리자 설정. 한도 초과 시 지급 보류 + 관리자 알림
6. **의심 건 표시** — 관리자 화면에서 확인 가능하게
7. 탈퇴한 회원의 실적 처리 — 추천 관계는 유지하되 이름은 마스킹 유지

---

## [B-8] 관리자 → 추천 관리

새 메뉴를 `고객 관리` 그룹에 추가합니다.

### 화면
1. **추천 현황** — 추천인별 방문·가입·구매 수, 정렬·검색
2. **목표 달성자 목록** — 달성 일시, 보상 종류, 처리 상태
3. **사은품 발송 관리**
   - 발송 대상자 목록
   - 받는 분 정보 — **마지막 배송지를 자동으로 가져오되 수정 가능**
   - 발송 완료 처리, 송장번호 입력
   - 텔레그램 알림
4. **의심 건 검토** — 같은 기기·IP로 판단된 건을 인정/거절
5. **통계** — 기간별 추천 유입 수, 전환율(방문→가입→구매)

---

# C. DB 변경

`supabase/schema-3f.sql`에 포함할 내용:

- 회원의 추천 코드 컬럼 (유일 인덱스)
- 추천 관계 (누가 누구를 데려왔는지)
- 방문 기록 테이블 (추천인 · 방문자 식별값 · 시각 · 유입 상품)
- 목표 이벤트 테이블
- 사은품 테이블
- 달성·지급 기록 테이블
- 회원 행에 집계값 컬럼 (방문·가입·구매 수) — 조회 성능용
- 기존 회원 전원에게 추천 코드 일괄 발급

`supabase/rls-3f.sql`:
- 새 테이블 RLS 활성화
- **본인 추천 실적만 조회 가능**하도록 정책
- 새 함수는 service_role 전용 잠금

여러 번 실행해도 안전해야 하고, 기존 데이터에 영향이 없어야 합니다.

---

# 작업 완료 후 보고할 것

1. 새 SQL 파일과 실행 순서
2. A-3의 "건수와 목록이 어긋나던" 실제 원인
3. 어뷰징 방지를 어떤 방식으로 구현했는지 (특히 기기·IP 판별)
4. 마이페이지 친구 초대 화면이 DB를 몇 번 조회하는지
5. 관리자에서 새로 설정해야 하는 값 목록
6. 빌드 통과 여부와 커밋 해시
7. **`vercel.json`의 cron이 `0 19 * * *`로 유지되고 있는지 확인**

---

# 이번 작업에서 하지 않을 것

- 방문·가입 자체에 대한 포인트 지급 (목표 달성 시에만 지급)
- PG 결제 연동
- 카카오·네이버 로그인
- SMS 발송
- 메인 페이지 리뉴얼
