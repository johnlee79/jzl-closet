# JZL CLOSET 3단계-E 작업 명세

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- 고객 화면 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #55524E / wine #6A2E3C
- 고객 화면 폰트: Pretendard(본문) · Gowun Batang(국문 제목) · Cormorant Garamond(영문 디스플레이)
- 관리자 화면은 Pretendard 단일 폰트, 다크모드 지원 (3-C 구현분 유지)
- **그림자 금지, 이모지 금지**
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role
- 새 SQL이 필요하면 `supabase/schema-3e.sql`로 만들고 실행 순서 보고
- **`vercel.json`의 cron은 `0 19 * * *`(하루 1회)를 반드시 유지하세요.**
  Hobby 플랜은 하루 1회만 허용하며, `*/10 * * * *`로 되돌리면 **배포 자체가 거부됩니다.**
- 체험단 후기의 `※ 제품을 무상으로 제공받아 작성된 후기입니다` 문구는 표시광고법상 필수입니다. 제거 금지.

---

# [1] 상품 목록 필터 — 브랜드를 위로

## 현재
```
CATEGORY   전체  OUTER  TOPS  KNIT  SHIRTS  T-SHIRTS  DENIM  PANTS  DRESS  ACTIVEWEAR
BRAND      전체  JZL CLOSET  ATELIER JZL  NORD BLANC  MAISON OAT  STITCH LAB
```

## 변경
**브랜드를 위, 카테고리를 아래**로 순서를 바꿉니다.

```
BRAND      전체  ARC'TERYX  alo  AMI PARIS  COMME des GARÇONS  GANNI  ...
CATEGORY   전체  OUTER  TOPS  KNIT  SHIRTS  T-SHIRTS  DENIM  PANTS  DRESS  ACTIVEWEAR
```

이유: JZL CLOSET은 브랜드 편집숍이라 손님이 브랜드로 먼저 찾습니다.

---

# [2] 브랜드 표시를 고급스럽게

## 방침 — 텍스트 우선, 로고는 선택

브랜드 로고는 각 브랜드사의 등록상표라 사용에 제약이 있을 수 있습니다.
**기본은 텍스트로 표시하되, 관리자가 로고 이미지를 등록한 브랜드는 로고가 우선 표시**되게 만드세요.

- 로고 이미지 있음 → 로고 표시
- 로고 이미지 없음 → 브랜드명 텍스트 표시
- 한 줄에 로고와 텍스트가 섞여도 어색하지 않도록 **높이와 여백을 통일**하세요

## 텍스트 표시 디자인

하이엔드 편집숍(SSENSE, MATCHESFASHION 등)의 절제된 인상을 목표로 합니다.

- 폰트: **Cormorant Garamond** (영문 디스플레이용, 이미 프로젝트에 있음)
- 대문자 표기 유지 (`GANNI` · `MAISON KITSUNÉ` · `COMME des GARÇONS`)
- **자간을 넓게** (tracking widest 계열)
- 선택되지 않은 상태: muted 컬러, 테두리 없음 또는 아주 얇게
- 선택된 상태: ink 컬러 + 아래 밑줄 (배경 채우기보다 밑줄이 더 정제되어 보입니다)
- 현재처럼 네모 버튼으로 꽉 채우지 말고, **여백을 살린 나열**로 바꾸세요
- 브랜드가 많아지면 가로 스크롤 또는 2줄 배치 (모바일에서 특히 주의)

## 로고 표시 규격

- 관리자 브랜드 관리에 이미 있는 대표 이미지 필드를 사용
- 가로 기준 일정 높이로 맞추고, 세로로 늘어나지 않게
- 배경 투명 PNG 기준으로 처리하되, 흰 배경 이미지도 어색하지 않게
- 다크 배경에서도 보이도록 처리 (관리자 다크모드와는 무관, 고객 화면 기준)

## 브랜드 페이지에도 동일 적용

`/brand/{slug}` 상단에도 같은 규칙으로 브랜드명 또는 로고를 표시하세요.

---

# [3] 문의 버튼을 카카오 오픈채팅으로 교체

## 현재
상품 상세 구매 영역에 `바로 문의 032-209-1058` 버튼이 있습니다.

## 변경
이 버튼을 **카카오톡 오픈채팅 버튼으로 교체**합니다. (추가가 아니라 교체)

```
[      장바구니 담기      ]   ← 검정 채움 (기존)
[      비회원 구매        ]   ← 아웃라인 (기존, 로그인 시 "바로 구매")
[  카카오톡으로 문의하기   ]   ← 신규
```

## 버튼 사양

- 링크: **관리자 → 설정의 "카카오톡 채널 링크" 값을 사용** (하드코딩 금지)
  - 현재 등록된 값: `https://open.kakao.com/o/sVcZwWlt`
- 새 탭으로 열기 (`target="_blank"` + `rel="noopener noreferrer"`)
- **설정값이 비어 있으면 버튼 자체를 숨김** (준비중 표시 금지)
- 디자인: 카카오 상징색 배경 `#FEE500` + 검정 글씨(`#191919`)
- 말풍선 아이콘은 **SVG로 직접 그려서** 사용 (이모지 금지, 외부 이미지 링크 금지)
- 그림자 없음 (프로젝트 규칙)

## 전화번호 처리

- 상품 상세에서 전화번호 버튼은 **제거**합니다
- 전화번호는 **판매정보 탭**의 판매자 정보에 표시 (3-B에서 만든 탭)
- 푸터에도 기존대로 유지

---

# [4] 연락처 번호 통일

현재 두 개의 번호가 섞여 있습니다.

- 관리자 설정: `010-3602-7122`
- 상품 페이지 버튼: `032-209-1058`

## 조치

**`010-3602-7122` 로 통일합니다.**

- 코드에 `032-209-1058`이 하드코딩된 곳을 **전부 찾아서** 관리자 설정값을 읽도록 바꾸세요
- 상품 상세 · 404 페이지 · 푸터 · 주문 완료 화면 · 이메일 문구 등 전 영역 점검
- 앞으로 번호가 바뀌면 관리자 설정 한 곳만 고치면 되도록
- 어디어디를 고쳤는지 목록으로 보고해 주세요

---

# [5] 함께 점검할 것

## 5-1. 가짜 브랜드 데이터

현재 브랜드에 AI가 생성한 임시 데이터가 남아 있습니다.

```
ATELIER JZL · NORD BLANC · MAISON OAT · STITCH LAB
```

이 브랜드들은 **운영자가 관리자 화면에서 직접 삭제**할 예정입니다.
코드에서 지우지 마세요. 다만 아래를 확인해 주세요.

- 브랜드 삭제 시 그 브랜드에 속한 상품이 어떻게 되는지 (에러 없이 처리되는지)
- `lib/brands.ts` 등에 **폴백(fallback) 값으로 가짜 브랜드명이 하드코딩**되어 있다면 제거하세요
  (README에 "테이블이 비어 있으면 lib/brands.ts의 폴백 값을 쓴다"는 언급이 있습니다)
- 같은 방식으로 `lib/categories.ts`도 확인

## 5-2. 브랜드가 늘어나는 상황 대비

운영자가 셀스타에서 상품을 가져오며 브랜드를 계속 추가합니다. (14개 이상 예상)

- 필터 영역이 브랜드 수에 따라 깨지지 않는지
- 모바일에서 가로 스크롤이 자연스러운지
- 브랜드 정렬 순서를 관리자가 지정할 수 있는지 (없다면 알파벳순 또는 등록순으로 일관되게)

---

# 작업 완료 후 보고할 것

1. `032-209-1058`이 하드코딩되어 있던 위치 전부와 수정 내역
2. 브랜드 필터가 로고/텍스트 혼재 상황에서 어떻게 보이는지
3. 브랜드가 20개일 때 필터 영역이 어떻게 되는지 (PC·모바일)
4. `lib/brands.ts` · `lib/categories.ts` 폴백 값 처리 결과
5. 빌드 통과 여부와 커밋 해시
6. **`vercel.json`의 cron이 `0 19 * * *`로 유지되고 있는지 확인**

---

# 이번 작업에서 하지 않을 것

- 브랜드 로고 이미지 등록 (운영자가 관리자에서 직접)
- 브랜드·카테고리 데이터 입력 (운영자가 직접)
- PG 결제 연동
- 카카오·네이버 로그인
- 메인 페이지 리뉴얼 (별도 작업으로)
