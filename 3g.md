# JZL CLOSET 3단계-G 작업 명세

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- 고객 화면 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #55524E / wine #6A2E3C
- 고객 화면 폰트: Pretendard(본문) · Gowun Batang(국문 제목) · Cormorant Garamond(영문 디스플레이)
- **그림자 금지, 이모지 금지** (아이콘은 SVG로 직접 그릴 것)
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role
- 새 SQL은 `supabase/schema-3g.sql`로 만들고 실행 순서 보고
- **`vercel.json`의 cron은 `0 19 * * *`(하루 1회)를 반드시 유지하세요.**
  Hobby 플랜은 하루 1회만 허용하며, `*/10 * * * *`로 되돌리면 **배포 자체가 거부됩니다.**
- 체험단 후기의 `※ 제품을 무상으로 제공받아 작성된 후기입니다` 문구는 표시광고법상 필수입니다. 제거 금지.
- 작업 후 `npm run check:schema`를 실행해 컬럼 불일치가 없는지 확인해 주세요.

---

# [1] 카카오 로그인 추가

## 배경
Supabase에 카카오 Provider 설정이 완료되었습니다. 이제 화면에 버튼만 붙이면 됩니다.

- 카카오 개발자 앱 등록·비즈 앱 전환 완료
- Redirect URI 등록 완료
- 동의항목: 닉네임(필수) · 카카오계정 이메일(필수)
- Supabase Kakao Provider: Enabled, REST API Key·Client Secret 입력 완료

## 조치

### 1-1. 로그인·회원가입 화면에 버튼 추가

현재 구글 버튼만 있습니다. 그 아래에 카카오 버튼을 추가하세요.

```
[  Google로 계속하기  ]   ← 기존
[  카카오로 계속하기   ]   ← 신규
─────── 또는 ───────
이메일 로그인 폼
```

- Supabase의 카카오 OAuth 호출 (`provider: 'kakao'`)
- 디자인: 배경 `#FEE500`, 글씨 `#191919`
- 카카오 심볼은 **SVG로 직접 그려서** 사용 (이모지·외부 이미지 링크 금지)
- 구글 버튼과 높이·모서리·간격을 동일하게

### 1-2. 기존 소셜 로그인 흐름과 동일하게

3-B에서 만든 간편가입 처리(provider 분기, 비밀번호 항목 숨김, 비밀번호 찾기 안내)가
카카오에도 그대로 적용되는지 확인하세요. `provider` 값만 다르고 로직은 같아야 합니다.

- 마이페이지 회원정보 수정 → "카카오 계정으로 로그인 중입니다" 안내
- 비밀번호 찾기 → "이 계정은 카카오 계정으로 가입하셨습니다" 안내
- 회원 탈퇴 → 비밀번호 대신 `탈퇴합니다` 입력 방식

### 1-3. 로딩 표시

3-C에서 만든 로그인 진행 표시(중복 클릭 차단, 3초 안내, 15초 재시도)를 카카오에도 적용하세요.

---

# [2] 모바일 상단바 정리

## 현재 문제
모바일 상단바에 로그인 버튼이 있고, 햄버거 메뉴 안에도 로그인·회원가입 버튼이 있어 중복입니다.

## 조치

### 비로그인 상태
상단바에서 **로그인 버튼을 제거**하고, 로고를 중앙에 배치합니다.

```
[☰]        JZL CLOSET        [장바구니 1]
```

### 로그인 상태
마이페이지 버튼은 **상단바에 그대로 유지**합니다.

```
[☰]     JZL CLOSET     [마이페이지] [장바구니 1]
```

- 햄버거 메뉴 안의 로그인·회원가입 버튼은 **그대로 유지**
- 로고 중앙 정렬 시 좌우 요소 폭이 달라도 로고가 정확히 가운데 오도록
- PC 화면은 건드리지 마세요

---

# [3] 전화 문의 버튼을 카카오 오픈채팅으로 전면 교체

## 배경
3-E에서 상품 상세만 교체했는데, 장바구니·주문서 등 다른 화면에 전화 버튼이 남아 있습니다.

## 조치

### 3-1. 전수 교체
사이트 전체에서 **전화 걸기를 유도하는 버튼**을 찾아 카카오 오픈채팅 버튼으로 교체하세요.

확인된 위치:
- 주문서(체크아웃) — `전화로 문의 010-3602-7122`
- 장바구니
- 그 외 발견되는 모든 곳

### 3-2. 버튼 사양
- 문구: **`카카오톡 실시간 문의`**
- 링크: **관리자 설정의 카카오톡 채널 링크** 사용 (하드코딩 금지)
- 새 탭으로 열기 (`target="_blank"` + `rel="noopener noreferrer"`)
- 설정값이 비어 있으면 **버튼 자체를 숨김**
- 디자인: 배경 `#FEE500`, 글씨 `#191919`, 말풍선 SVG 아이콘

### 3-3. 안내 문구도 함께 수정
```
현재: 온라인 주문이 어려우시면 위 내용을 복사해 보내주시거나 전화 주세요.
변경: 온라인 주문이 어려우시면 위 내용을 복사해 카카오톡으로 보내주세요.
```
전화를 안내하는 다른 문구도 찾아서 같은 방향으로 수정하세요.

### 3-4. 남겨둘 것 (제거 금지)
- **푸터의 고객센터 전화번호·이메일** — 사업자 정보 표시 의무. 텍스트로 그대로 유지
- **판매정보 탭의 판매자 연락처** — 그대로 유지

즉 "전화 걸기 버튼"만 없애고, "정보 표시"는 남깁니다.

---

# [4] 푸터 SNS 아이콘

## 배경
3-E 명세에 있었으나 구현되지 않았습니다. 이번에 만듭니다.

## 항목
- 인스타그램
- 스레드(Threads)
- 틱톡
- **위챗** — 링크가 아니라 **QR 이미지**

## 동작
- 인스타·스레드·틱톡: 새 탭으로 열기
- **위챗: 클릭 시 QR 이미지를 모달로 표시** (외부 이동 없음)
  ```
  [QR 이미지]
  위챗에서 스캔해 주세요
  ```
- 아이콘은 **SVG로 직접 그려서** 사용 (이모지·외부 이미지 링크 금지)
- **모노톤(ink 또는 muted)으로 통일** — 각 SNS 브랜드컬러 사용 금지, 브랜드 톤 유지
- 푸터 하단, 사업자 정보 위쪽에 가로로 나열

## 관리자 설정
관리자 → 설정에 **[SNS]** 탭을 신설합니다.

- 인스타그램 / 스레드 / 틱톡 — 각각 URL 입력란
- 위챗 — QR 이미지 업로드 (R2 저장)
- **값이 비어 있는 항목은 화면에서 자동으로 숨김**
- 나중에 유튜브·페이스북 등을 추가할 수 있는 구조로

## 브랜드 페이지에도
`/brand/{slug}` 하단에도 같은 컴포넌트를 재사용할 수 있게 만들어 주세요.

---

# [5] 브랜드 목록 페이지 `/brands` 신설

## 배경
푸터에 `브랜드 목록` 항목이 있으나 제대로 된 페이지가 없습니다.
관리자에 입력한 브랜드 스토리가 검색 유입 경로가 되도록 목록 페이지를 만듭니다.

## 구성

```
BRANDS
JZL CLOSET이 다루는 브랜드입니다

──────────────────────────────────
[대표 이미지]
GANNI · 가니
코펜하겐에서 온 자유로운 감각
덴마크 · 2000                [ 상품 보기 → ]
──────────────────────────────────
[대표 이미지]
ARC'TERYX · 아크테릭스
산에서 검증된 기능성
캐나다 · 1989                [ 상품 보기 → ]
──────────────────────────────────
```

## 요구사항
- 노출 설정된 브랜드만 표시, 관리자가 지정한 정렬 순서 따르기
- 대표 이미지는 **원본 비율 유지** (085e550에서 수정한 방식과 동일하게)
- 대표 이미지가 없으면 브랜드명을 Cormorant Garamond로 크게 표시
- 각 항목 클릭 시 `/brand/{slug}` 상세로 이동
- `강조` 설정된 브랜드는 위쪽에 더 크게 노출
- 메타 태그·JSON-LD 추가 (SEO)
- 모바일에서 한 줄에 하나씩

## 푸터 연결 정리
- **`브랜드 소개`** → `/brand/jzl-closet` (JZL CLOSET 자체 브랜드 페이지)
- **`브랜드 목록`** → `/brands` (신설)

두 항목이 각각 어디로 연결되어 있는지 확인하고 위와 같이 정리하세요.

---

# [6] 공유 버튼 안내 문구

## 현재
상품 상세에 `친구에게 공유하기`만 있어 공유할 이유가 드러나지 않습니다.

## 조치
버튼 아래에 안내 문구를 추가하되, **상황에 따라 다르게** 표시합니다.

### 진행 중인 목표 이벤트가 있을 때 (로그인 회원)
```
⌁ 친구에게 공유하기
   이벤트 중! 친구를 초대하고 포인트와 사은품을 받아가세요
```

### 이벤트가 없을 때 (로그인 회원)
```
⌁ 친구에게 공유하기
```
문구 없이 버튼만 표시합니다.

### 비회원일 때
```
⌁ 친구에게 공유하기
   로그인하면 친구 초대 혜택을 받으실 수 있어요
```
비회원은 추천 코드가 없어 실적이 쌓이지 않으므로 로그인을 유도합니다.

## 관리자 설정
- 이 안내 문구를 관리자에서 수정할 수 있게 하세요 (3-F 추천 관리 설정에 추가)
- 이벤트 진행 여부는 3-F에서 만든 목표 이벤트의 활성 상태로 판단
- **DB 조회를 추가하지 마세요.** 이미 불러오는 값으로 판단하거나 캐싱을 사용하세요

---

# 작업 완료 후 보고할 것

1. 새 SQL 파일과 실행 순서
2. 전화 문의 버튼이 하드코딩되어 있던 위치 전부와 수정 내역
3. 푸터의 `브랜드 소개` · `브랜드 목록`이 원래 어디로 연결되어 있었는지
4. 카카오 로그인이 3-B의 간편가입 분기 로직과 제대로 연동되는지
5. 관리자에서 새로 설정해야 하는 값 목록
6. `npm run check:schema` 결과
7. 빌드 통과 여부와 커밋 해시
8. **`vercel.json`의 cron이 `0 19 * * *`로 유지되고 있는지 확인**

---

# 이번 작업에서 하지 않을 것

- 네이버 로그인 (Supabase 미지원, 직접 구현 필요 — 당분간 보류)
- PG 결제 연동
- SMS 발송
- 메인 페이지 리뉴얼
- 고객 화면 다크모드
