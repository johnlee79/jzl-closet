# JZL CLOSET 전체 점검 결과

점검일 2026-08-21 · **아무것도 고치지 않았습니다. 목록만입니다.**

---

## 읽는 법

심각도 네 단계로 나눴습니다.

| 표시 | 뜻 |
| --- | --- |
| **A · 돈/주문** | 돈이 어긋나거나 재고·주문이 잘못될 수 있는 것 |
| **B · 저장 실패** | 저장 버튼을 눌렀는데 실제로 저장되지 않는 것 |
| **C · 화면 어긋남** | 두 화면이 서로 다른 말을 하는 것 |
| **D · 사소한 것** | 불편하지만 손해나 오류로 이어지지 않는 것 |

번호는 심각도 순입니다. **A-1 이 가장 급합니다.**

각 항목에 **재현 방법**과 **왜 그런지**를 적었습니다.
고칠 것을 고르시면 그때 하나씩 손대겠습니다.

### 확인 방법과 한계

- 코드를 직접 읽었고, 손님 화면은 빌드본을 띄워 눌러 봤습니다.
- **관리자 화면은 로그인하지 않아 화면으로 확인하지 못했습니다.** 코드만 읽었습니다.
- **SQL·스크립트를 실행하지 않았습니다.** DB 안의 실제 데이터는 확인하지 못했습니다.
- 모바일은 창 크기를 바꿀 수 없어, 폭을 실제 기기 크기로 좁힌 상자를 만들어
  글자 폭을 **재서** 확인했습니다. 실제 기기로 본 것은 아닙니다.

---

# A · 돈 / 주문

## A-1. 동시에 주문이 들어오면 재고보다 많이 팔릴 수 있습니다

**심각도** A — 가장 급합니다
**어디** 모든 주문 (손님이 [주문하기] 를 누를 때)

**재현**

1. 어떤 옵션의 재고를 5개로 둡니다
2. 서로 다른 손님 두 명이 **거의 같은 순간** 그 옵션을 3개씩 주문합니다
3. 두 주문 다 성공합니다. 재고는 2개로 남습니다
4. 실제로는 6개가 팔렸는데 재고는 2개라고 표시됩니다

**왜 그런지**

재고가 `products.options` 라는 하나의 JSON 칸 안에 들어 있습니다.
재고를 깎을 때 이렇게 합니다.

```
1) 상품을 통째로 읽는다   → 재고 5
2) 화면(코드) 안에서 뺀다 → 5 - 3 = 2
3) 통째로 다시 쓴다       → 2
```

두 주문이 동시에 1번을 하면 **둘 다 5를 읽습니다.** 각자 2를 계산하고
각자 2를 씁니다. 나중에 쓴 쪽이 이깁니다. 3개가 사라집니다.

주문 직전 재고 검사(`priceLines`)도 같은 방식이라 막지 못합니다.
검사와 차감 사이에 다른 주문이 끼어들 수 있습니다.

**참고** — 포인트는 이 문제가 없습니다. `apply_point_change` 라는 DB 함수로
한 번에 처리하고 있습니다. 재고에는 같은 장치가 없습니다.

**얼마나 자주 나나** 평소에는 거의 안 납니다. 다만 **재고가 1~2개 남은 인기
상품**, **세일 시작 직후**, **SNS 에 올린 직후**처럼 주문이 몰리는 순간에 납니다.
하필 그때가 가장 아픈 순간입니다.

---

## A-2. 관리자가 품목을 하나씩 다 취소하면 손님 포인트가 돌아오지 않습니다

**심각도** A
**어디** 관리자 → 주문 관리 → 주문 상세 → 품목별 [취소]

**재현**

1. 손님이 포인트 5,000원을 쓰고 상품 2개를 주문합니다
2. 관리자가 주문 상세에서 상품을 **하나씩 [취소]** 합니다
3. 마지막 하나를 취소하면 주문이 "취소완료" 로 바뀝니다
4. **손님의 포인트 5,000원이 돌아오지 않습니다**
5. 구매 적립도 회수되지 않습니다
6. 텔레그램 취소 알림도 가지 않습니다

**왜 그런지**

품목을 하나씩 취소하는 함수(`cancelOrderItem`)는 마지막에 이렇게 합니다.

```js
if (remaining.length === 0 && order.status !== 'cancelled') {
  await supabase.from(ORDERS).update({ status: 'cancelled' }).eq('id', orderId);
}
```

상태만 바꿉니다. 포인트 반환·적립 회수·알림은 전부
`updateOrderStatus` 라는 다른 함수 안에 있는데, 여기서는 그 함수를 거치지 않고
DB 를 직접 고칩니다.

**[취소 완료] 버튼**으로 취소하면 정상적으로 돌아갑니다. 품목을 하나씩
취소해서 전부 없앤 경우에만 새는 길입니다.

**재고는 돌아갑니다.** 품목마다 따로 되돌리고 있습니다. 포인트만 빠졌습니다.

---

## A-3. 부분 취소를 하면 결제금액이 마이너스가 될 수 있습니다

**심각도** A
**어디** 관리자 → 주문 관리 → 주문 상세 → 품목별 [취소]

**재현**

1. 손님이 상품 10,000원어치를 사면서 포인트 5,000원을 씁니다 (실제 결제 5,000원)
2. 관리자가 그 상품을 취소합니다
3. 주문의 결제금액이 **−5,000원**이 됩니다

**왜 그런지**

금액을 다시 계산할 때 할인(쓴 포인트)을 그대로 빼기만 합니다.

```js
const totalAmount = itemsTotal + shippingFee + extraShippingFee - order.discount;
//                      0      +     0       +        0        -    5000   = -5000
```

남은 상품이 없으면 상품금액이 0인데 할인은 그대로 5,000원을 빼서 음수가 됩니다.
전부 취소가 아니어도, 남은 상품 금액보다 쓴 포인트가 크면 같은 일이 납니다.

**뒤따르는 문제** — 이 음수가 매출 통계와 주문 목록에 그대로 들어갑니다.
관리자 화면과 CSV 내보내기에 마이너스 주문이 섞입니다.

---

## A-4. 구매 적립이 두 번 나갈 수 있습니다

**심각도** A
**어디** 관리자 → 주문 상태를 배송완료로 바꿀 때

**재현** (조건이 겹쳐야 납니다)

1. 관리자가 주문을 **배송완료**로 바꿉니다 → 손님에게 적립 포인트가 나갑니다
2. 그 직후 "이 주문은 적립을 줬다" 는 표시를 남기는 저장이 **실패합니다**
   (일시적인 연결 문제 등)
3. 관리자가 상태를 **결제완료 → 배송완료**로 다시 바꿉니다
4. **적립이 또 나갑니다**

**왜 그런지**

`lib/points.ts` 에서 포인트를 먼저 주고, 그 다음에 표시를 남깁니다.

```js
await tryEarnPoints(userId, amount, 'purchase', '구매 적립', orderId);
await supabase.from('orders').update({ points_earned: amount }).eq('id', orderId);
//                                                     ↑ 성공했는지 확인하지 않습니다
```

두 번째 줄이 실패해도 아무도 모릅니다. 오류를 확인하지도, 기록하지도 않습니다.
이 프로젝트의 다른 쓰기 경로는 `assertWritten` 으로 확인하는데 여기만 빠져 있습니다.

**얼마나 자주 나나** 드뭅니다. 다만 나면 손님에게 준 돈이라 되돌리기 어렵습니다.

---

## A-5. 포인트 차감에 실패하면 할인만 남을 수 있습니다

**심각도** A
**어디** 손님이 포인트를 쓰고 주문할 때

**재현** (조건이 겹쳐야 납니다)

1. 손님이 포인트를 쓰고 주문합니다
2. 포인트 차감이 실패합니다 (다른 창에서 이미 다 써 버린 경우 등)
3. 코드가 "그럼 할인을 없던 걸로" 하고 주문 금액을 되돌리려 합니다
4. **그 되돌리기가 실패하면 아무도 모릅니다**
5. 주문에는 할인이 남고, 손님 포인트는 그대로입니다 → 공짜 할인

**왜 그런지**

```js
await supabase
  .from(ORDERS)
  .update({ discount: 0, total_amount: totalAmount + discount })
  .eq('id', row.id);
//  ↑ 성공했는지 확인하지 않습니다
```

A-4 와 같은 모양입니다. 되돌리기 자체가 실패할 수 있는데 확인하지 않습니다.

---

## A-6. 장바구니에 51줄 이상 담으면 화면 합계와 실제 결제액이 어긋납니다

**심각도** A
**어디** 장바구니 · 주문서
**★ 오늘 제가 만든 것입니다.**

**재현**

1. 서로 다른 상품(또는 옵션)을 **51종류 이상** 장바구니에 담습니다
2. 51번째부터는 가격 확인을 건너뜁니다
3. 화면 합계에는 그 상품들이 **빠진 금액**이 나옵니다
4. 그런데 주문에는 **포함되어** 들어갑니다
5. 결제 금액이 화면보다 큽니다

**왜 그런지**

오늘 만든 "지금 가격 다시 확인하기" 가 한 번에 50줄까지만 봅니다.

```js
const MAX_CART_LINES = 50;
const safe = items.slice(0, MAX_CART_LINES);
```

51번째부터는 서버 응답이 없어 담을 때 값으로 그려지는데, 그 줄도
"주문 가능" 으로 취급되어 주문에 들어갑니다. 서버 합계에는 안 들어갔는데
주문에는 들어가는 것입니다.

**얼마나 자주 나나** 손님이 51종류를 담는 일은 드뭅니다. 다만 이건
**오늘 고친 바로 그 문제**가 51번째부터 되살아나는 것이라, 고친 의미가
반쪽이 됩니다.

**고치는 방법은 간단합니다** — 잘라 낸 줄은 주문에 넣지 않거나,
잘랐다는 사실을 손님에게 알리면 됩니다.

---

# A · 보안

## A-7. 관리자 로그인에 시도 횟수 제한이 없습니다

**심각도** A
**어디** `/admin/login`

**재현**

1. `/api/admin/login` 에 비밀번호를 넣어 계속 보냅니다
2. **몇 번을 틀려도 막히지 않습니다.** 얼마든지 계속 시도할 수 있습니다

**왜 그런지**

손님 로그인에는 제한이 있습니다.

```js
const limited = rateLimit(`login:${ip}`, 5, 60_000);   // 1분에 5회
```

**관리자 로그인 쪽에는 이 코드가 없습니다.** 비밀번호만 맞으면 주문·회원 정보·
설정 전체에 들어갈 수 있는 입구인데, 손님 로그인보다 보호가 약합니다.

회원가입에도 제한이 있고(1분 5회), 주문 조회에도 있습니다.
**관리자만 없습니다.**

---

## A-8. KSNET 노티 자동 완료를 켜면 위험합니다 (지금은 꺼져 있습니다)

**심각도** A — 단, **지금은 꺼져 있어 실제 위험은 없습니다**
**어디** 관리자 → 설정 → 결제·주문 → "KSNET 노티 자동 완료 처리"

**지금 상태** 기본값 꺼짐. 켜져 있지 않으면 기록만 남기고 주문 상태를 바꾸지 않습니다.

**켜면 무슨 일이 생기나**

`/api/payment/ksnet/notify` 는 **인증이 없습니다.** 주소만 알면 누구나
아무 내용이나 보낼 수 있습니다. 코드 주석에도 그렇게 적혀 있습니다.

금액과 주문번호를 우리 DB 와 대조하기는 합니다. 하지만 주문번호는
`ORD-20260821-0001` 같은 규칙적인 형태라 **추측할 수 있고**, 금액은
주문 조회 화면에서 확인할 수 있습니다.

**켜기 전에 반드시 확인하실 것**

1. KSNET 에서 **발신 IP** 를 받아 그 IP 만 받도록 막기
2. KSNET 의 **노티 규격**(어떤 값이 오는지)을 문서로 확정하기
3. 요청 횟수 제한 걸기 (지금은 없어서 알림을 계속 울리게 만들 수 있습니다)

---

# B · 저장 버튼을 눌렀는데 저장 안 되는 곳

## B-1. 판매정보·이벤트 문구는 "비우기" 가 되지 않습니다

**심각도** B
**어디**
- 관리자 → 설정 → **판매정보** — 배송 기간 · 교환/반품 안내 · 교환 비용 · 불가 사유 · A/S 안내
- 관리자 → 설정 → **문구·이벤트** — 가입 완료 문구 · 마이페이지 환영 문구
- 관리자 → 설정 → **스토어 정보** — 소개 · 슬로건 · 카테고리 등

**재현**

1. 관리자 → 설정 → 판매정보 에서 **"A/S 안내"** 칸의 글을 전부 지웁니다
2. 저장합니다. **"저장했습니다"** 초록 메시지가 뜹니다
3. 상품 상세의 [판매정보] 탭을 엽니다
4. **지운 글이 그대로 있습니다**
5. 관리자 화면을 새로고침하면 칸에도 글이 되살아나 있습니다

**왜 그런지**

값을 다듬는 함수가 **빈 값을 "입력 안 함" 으로 보고 기본값으로 되돌립니다.**

```js
function text(value, fallback) {
  return typeof value === 'string' && value.trim() ? value : fallback;
  //                                  ↑ 비어 있으면 fallback
}
```

같은 화면의 "배송비 안내" 와 "반품 주소" 는 fallback 이 빈 문자열이라
정상적으로 비워집니다. 그래서 **어떤 칸은 비워지고 어떤 칸은 안 비워집니다.**
화면에는 그 차이를 알려 주는 표시가 없습니다.

**판매정보는 법으로 정해진 고지 항목이라 늘 채워 두는 것이 맞습니다.**
문제는 "비워지지 않는다" 가 아니라 **"저장했다고 해 놓고 안 한다"** 는 점입니다.

---

## B-2. 반품·교환 배송비에 음수를 넣으면 조용히 기본값이 됩니다

**심각도** B
**어디** 관리자 → 설정 → 배송·반품 → 반품 배송비 / 교환 배송비
**★ 오늘 제가 만든 칸입니다.**

**재현**

1. 반품 배송비에 `-1000` 을 넣고 저장합니다
2. "저장했습니다" 가 뜹니다
3. 새로고침하면 **3,000** 으로 돌아와 있습니다

**왜 그런지**

같은 화면의 기본배송비·무료배송기준·도서산간비는 저장 전에 검사합니다.

```js
if (input.baseFee < 0 || input.freeThreshold < 0 || input.islandFee < 0) {
  return { ok: false, error: '금액은 0 이상으로 넣어 주세요.' };
}
```

**제가 오늘 넣은 두 칸이 이 검사에 빠져 있습니다.** 검사를 통과해 저장되고,
값을 다듬는 단계에서 음수가 걸러져 기본값으로 바뀝니다.

같은 성격의 칸이 포인트 설정에도 있습니다 — 가입 축하·리뷰·생일 포인트에
음수를 넣으면 같은 일이 납니다. (다만 그쪽은 원래 그랬습니다)

---

## B-3. 소셜 로그인 시 이름·이메일 보충이 실패해도 모릅니다

**심각도** B — 낮은 편
**어디** 구글 / 카카오 로그인

**재현** 재현하기 어렵습니다. 저장 실패가 겹쳐야 납니다.

**왜 그런지**

구글·카카오로 처음 들어온 회원의 비어 있는 이름·이메일을 채우는 코드입니다.

```js
if (Object.keys(patch).length > 0) {
  await supabase.from(TABLE).update(patch).eq('id', input.id);
  //  ↑ 성공했는지 확인하지 않습니다
}
```

실패하면 회원 이름이 비어 있는 채로 남습니다. 주문서에 이름이 안 채워지고,
관리자 회원 목록에도 빈칸으로 보입니다. 원인을 찾을 단서가 남지 않습니다.

---

# C · 화면 두 곳이 서로 다른 말을 하는 곳

## C-1. 출고까지 걸리는 날짜가 두 가지입니다

**심각도** C
**어디**

| 화면 | 뭐라고 하나 |
| --- | --- |
| 상품 상세 → [판매정보] 탭 | 결제(입금) 확인 후 **2~5일** 이내에 발송 |
| 배송·교환·반품 안내 (`/guide`) | 결제 확인이 완료된 후 **1~3영업일** 이내에 출고 |
| 이용약관 제10조 | 대금을 지급한 날부터 **영업일 기준 3일** 이내 |
| 설정 → 배송·반품 (leadTime) | 주문 확인 후 **1~3영업일** 내 출고 |

**재현** 아무 상품이나 열어 [판매정보] 탭을 보고, 그다음 `/guide` 를 봅니다.

**왜 그런지**

각 화면이 서로 다른 설정에서 문구를 가져옵니다.
`판매정보` 는 별도 설정(`SalesSettings.deliveryPeriod`)을 쓰고,
`안내 페이지` 는 사이트 문구(`copy.guide`)를 씁니다.
두 곳을 잇는 장치가 없습니다.

**어느 쪽이 맞는지 정하셔야 합니다.** 손님이 두 화면을 다 보면 어느 쪽을
믿어야 할지 모릅니다. 늦어졌을 때 "2~5일이라며" 와 "1~3영업일이라며" 중
어느 쪽으로도 항의할 수 있습니다.

---

## C-2. 입금 기한 "24시간" 이 문구에 글자로 박혀 있습니다

**심각도** C
**어디** 장바구니 오른쪽 "결제 수단 안내"

**지금 문구**

> 무통장입금을 고르신 경우, 안내된 계좌로 입금해 주시면 확인 후 발송합니다.
> **주문일로부터 24시간 안에** 입금이 확인되지 않으면 주문이 자동으로 취소됩니다.

**재현**

1. 관리자 → 설정 → 결제·주문 에서 **입금 기한을 48시간**으로 바꿉니다
2. 장바구니를 엽니다 → 여전히 **"24시간"** 이라고 나옵니다
3. 주문 완료 화면을 엽니다 → **"48시간"** 이라고 나옵니다
4. 자동 취소는 **48시간**에 걸립니다

**왜 그런지**

주문 완료 화면은 설정값을 읽어 씁니다.

```jsx
아래 계좌로 <strong>{payment.depositHours}시간</strong> 이내에 입금해 주시면…
```

장바구니 문구는 관리자 문구에 **글자로 적혀 있습니다.** 설정을 바꿔도
따라가지 않습니다.

**곁들여** — 자동취소를 **꺼도** 이 문구는 "자동으로 취소됩니다" 라고 계속
말합니다. 취소되지 않는데 취소된다고 겁을 주는 셈입니다.

---

## C-3. 적립 안내 문구가 두 가지입니다

**심각도** D — 사소하지만 **오늘 제가 만든 것입니다**

| 화면 | 문구 |
| --- | --- |
| 장바구니 · 주문서 | 적립 예정 · **(배송완료 시점에 지급)** |
| 주문 완료 · 주문 조회 · 주문 내역 | 적립 예정 · **(배송완료 후 지급)** |

같은 뜻인데 말이 다릅니다. 어느 쪽으로 맞출지 정해 주시면 통일하겠습니다.
"배송완료 후 지급" 이 짧고 분명합니다.

---

## C-4. 관리자에 저장된 옛 약관이 화면에 나오고 있습니다

**심각도** A — 다만 **이미 알고 계신 건이고 되돌리기만 하시면 됩니다**

`새약관.md` 에 자세히 정리해 두었습니다. 요점만 옮깁니다.

- 화면에 나오는 약관은 **12개 조문**, 코드에 있는 것은 **18개 조문**
- 화면 약관에 **사실과 다른 결제 문장이 셋** 남아 있습니다
  - 제5조 "본 몰은 별도의 온라인 결제 기능을 제공하지 않으며"
  - 제6조 "회사는 주문 확인 후 이용자에게 개별적으로 지급 방법을 안내합니다"
- 회원가입·포인트·게시물 조항이 **통째로 없습니다** (기능은 다 돌아가고 있습니다)

관리자 → 디자인 관리 → 사이트 문구 → **이용약관 · 주문 절차 3단계 ·
주문 절차 아래 상세 안내** 세 항목에 [기본값으로 되돌리기].

---

# D · 모바일

## D-1. 마이페이지 요약 세 칸의 글자가 3줄로 접힙니다

**심각도** D
**어디** 마이페이지 첫 화면 위쪽 세 칸 (결제 확인 중 / 배송중 / 배송완료)

**실제로 재 본 결과**

| 기기 폭 | 한 칸 폭 | 글자가 쓸 수 있는 폭 | 결과 |
| --- | --- | --- | --- |
| 320px (아이폰 SE) | 85px | **45px** | "결제 확인 중" **3줄** · "배송완료" **3줄** |
| 375px (아이폰) | 104px | 64px | "결제 확인 중" **3줄** · 나머지 1줄 |

**왜 그런지**

칸이 화면 크기와 상관없이 **항상 3칸**으로 고정돼 있고(`grid-cols-3`),
각 칸 안쪽에 20px 씩 여백(`p-5`)이 있습니다. 320px 기기에서는
글자가 쓸 수 있는 폭이 45px 밖에 남지 않습니다.

**★ 오늘 제가 상태 탭을 묶으면서 "결제대기"(4글자) 를 "결제 확인 중"(6글자)
으로 바꿔 더 심해졌습니다.** 이름은 그대로 두고 칸 여백을 줄이거나,
좁은 화면에서만 세로로 쌓는 방법이 있습니다.

**넘쳐서 잘리지는 않습니다.** 접힐 뿐입니다.

## 모바일에서 확인했는데 괜찮았던 것

- 주문 완료 화면의 **주문번호** — 320px 에서 230px, 넘치지 않습니다
- 오늘 만든 **진행 단계 네 칸** — 320px 에서 네 이름 모두 한 줄
- 상품 상세 **가격 줄** — 375px 에서 값과 배지가 한 줄 (320px 은 두 줄이지만 자연스럽게 끊깁니다)
- 상품 상세 **탭 네 개** (상품정보/리뷰/Q&A/판매정보) — 320px 에서 넘치지 않습니다
- **결제수단 네 개** — 세로로 쌓여 있어 문제없습니다
- 관리자 **표** 전부 가로 스크롤 상자 안에 있습니다 (인쇄 화면만 없는데 의도된 것입니다)

---

# 확인했는데 문제가 없던 것

없는 문제를 만들어 적지 않기 위해, 살펴봤지만 괜찮았던 것도 남깁니다.

**값이 비어 있을 때 (5번 항목)**
- 배너 없음 · 브랜드 없음 · 상품 없음 · 리뷰 없음 — 전부 빈 화면 안내가 따로 있습니다
- 리뷰 평점 평균 계산 — 리뷰 0개면 계산 전에 빠져나갑니다 (0으로 나누지 않습니다)
- 통계·추천 비율 계산 — 전부 0인지 먼저 봅니다
- 장바구니 비었을 때 · 주문 상품이 전부 취소됐을 때 — 각각 안내가 있습니다

**오류 안내 (7번 항목)**
- 회원가입·로그인·비밀번호 재설정·리뷰·문의·주문서 — 전부 오류를 화면에 보여 줍니다
- 이미지 업로드 실패 — 관리자 화면에 메시지가 뜹니다
- 결제 실패·확인 중 — 손님에게 "실패" 라고 단정하지 않고 안내합니다

**삭제 보호**
- 분류 삭제 — 상품이 남아 있으면 막고 개수를 알려 줍니다
- 소분류가 남아 있으면 대분류 삭제를 막습니다
- 브랜드 삭제 — 상품이 남아 있으면 막습니다

**보안**
- 크론 3개 — `CRON_SECRET` 으로 막혀 있습니다
- 비회원 주문 조회 — 주문번호 + 연락처가 둘 다 맞아야 하고 횟수 제한이 있습니다
- 주문 완료 화면 — 서명이 있어야 열립니다 (번호를 바꿔 가며 남의 주문을 볼 수 없습니다)
- 비밀글 문의 — 비밀번호로 보호되고 목록에서는 가려집니다
- 손님 로그인·회원가입 — 횟수 제한이 있습니다

---

# 확인하지 못한 것

정직하게 남깁니다.

1. **관리자 화면 전체** — 로그인하지 않아 눈으로 보지 못했습니다.
   위의 관리자 관련 항목은 전부 **코드만 읽고** 적은 것입니다.
   실제로 눌러 봐야 확실해지는 것들입니다.

2. **DB 안의 실제 설정값** — 스크립트를 돌리지 않아, 관리자에 지금 무엇이
   저장돼 있는지 대부분 모릅니다. 손님 화면으로 볼 수 있는 것만 확인했습니다.

3. **실제 결제** — 카드 결제를 끝까지 눌러 보지 않았습니다.
   시험 주문이 남고 알림이 나가는데 스크립트 없이 지울 수 없습니다.

4. **실제 모바일 기기** — 브라우저 창 크기를 바꿀 수 없어, 폭을 좁힌 상자로
   재서 확인했습니다. 실제 아이폰·안드로이드로 본 것은 아닙니다.

5. **동시 주문 (A-1)** — 실제로 두 주문을 동시에 넣어 재현하지 않았습니다.
   코드 구조상 반드시 나는 문제라고 판단한 것입니다.

---

# 제가 먼저 고치겠다고 권하는 순서

정하시는 것은 사장님이지만, 참고로 제 판단을 적습니다.

| 순서 | 항목 | 이유 |
| --- | --- | --- |
| 1 | **A-7** 관리자 로그인 제한 | 몇 줄이면 되고, 뚫리면 전부 잃습니다 |
| 2 | **A-6** 장바구니 51줄 | 오늘 만든 것이고 금방 고칩니다 |
| 3 | **A-2 · A-3** 부분 취소 | 관리자가 실제로 쓰는 버튼입니다. 지금도 새고 있습니다 |
| 4 | **C-4** 약관 되돌리기 | 고칠 것도 없고 버튼만 누르면 됩니다 |
| 5 | **A-1** 재고 동시성 | 가장 위험하지만 DB 함수를 새로 만들어야 해서 가장 큽니다 |
| 6 | **C-1 · C-2** 문구 어긋남 | 손님 항의로 이어지는 자리입니다 |
| 7 | **B-1 · B-2** 저장 실패 | 관리자만 겪습니다 |
| 8 | **A-4 · A-5 · B-3** 확인 없는 저장 | 드물게 나지만 나면 찾기 어렵습니다 |
| 9 | **D-1 · C-3** 모바일·문구 | 급하지 않습니다 |
