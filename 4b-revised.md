# JZL CLOSET 4단계-B(수정본) — 미완결 카드 주문 처리

**이 파일은 기존 `4b.md`를 대체합니다.** 4b.md는 4a.md와 대조해 보니 그대로 구현할 수 없는 부분이 있어 다시 만들었습니다.

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel Pro(서울 리전)
- 고객 화면 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #46433E / wine #6A2E3C
- **그림자 금지, 이모지 금지** (아이콘은 SVG로 직접 그릴 것)
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role
- 새 SQL은 `supabase/schema-4b.sql`로 만들고 실행 순서를 반드시 보고할 것
- 작업 후 `npm run build`와 `npm run check:schema`를 실행해 확인할 것
- **추측하지 말고 실제로 확인해줘.** 파일 경로·컬럼명·상태값을 기억이나 관례로 짐작하지 말 것
- 확실하지 않은 것은 임의로 결정하지 말고 마지막 `## 질문`에 전부 남길 것

---

# ⚠️ 이 작업의 성격

**재고와 돈이 얽힌 문제입니다.**

```
너무 늦게 풀면   팔 수 있는 상품이 재고 0으로 잠김
너무 빨리 풀면   결제한 손님의 주문이 사라짐  ← 이쪽이 훨씬 나쁨
```

**확실하지 않으면 재고를 푸는 쪽이 아니라 사람이 확인하는 쪽으로 보내세요.**

---

# [0] 이미 만든 것이 있는지 먼저 확인

`4b.md`로 작업이 시작됐을 수 있습니다. **코드를 새로 쓰기 전에 아래를 확인하고 보고하세요.**

- 카드 주문 정리용 cron·API 라우트가 이미 있는가
- `supabase/schema-4b.sql`이 이미 있고 실행됐는가
- `vercel.json`의 cron이 이미 변경됐는가

있으면 지우고 새로 만들지 말고, **이 명세에 맞게 고치는 방향**으로 진행하세요.

---

# [1] 먼저 조사할 것 — 여기가 이번 작업의 성패입니다

## 1-1. ★ 결제 Key(`reCommConId`)를 주문에 저장하고 있는가

**이것이 가장 중요합니다.**

4a의 승인 재조회(`recv_post.jsp`, `sndActionType=1`)는 **`sndCommConId`(결제창이 돌려준 결제 Key)를 키값으로 씁니다. 주문번호로는 조회할 수 없습니다.**

따라서:

```
결제 Key를 주문에 저장해 뒀다  →  나중에 재조회 가능
저장하지 않았다              →  재조회 자체가 불가능
```

**실제로 코드와 DB를 열어 확인하세요.**

1. `reCommConId`(또는 그에 해당하는 값)를 받는 지점이 어디인가
2. 그 값을 주문 행에 **저장하고 있는가**, 아니면 승인 확인에만 쓰고 버리는가
3. 저장한다면 컬럼명이 무엇인가

**참고 — 컬럼 현황 (2-A에서 확정된 사실입니다. 다시 조사하지 마세요)**

`orders`에 PG용 컬럼으로 `pg_provider` · `pg_tid` · `paid_at` 이 이미 있습니다.
`pg_tid`는 **KSNET 거래번호(`trno`) 자리**이고, **결제 Key(`reCommConId`)를 담을 컬럼은 없습니다.**

4-A에서 별도로 추가했는지 확인하고, 없으면 `supabase/schema-4b.sql`로 추가하세요.
`pg_tid`에 결제 Key를 같이 넣어 돌려쓰지 마세요. 취소 요청 시 에이전시에 넘길 값이 거래번호라서 섞이면 사고가 납니다.

**저장하고 있지 않다면, 저장하도록 먼저 고치세요.** 이후 주문부터는 재조회가 가능해집니다.
이미 쌓인 `결제대기` 주문은 키가 없으므로 [3-3]의 방식으로 처리합니다.

## 1-2. 나머지 조사 항목

**재고 차감 시점은 이미 확정되어 있습니다. 조사하지 마세요.**
2-A에서 `주문이 생성되는 시점에 옵션 조합의 재고를 차감한다`로 정해졌고, `stock`이 `null`인 조합은 차감하지 않습니다.

아래만 조사하세요.

1. ★ **카드 주문과 무통장 주문을 무엇으로 구분하고 있는가.** `payment_method`에 실제로 어떤 값이 들어가는지 DB에서 확인하세요 (`bank_transfer` 외에 카드·카카오페이·네이버페이가 각각 무엇으로 저장되는지)
2. ★ **무통장 자동취소가 `payment_method`로 거르고 있는가.** 상태만 보고 있다면 카드 주문까지 취소하고 있을 수 있습니다. [2-2] 참조
3. 무통장입금 자동취소는 어떤 cron·어떤 라우트로 도는가
4. 취소 시 재고·포인트·쿠폰을 되돌리는 함수가 이미 있는가, 있다면 이름과 위치
5. 4-A가 `승인확인실패`·`검토필요`에 해당하는 상태값을 추가했는가. 추가했다면 **실제 문자열**은 무엇인가
6. 옵션 조합별 재고가 어느 테이블·어느 컬럼에 있는가 (되돌릴 대상)
7. 지금 `pending_payment`로 남아 있는 카드 주문이 몇 건이고, 그중 결제 Key가 있는 건이 몇 건인가

---

# [2] 상태값 정리 — 4a와 어긋나 있습니다

4a에서 정한 상태와 4b.md에서 쓴 `결제실패`가 서로 맞지 않습니다. **아래로 통일하세요.**

**DB에 저장되는 값은 영문입니다.** 2-A에서 확정된 실제 문자열은 아래와 같습니다. 한글로 새로 만들지 마세요.

```
pending_payment(입금대기) | paid(결제완료) | preparing | shipping
| delivered | confirmed | cancelled | exchange | returned | failed(결제실패)
```

| 화면 표기 | DB 값 | 언제 | 재고 | 사람 확인 |
|---|---|---|---|---|
| 결제대기 | `pending_payment` | 결제창 호출 전·중 | 잠김 | — |
| 결제완료 | `paid` | 승인 확인까지 끝남 | 잠김(정상) | — |
| 결제실패 | `failed` | **조회 결과 승인이 없음이 확인됨** | 되돌림 | 불필요 |
| 승인확인실패 | **4-A 확인 후 결정** | 조회를 못 했거나 키가 없어 알 수 없음 | **그대로 둠** | **필요** |
| 검토필요 | **4-A 확인 후 결정** | 금액·주문번호 불일치 | 그대로 둠 | 필요 |
| 취소 | `cancelled` | 실제 환불까지 끝남 | 되돌림 | — |

`승인확인실패`·`검토필요`는 2-A의 상태 목록에 없습니다. **4-A에서 이미 추가했다면 그 값을 그대로 쓰고, 없으면 신설**하세요. 임의로 다른 이름을 만들어 두 벌이 되게 하지 마세요.

신설한다면 관리자 주문 목록의 **상태 탭에도 추가**해야 합니다. 탭에 없으면 운영자가 그 주문을 영영 못 봅니다.

**핵심: `failed`와 `승인확인실패`를 절대 섞지 마세요.**
`failed`는 "확인했고 결제 안 됨", `승인확인실패`는 "모름"입니다. 모르는 것을 실패로 적으면 손님 돈이 사라집니다.

---

## 2-2. ⚠️ 카드와 무통장을 상태로 구분하면 안 됩니다

**`pending_payment`은 무통장 입금대기와 카드 결제대기가 함께 씁니다.**
2-A에서 `나중에 PG를 붙이면 "입금대기" 자리에 카드결제가 들어간다`고 설계했기 때문입니다.

따라서 상태만 보고 대상을 고르면 **두 방향으로 사고가 납니다.**

```
무통장 자동취소가 상태만 본다  →  카드 결제 중인 주문을 24시간 뒤 취소
카드 정리가 상태만 본다        →  입금 기다리는 무통장 주문을 40분 뒤 정리  ← 훨씬 나쁨
```

**대상 선정은 반드시 `payment_method`로 하세요.**

- 카드 정리 대상: `status = 'pending_payment'` **AND** `payment_method`가 무통장이 아닌 것
- 무통장 자동취소 대상: `status = 'pending_payment'` **AND** `payment_method`가 무통장인 것

[1-2] 2번에서 **무통장 자동취소가 지금 `payment_method`로 거르고 있는지 반드시 확인하세요.**
거르고 있지 않다면 그건 이미 나 있는 버그입니다. **로직을 바꾸지 말고, 대상 조건에 `payment_method` 필터만 더하세요.** 그 한 줄이 무엇인지 보고에 적으세요.

**핵심: `결제실패`와 `승인확인실패`를 절대 섞지 마세요.**
`결제실패`는 "확인했고 결제 안 됨", `승인확인실패`는 "모름"입니다. 모르는 것을 실패로 적으면 손님 돈이 사라집니다.

기존 코드에 이미 다른 문자열을 쓰고 있으면 **기존 값을 따르고, 대응표를 보고에 적으세요.**

## 손님에게 보이는 문구

내부 상태명을 그대로 노출하지 마세요.

```
결제실패        → "결제가 완료되지 않았습니다"
승인확인실패     → "결제 확인 중입니다. 확인되는 대로 안내드립니다"
검토필요        → "결제 확인 중입니다. 확인되는 대로 안내드립니다"
```

**"결제 실패"라고 단정해서 안내하는 상태는 `결제실패` 하나뿐입니다.**

---

# [3] 만들 것

## 3-1. 결제창에서 취소를 눌렀을 때

4a에서 `reCnclType=1`로 돌아오는 것이 확인되었습니다. **이 경로는 확실히 결제가 안 된 것**입니다.

- 주문을 `결제실패`로 변경
- **재고 즉시 되돌림**
- 손님 안내: "결제가 취소되었습니다. 장바구니는 그대로 있으니 다시 시도하실 수 있습니다"
- **장바구니를 비우지 마세요.**

## 3-2. 결제 Key가 있는 `결제대기` 주문 — 재조회로 판정

일정 시간이 지난 주문을 주기적으로 정리합니다.

```
① recv_post.jsp 로 승인 여부 재조회 (sndActionType=1)

② authyn = O 이고 금액·주문번호가 우리 DB와 일치
   → 결제완료. 재고 유지. 텔레그램 알림 (우리가 놓쳤던 승인이므로 반드시 알림)

③ authyn = O 인데 금액이나 주문번호가 다름
   → 검토필요. 재고 그대로. 텔레그램 알림
      (자동으로 완료 처리하지 마세요 — 4a에서 가장 위험하다고 정한 경우입니다)

④ authyn = X (승인 없음이 확인됨)
   → 결제실패. 재고 되돌림. 알림 없음

⑤ 조회 자체가 실패 (네트워크·응답 파싱 실패 등)
   → 1회 재시도. 그래도 실패하면 승인확인실패로 변경. 재고 그대로. 텔레그램 알림 1회
```

**⑤에서 상태를 반드시 바꾸세요.** `결제대기`로 두면 다음 cron이 같은 주문을 또 집어 10분마다 같은 알림이 반복됩니다.

## 3-3. 결제 Key가 없는 `결제대기` 주문

결제창을 연 뒤 아무 응답 없이 이탈한 경우입니다. **조회할 열쇠가 없습니다.**

일정 시간이 지나면:

- `승인확인실패`로 변경
- **재고는 되돌립니다** (이 건들이 재고를 묶는 주된 원인입니다)
- 텔레그램은 **건별로 보내지 말고**, [3-6]의 일일 요약에만 포함

재고를 되돌리되 주문은 남기고 사람이 확인할 수 있게 두는 절충입니다.
`결제실패`가 아니라 `승인확인실패`인 이유는, KSNET 쪽에서 승인이 살아 있을 가능성을 배제할 수 없기 때문입니다([6] 질문 참조).

## 3-4. 시간 기준

- 무통장입금 24시간과 **다른 값**이어야 합니다
- **기본 40분**. 카드 정보 입력 + 앱 전환(카카오페이·네이버페이) + 3D인증까지 넉넉히 잡은 값입니다
- **관리자에서 바꿀 수 있게** 하세요. `설정 → 결제·주문`, 무통장 자동취소 시간과 나란히
- 설명 문구: `이 시간이 지난 결제대기 카드 주문은 KSNET에 승인 여부를 확인한 뒤 정리합니다`

## 3-5. ★ cron — 반드시 분리하세요

**무통장 자동취소와 같은 cron을 쓰면 안 됩니다.** 로직을 안 고쳐도 실행 빈도가 하루 1회에서 144회로 바뀌면 그것도 동작 변경입니다.

`vercel.json`을 아래와 같이 **세 개로 분리**하세요. 경로는 기존 구조를 확인해서 맞추세요.

| 경로 | 주기(UTC) | KST | 역할 |
|---|---|---|---|
| 기존 무통장 자동취소 라우트 | `0 19 * * *` | 04:00 | **건드리지 말 것** |
| 카드 정리 (신설) | `*/10 * * * *` | 10분마다 | [3-2]·[3-3] |
| 자정 전 마감 점검 (신설) | `50 14 * * *` | 23:50 | [3-7] |

무통장 자동취소 라우트는 **파일도 열지 마세요.**

## 3-6. 텔레그램 알림 — 1회 원칙

```
· 우리가 놓쳤던 승인을 발견했을 때        건별 즉시   ← 가장 중요
· 금액·주문번호 불일치 (검토필요)         건별 즉시
· 조회 실패로 승인확인실패가 된 건         건별 1회
· 결제 Key 없이 정리된 건                 일일 요약 1회
· 정상적으로 결제실패 처리된 건            알리지 않음
```

**같은 주문으로 두 번 알리지 마세요.** 주문 행에 `알림 보냄` 표시를 두고 조건부로 갱신하세요.
알림이 잦으면 정작 중요한 걸 놓칩니다.

## 3-7. 자정 경계 처리 (신설 — 4b.md에 없던 부분)

4a에 **재조회는 당일에 한해 가능**하다고 되어 있습니다.
23시 50분에 들어온 주문은 40분 뒤면 다음 날이라 영영 확인할 수 없게 됩니다.

23:50 cron에서:

- **그날 생성된 `결제대기` 카드 주문 중 결제 Key가 있는 건을 전부** 재조회
- 40분이 안 지났어도 조회합니다. **조회는 조회일 뿐 상태를 바꾸지 않습니다**
- 판정이 확실한 건(②③④)만 [3-2] 규칙대로 처리
- 아직 진행 중이라 판정이 안 서는 건은 **건드리지 말고 그대로 두세요**
- 자정을 넘겨 조회가 불가능해진 주문은 `승인확인실패`로 보내고 일일 요약에 포함

## 3-8. 재고 되돌리기 — 반드시 지킬 것

- **같은 주문의 재고를 두 번 되돌리면 안 됩니다.** DB 수준 조건부 업데이트로 한 번만 일어나게 하세요
- 주문 행에 `재고 복구됨` 표시를 두고, 그 표시가 없을 때만 되돌리도록 하세요

**★ 되돌리면 안 되는 두 가지 — 반드시 제외하세요**

```
① stock이 null인 조합
   2-A에서 "재고를 관리하지 않는 조합은 차감하지 마라"로 정했습니다.
   차감한 적이 없으므로 되돌리면 없던 재고가 생깁니다.

② item_status = 'cancelled' 인 품목
   부분취소로 이미 재고가 돌아갔을 수 있습니다.
   부분취소가 재고를 되돌리고 있는지 먼저 확인하고, 되돌리고 있다면 제외하세요.
```

두 경우 모두 **조용히 건너뛰지 말고 복구 기록에 "제외됨"과 그 이유를 남기세요.** 나중에 재고가 안 맞을 때 이 줄이 단서가 됩니다.
- **되돌릴 때마다 기록을 남기세요** — 언제·어느 주문·어느 옵션·몇 개. 재고가 안 맞을 때 유일한 단서입니다
- **포인트·쿠폰도 함께 되돌려야 합니다.** 3-C의 미입금 자동취소가 쓰는 함수를 그대로 재사용하세요.
  포인트는 반드시 `apply_point_change`를 통하고, 잔액을 직접 수정하지 마세요
- 새 DB 함수를 만들면 `rls-3b.sql`의 pg_proc 조회 방식으로 **service_role 전용 잠금**

## 3-9. 다시 결제하기

`결제실패` 주문에서 다시 결제할 수 있게 하면 손님이 처음부터 담지 않아도 됩니다.

**단, 반드시 새 주문번호를 발급하세요.**
같은 주문번호로 재시도하면 KSNET 쪽에서 앞선 거래와 충돌하고, 앞 시도가 실제로는 승인돼 있었을 경우 **이중 청구**가 됩니다.

- 원 주문과 새 주문을 연결해 두고, 원 주문은 `결제실패`로 남깁니다
- 재고를 이미 되돌렸으므로 **새 주문에서 재고를 다시 확보**해야 합니다.
  그 사이 품절됐으면 재결제를 막고 안내하세요
- 구현이 복잡하다고 판단되면 **만들지 말고 질문으로 남기세요.** 반쯤 만들면 더 위험합니다

## 3-10. 관리자 화면

`승인확인실패`·`검토필요` 주문은 사람이 봐야 합니다.

- 주문 목록에 **이 두 상태만 걸러보는 필터**를 두세요
- 주문 상세에 **결제 Key·KSNET 거래번호·조회 시도 이력**(시각·결과)을 표시하세요.
  ksta.ksnet.co.kr에서 대조할 때 필요합니다. 복사 버튼도 두세요
- 관리자가 확인 후 **`결제완료` 또는 `결제실패`로 직접 확정**할 수 있게 하세요.
  `결제실패`로 확정할 때 재고가 아직 안 돌아왔으면 그때 되돌리도록 하세요

---

# [4] 하지 말 것

- **결제완료 주문을 자동으로 건드리지 마세요.** 대상은 `결제대기`뿐입니다
- **무통장입금 자동취소 로직·cron·라우트를 건드리지 마세요**
- 조회 결과가 불확실한 주문을 `결제실패`로 단정하지 마세요
- 손님에게 "결제 실패"라고 안내하는 상태는 `결제실패` 하나뿐입니다
- 취소·환불 API를 부르려 하지 마세요. **KSNET이 가맹점에 권한을 주지 않습니다**

---

# [5] 확인 사항

1. 결제창에서 취소를 누르면 재고가 즉시 돌아오는가
2. 그때 장바구니가 그대로 남아 있는가
3. 결제 Key가 주문에 저장되는가
4. 시간이 지난 카드 주문이 정리되는가
5. 정리 전에 KSNET 재조회를 **실제로** 하는가
6. **승인이 났던 주문이 결제실패가 되지 않고 결제완료가 되는가** (가장 중요)
7. 금액이 다른 승인이 자동 완료되지 않고 `검토필요`가 되는가
8. 조회 실패 건이 `승인확인실패`가 되고, **같은 주문으로 알림이 두 번 오지 않는가**
9. 같은 주문의 재고를 두 번 되돌리지 않는가
10. 포인트·쿠폰도 함께 되돌아오는가
11. **무통장입금 자동취소가 작업 전과 똑같은 주기로, 똑같이 동작하는가**
11-1. **무통장 주문을 하나 만들어 두고 카드 정리 cron을 돌렸을 때, 그 주문이 건드려지지 않는가**
11-2. **카드 결제대기 주문을 하나 만들어 두고 무통장 자동취소를 돌렸을 때, 그 주문이 취소되지 않는가**
12. 관리자에서 카드 정리 시간을 바꿀 수 있는가
13. 관리자에서 `승인확인실패`·`검토필요` 주문만 걸러볼 수 있는가
14. `npm run build` 통과 (Type error·Failed to compile 0건)
15. `npm run check:schema` 통과

## 테스트 시 유의

4a에 따르면 **테스트 상점아이디로 결제하면 수 초 뒤 자동 취소**됩니다.
따라서 [3-2]②(놓친 승인 발견) 경로는 테스트로 재현하기 어려울 수 있습니다.
재현이 안 되면 억지로 맞추지 말고, **어디까지 확인했고 무엇을 못 해봤는지** 정확히 보고하세요.

---

# [6] 운영자가 KSNET에 확인해야 할 것

아래는 코드로 해결할 수 없습니다. **보고서 맨 앞에 다시 적어 주세요.**

1. 손님이 결제창에서 카드 승인까지 마쳤는데 우리 서버가 결제 Key를 못 받은 경우,
   **KSNET 쪽에는 승인이 살아 있는가?** 살아 있다면 우리가 모르는 매출이 생깁니다
2. 그런 거래를 **주문번호로 조회할 방법**이 있는가 (`recv_post.jsp`는 결제 Key가 있어야 합니다)
3. 재조회 "당일"의 기준 — 자정인가, 정산 마감 시각인가
4. 미완결 거래가 KSNET에서 자동으로 정리되는 기준이 있는가

---

# [7] 마무리

- 커밋하고 푸시하세요
- 마지막에 한국어로 알려주세요:
  1) **실행해야 할 SQL 파일명과 순서**
  2) [1] 조사 결과 — **특히 결제 Key 저장 여부**
  2-1) **무통장 자동취소가 `payment_method`로 거르고 있었는지**, 아니었다면 무엇을 고쳤는지
  3) `vercel.json` cron을 어떻게 분리했는지, 무통장 쪽에 영향이 없음을 어떻게 확인했는지
  4) 기존 상태값과 [2] 표의 대응 관계
  5) 관리자 어디에 새 항목이 생겼는지 (메뉴 경로를 정확히)
  6) 지금 `결제대기`로 남아 있는 테스트 주문들을 어떻게 할지 제안
  7) 3-9(다시 결제하기)를 만들었는지, 안 만들었다면 이유
  8) 테스트로 확인한 것과 못 한 것

## 질문

확실하지 않은 것은 임의로 결정하지 말고 전부 여기에 남길 것.
