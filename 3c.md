# JZL CLOSET 3단계-C 작업 명세

이 파일의 내용을 전부 읽고 아래 명세대로 구현해 주세요.
기존 프로젝트(C:\dev\shop)의 구조·디자인 토큰·코딩 규칙을 그대로 따릅니다.

## 공통 규칙 (반드시 지킬 것)

- 기술 스택: Next.js 14 App Router · TypeScript · Tailwind v3 · Supabase(서울) · Cloudflare R2 · Vercel
- 고객 화면 디자인 토큰: ink #14141A / paper #F6F5F2 / stone #DBD7D1 / muted #55524E / wine #6A2E3C
- 고객 화면 폰트: Pretendard(본문) · Gowun Batang(국문 제목) · Cormorant Garamond(영문 디스플레이)
- **그림자 금지, 이모지 금지**
- 체험단 후기의 `※ 제품을 무상으로 제공받아 작성된 후기입니다` 문구는 표시광고법상 필수입니다. 절대 제거·축소 금지.
- 관리자 쓰기 경로는 전부 서버 라우트 + service_role 유지. anon 키로 쓰지 않습니다.
- 새 SQL이 필요하면 `supabase/schema-3c.sql`, `supabase/rls-3c.sql`로 나눠 만들고 실행 순서를 보고해 주세요.
- **RLS는 모든 테이블에서 켜져 있고, 포인트·번호발급 함수는 service_role 전용으로 잠겨 있습니다.**
  새 함수를 만들면 반드시 같은 방식으로 잠가 주세요 (rls-3b.sql의 pg_proc 조회 방식 재사용).
- Vercel을 곧 Pro(서울 리전 icn1)로 옮깁니다. 리전으로 해결될 지연은 무시하고, **조회 횟수 자체를 줄이는 설계**에 집중.

---

# A. 치명적 버그 — 최우선

## [A-1] 우편번호 검색이 "불러오는 중..."에서 멈춤

### 증상
주문서(비회원·회원 공통) → 우편번호 옆 버튼이 `불러오는 중...` 상태로 멈추고 영영 진행되지 않음.
새로고침하면 정상 동작하지만, **새로고침 시 입력하던 값이 전부 사라짐**.

### 원인 (추정)
주소 검색 스크립트(다음/카카오 우편번호 서비스) 로딩 실패 시 재시도 로직이 없어 영구 대기 상태에 빠짐.

### 조치
1. 스크립트 로딩에 **타임아웃(5초)** 을 두고, 실패 시 자동 재시도 (최대 2회)
2. 재시도까지 실패하면 버튼을 `주소 검색 다시 시도`로 바꾸고 **수동 재시도 가능**하게
3. 그래도 안 되면 안내 노출:
   ```
   주소 검색을 불러오지 못했습니다.
   우편번호와 주소를 직접 입력하시거나, 032-209-1058로 연락 주세요.
   ```
   이때 우편번호·주소 입력란의 readonly를 해제해 **직접 입력 가능**하게 전환
4. 스크립트를 **페이지 진입 시 미리 로드**해 버튼 클릭 시 즉시 열리게 개선
5. 이미 로드된 경우 중복 로드하지 않도록 처리

### 확인
느린 회선(브라우저 개발자도구 Network throttling)에서도 멈추지 않고 재시도·수동입력으로 넘어가야 합니다.

---

## [A-2] 주문서 입력값이 새로고침·이탈 시 사라짐

### 배경
[A-1] 때문에 새로고침하면 입력값이 전부 날아갑니다.
같은 문제가 **뒤로가기·결제 실패·세션 만료** 시에도 발생합니다.

### 조치
주문서 입력값을 브라우저에 임시 저장하고 복구합니다.

- 저장 대상: 주문자·수령인 이름, 연락처, 우편번호·주소·상세주소, 배송 요청사항, 현금영수증 정보
- **저장 금지**: 비밀번호, 카드정보 등 민감정보는 절대 저장하지 마세요
- 저장 시점: 입력 변경 시 (과도한 저장을 막기 위해 디바운스 적용)
- 복구 시점: 주문서 재진입 시 자동 복구 + 상단 안내
  ```
  이전에 입력하시던 내용을 불러왔습니다.  [ 새로 입력하기 ]
  ```
- 삭제 시점: 주문 완료 시 즉시 삭제
- 저장 기간: 24시간 경과 시 자동 폐기
- 회원 로그인 상태면 저장된 배송지 정보를 우선 사용하고, 임시저장은 보조로만 사용

---

## [A-3] 공지·이벤트 팝업 노출 조건에서 시간 제거

### 요구사항
팝업 노출 기간을 **날짜 단위로만** 설정하도록 단순화합니다.

- 관리자 → 팝업 관리에서 **시작 시각·종료 시각 입력란 제거**, 날짜만 남김
- 시작일 00:00:00 ~ 종료일 23:59:59 로 처리 (한국시간 기준)
- 종료일을 비워두면 무기한 노출
- 기존에 시간이 들어간 데이터는 날짜만 남기고 위 규칙으로 변환
- **포인트 팝업(3-B)과 동시에 뜨지 않도록** 우선순위 유지: 공지·이벤트 팝업 먼저, 닫으면 포인트 팝업

### 참고
사용자가 노출 설정을 했는데도 안 뜨는 문제가 있었습니다. 시간 조건이 원인일 가능성이 높으나,
날짜 전용으로 바꾼 뒤에도 **실제로 뜨는지 반드시 확인**하고 결과를 보고해 주세요.

---

# B. 신규 기능

## [B-1] 입금대기 주문 자동취소

### 요구사항
무통장입금 주문이 기한 내 입금되지 않으면 자동으로 취소합니다.

- **기본 기한: 24시간** (관리자 → 설정에서 시간 단위로 조절 가능)
- 기준 시각: 주문 생성 시각
- 대상: 상태가 `입금대기`인 주문만

### 자동취소 시 처리 (반드시 전부)
1. 주문 상태를 `취소완료`로 변경, 취소 사유 `미입금 자동취소` 기록
2. **재고 복구** (차감했던 수량 되돌리기)
3. **사용한 포인트 반환**
4. **적립 예정 포인트는 지급하지 않음** (배송완료 시 지급 구조이므로 해당 없음, 확인만)
5. 주문 상태 이력에 자동취소 기록 남기기
6. 텔레그램 알림 발송 (`자동취소` 표시)

### 제외 조건 (매우 중요)
JZL CLOSET은 위탁배송 구조로, 뉴욕트렌딕에 이미 발송 요청이 나간 건이 있습니다.
**아래 경우는 자동취소에서 반드시 제외**하고, 관리자가 수동으로 처리하게 두세요.
- 송장번호가 입력된 주문
- 관리자가 `발송요청` 또는 그에 준하는 표시를 한 주문
- 관리자가 해당 주문에 `자동취소 제외` 체크를 한 경우 (체크박스 신설)

### 실행 방식
- Supabase pg_cron으로 **10분마다 실행**하는 방식을 권장
- pg_cron 설정이 복잡하면, 관리자 주문 목록 진입 시 만료 건을 처리하는 방식으로 먼저 구현하고
  **어느 방식으로 했는지 보고**해 주세요
- 어느 쪽이든 **함수는 service_role 전용으로 잠글 것**

---

## [B-2] 자동취소 사전 안내

기한을 모르고 취소당하는 일이 없도록 세 곳에 안내합니다.

### ① 주문 완료 화면 (가장 중요)
입금 계좌 안내 바로 아래에 배치:
```
입금 기한 안내

2026년 8월 15일 (금) 오후 2시 30분까지 입금해 주세요.
기한이 지나면 주문이 자동으로 취소됩니다.

남은 시간  23시간 45분
```
- 남은 시간은 화면에서 실시간 카운트다운 (DB 조회 없이 클라이언트 계산)
- 6시간 미만 남으면 시간 표시를 wine 색으로 강조

### ② 주문 확인 텔레그램 알림
기존 알림 내용에 한 줄 추가:
```
입금기한 08/15 14:30
```

### ③ 마이페이지 주문 내역
입금대기 상태인 주문에 기한 표시:
```
입금대기 · 8월 15일 14:30까지
```

### ④ 취소 1시간 전 안내 (회원만)
- 마이페이지 상단에 배너 노출
  ```
  입금 기한이 1시간 남았습니다. 기한이 지나면 주문이 자동 취소됩니다.  [ 주문 확인 ]
  ```
- 비회원은 연락 수단이 없어 생략합니다 (문자 발송은 추후 별도 작업)

---

## [B-3] 리뷰 포토·영상 갤러리 개선

### 현재
리뷰 탭 상단 갤러리에 이미지만 나열되어 있어, 무슨 후기인지 알 수 없고 맥락이 끊깁니다.

### 조치
1. **썸네일 위에 후기 내용 미리보기**
   - 이미지 하단에 어두운 그라데이션을 깔고 흰 글씨로 후기 본문 앞부분 표시 (1~2줄, 말줄임)
   - 별점도 함께 표시 (작게)
   - 그라데이션은 그림자가 아니므로 사용 가능하나, 과하지 않게

2. **썸네일 클릭 시 동작**
   - 확대 보기가 아니라 **해당 후기 위치로 부드럽게 스크롤 이동**
   - 이동 후 해당 후기 카드를 잠시 강조 (배경색 1.5초간 은은하게)
   - 이미지 확대 보기는 후기 카드 안의 이미지를 눌렀을 때만 동작

3. 영상은 썸네일에 재생 아이콘 표시 (SVG, 이모지 금지)

---

## [B-4] 비회원 구매 경로 개선

### 조치
상품 상세 `장바구니 담기` 버튼 아래에 **`비회원 구매`** 버튼을 신설합니다.

- 기존 `바로 문의 032-209-1058` 버튼은 그 아래로 이동 (제거하지 마세요)
- 클릭 시 해당 상품으로 바로 주문서 진입 (장바구니 거치지 않음)
- 디자인: 아웃라인 버튼, `장바구니 담기`(채움)와 위계 구분
- 로그인 상태에서는 `바로 구매`로 문구 변경

---

## [B-5] 가입 유도 포인트 배지

### 요구사항
비회원 구매보다 회원가입을 유도하기 위해, 가입 시 지급 포인트를 눈에 띄게 표시합니다.

### 표시 내용
```
가입하고 3,000P 받기
```
- **금액은 관리자 설정값(가입 축하 포인트)과 자동 연동** — 하드코딩 금지
- 설정에서 해당 항목이 꺼져 있거나 0이면 **배지 자체를 숨김**

### 디자인
- 작은 알약(pill) 형태, 배경 wine `#6A2E3C`, 흰 글씨
- **2.5초 주기로 밝기가 서서히 변하는 은은한 애니메이션** (깜박임이 아니라 숨쉬기 느낌)
- 급격한 점멸 금지 — 브랜드 톤을 해치지 않는 선에서 시선만 끌 것
- `prefers-reduced-motion` 설정 시 애니메이션 정지 (접근성)

### 표시 위치 (3곳)
1. 로그인 화면 — 로그인 버튼 **위**
2. 장바구니 — 비회원 상태일 때 주문 버튼 근처
3. 주문서 — 비회원으로 진입했을 때 상단

2·3번은 전환이 가장 잘 일어나는 지점이라 함께 넣습니다.

---

## [B-6] 로그인 진행 표시

### 배경
구글 로그인 시 응답이 느려 사용자가 멈춘 것으로 오해합니다.

### 조치
- 소셜 로그인 버튼 클릭 즉시 **버튼을 로딩 상태로 전환** (`연결 중...`) 및 중복 클릭 차단
- 3초 이상 걸리면 안내 추가: `잠시만 기다려 주세요. 곧 연결됩니다.`
- 15초 이상이면 실패로 간주하고 재시도 버튼 노출
- 이메일 로그인·회원가입·주문 제출 버튼에도 동일한 중복 클릭 차단 적용

---

# C. 관리자 화면 개편

## [C-1] 상품 등록 이미지 잘림 수정

### 증상
관리자 상품 등록·수정 화면에서 업로드한 이미지가 잘려 보입니다.

### 조치
1. **원본 비율을 유지**하도록 미리보기 영역 수정 (`object-fit: contain` 방향)
2. 세로로 긴 상세페이지 이미지도 전체가 보이게 처리 (최대 높이 제한 + 스크롤 또는 축소)
3. 썸네일 미리보기는 **실제 고객 화면에서 잘리는 범위를 그대로 보여주고**,
   그 옆에 원본 전체 미리보기를 함께 표시해 어디가 잘리는지 알 수 있게
4. 업로드 직후 **원본 크기(px)와 용량**을 표시
5. 고객 화면에서도 상세설명 이미지가 잘리는지 점검하고, 잘린다면 함께 수정
6. 이미지 순서 변경 시 미리보기가 즉시 반영되는지 확인

---

## [C-2] 관리자 다크모드

- Tailwind `darkMode: 'class'` 방식
- 관리자 헤더 우측에 해/달 토글 버튼
- **기본값은 라이트** (상품 이미지 색감 판단을 위해)
- 선택값을 브라우저에 저장해 다음 접속에도 유지
- 다크 팔레트: 배경 `#0F1117` / 카드 `#161923` / 테두리 `#252A36` / 본문 `#E6E8EC` / 보조 `#9BA1AC`
- 포인트 색은 기존 wine `#6A2E3C` 계열 유지
- 주문 상태 뱃지 색을 라이트·다크 양쪽에서 대비가 확보되게 조정
  - 입금대기 앰버 / 결제완료 블루 / 상품준비중 슬레이트 / 배송중 앰버 / 배송완료 그린 / 취소완료 레드
- **고객 화면은 다크모드를 적용하지 않습니다.** 관리자 영역(/admin)에만 적용하세요.

---

## [C-3] 관리자 폰트 통일

- 관리자 영역 전체를 **Pretendard 단일 폰트**로 통일
- 숫자는 `tabular-nums` 적용해 자릿수 정렬 (금액·건수 표에서 중요)
- **고객 화면 폰트는 절대 변경하지 마세요.** Gowun Batang·Cormorant Garamond 그대로 유지합니다.
  관리자는 업무 도구, 고객 화면은 브랜드 얼굴이라 의도적으로 다르게 갑니다.

---

## [C-4] 관리자 사이드바 그룹화

현재 메뉴가 평면 나열이라 항목이 늘수록 찾기 어렵습니다. 그룹으로 묶고 접기/펼치기를 넣습니다.

```
대시보드
상품 관리      ▸ 상품 목록 / 상품 등록 / 분류 관리 / 브랜드 관리
주문 관리      ▸ 주문 목록 / 송장 일괄등록 / 주문자 목록
고객 관리      ▸ 회원 목록 / 문의 목록 / 리뷰 관리
스토어 관리    ▸ 디자인 관리 / 공지 관리 / 팝업 관리 / 포인트 / 설정
통계
```

- 현재 페이지가 속한 그룹은 **자동으로 펼침**
- 펼침 상태를 브라우저에 저장해 유지
- **미답변 문의·입금대기 건수 뱃지는 접힌 상태에서도 그룹 옆에 표시** (지금 보이는 숫자 유지)
- 아이콘은 SVG 사용, 이모지 금지

---

## [C-5] 관리자 속도 최적화

Pro 전환(서울 리전) 후에도 남는 개선만 적용합니다.

1. **순차 DB 조회를 Promise.all 병렬 처리**
2. **`select('*')` 제거** — 화면에 필요한 컬럼만 조회
3. **건수 뱃지는 count 쿼리**로 처리 (전체 행을 가져와 세지 말 것)
4. **로딩 스켈레톤 추가** — 빈 화면 대신 구조가 먼저 보이게
5. **페이지 전환 시 이전 데이터 유지** — 새 데이터 도착 전까지 화면이 비지 않게
6. 목록 화면의 페이지네이션이 전체 행을 가져오지 않는지 점검

---

# 작업 완료 후 보고할 것

1. 새로 만든 SQL 파일과 실행 순서
2. 자동취소를 pg_cron / 진입 시 처리 중 어느 방식으로 구현했는지
3. 팝업이 실제로 노출되는지 확인한 결과
4. 새로 만든 DB 함수가 있다면 service_role 전용으로 잠갔는지
5. 관리자에서 새로 설정해야 하는 값 목록
6. 빌드 통과 여부와 커밋 해시

---

# 이번 작업에서 하지 않을 것

- PG 결제 연동
- 카카오·네이버 로그인
- 문자(SMS) 발송
- 고객 화면 다크모드
