로그인·회원가입 화면을 가운데 정렬로 바꾸고, 구글 간편로그인을 붙여줘.

질문하지 말고 끝까지 완성하고, npm run build가 통과할 때까지 스스로 고쳐라.

────────────────────────────────
[1] 인증 화면 가운데 정렬
────────────────────────────────
현재 로그인 화면이 왼쪽에 붙어 있다. 가운데로 옮겨라.

대상: /login, /signup, /reset-password, /order-lookup, /inquiry/lookup
그 외 폼 하나만 있는 단독 화면도 같은 규칙을 적용하라.

- 화면 가로 가운데에 카드로 배치. 카드 최대폭 420px, 좌우 여백 px-5
- 세로는 화면 높이 위쪽 1/4 지점에서 시작
  (완전 수직 중앙은 모바일에서 키보드가 올라오면 어색해진다)
- 카드: paper 배경, 1px stone 테두리, 그림자 없음, 내부 여백 px-8 py-10
- 제목·라벨·입력칸·버튼 모두 카드 폭에 맞춰 정렬
- 상단 영문 라벨(LOGIN 등)과 한글 제목도 가운데 정렬
- 하단 안내 문구(회원가입 링크, 비회원 주문 안내)도 가운데 정렬
- 회원가입은 항목이 많으니 카드 최대폭 520px,
  약관 동의 영역은 옅은 회색 박스로 묶어 구분하라
- 모바일(sm 미만)에서는 카드 테두리를 없애고 화면 전체 폭 사용
- 입력칸 높이 48px 이상, 버튼 높이 52px 이상
- 마이페이지는 좌측 메뉴 + 우측 내용 구조를 유지하되
  전체를 max-w-[1100px] mx-auto 로 감싸 가운데 정렬

────────────────────────────────
[2] 구글 간편로그인 ★ 핵심
────────────────────────────────
Supabase Auth의 Google Provider를 이미 활성화해 두었다.
(클라이언트 ID / Secret은 Supabase 대시보드에 등록됨. 코드에 넣지 마라)

■ 버튼 배치
- /login 과 /signup 의 폼 맨 위에 [Google로 계속하기] 버튼을 두어라
- 화면 순서는 반드시 아래와 같아야 한다:

      제목
    ┌────────────────────┐
    │ G  Google로 계속하기 │
    └────────────────────┘
    ────── 또는 ──────
      이메일
      비밀번호
    ┌────────────────────┐
    │       로그인        │
    └────────────────────┘

- 버튼 모양: 흰 배경, 1px 회색(#DADCE0) 테두리, 검은 글씨,
  왼쪽에 구글 G 로고를 인라인 SVG로 직접 그려라(4색 정품 로고 형태),
  텍스트는 가운데 정렬, 높이 52px, rounded-sm
- 버튼 아래 "또는" 구분선: 좌우 1px stone 선 + 가운데 텍스트, muted 색
- 카카오·네이버 버튼 자리를 주석으로 남겨두어라
  (예: {/* TODO: 카카오 로그인 버튼 */})

■ 처리 로직
- supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo } })
- 콜백 처리 라우트 app/auth/callback/route.ts 를 만들어라
  · code를 세션으로 교환하고, 원래 가려던 페이지로 되돌려라
  · 실패하면 /login?error=... 로 보내고 화면에 안내를 띄워라

■ profiles 자동 생성 ★ 중요
구글로 처음 로그인하면 profiles 행이 없다. 콜백에서 없으면 자동 생성하라.
  name  = 구글 프로필 이름 (없으면 이메일 앞부분)
  email = 구글 이메일
  agree_terms / agree_privacy / agree_age14 = true
  agreed_at = now()

★ 그래서 소셜 버튼 아래에 아래 문구를 반드시 표시하라:
  "가입 시 이용약관과 개인정보처리방침에 동의하는 것으로 간주됩니다"
  (이용약관 / 개인정보처리방침은 각각 링크로)

■ 연락처 입력 유도
구글 로그인은 연락처를 주지 않는다. 주문 시 필요하므로:
- 로그인 후 profiles.phone이 비어 있으면
  마이페이지 상단과 헤더 아래에 안내 배너를 띄워라
  "주문을 위해 연락처를 입력해 주세요 → [입력하기]"
- 배너는 닫을 수 있게 하되, 연락처를 넣을 때까지 다시 표시하라

■ 계정 연결
같은 이메일로 이미 이메일 가입된 계정이 있으면 같은 계정으로 연결되게 하라.

────────────────────────────────
[3] 이메일 인증 안내 강화
────────────────────────────────
Supabase의 Confirm email이 켜져 있다. 이메일 가입은 인증 후 로그인된다.
(구글 로그인은 인증이 필요 없다. 바로 로그인된다)

- 이메일 가입을 마치면 /signup/complete 화면으로 이동시켜라
- 그 화면에 아래를 명확히 표시하라:
  · 큰 제목: "인증 메일을 보냈습니다"
  · 가입한 이메일 주소를 그대로 표시
  · "메일함에서 인증 링크를 눌러야 로그인할 수 있습니다"
  · ★ 눈에 띄는 박스(옅은 노란 배경)로:
    "메일이 오지 않았나요? 스팸함(정크메일)을 확인해 주세요.
     네이버·다음 메일은 스팸으로 분류되는 경우가 있습니다."
  · [인증 메일 다시 보내기] 버튼 — 60초 재전송 제한, 남은 시간 표시
  · [메일함 열기] 버튼 — 이메일 도메인을 보고 해당 서비스로 이동
      gmail.com → https://mail.google.com
      naver.com → https://mail.naver.com
      daum.net, hanmail.net → https://mail.daum.net
      nate.com → https://mail.nate.com
      그 외 도메인은 이 버튼을 숨겨라
- 인증 안 된 계정으로 로그인을 시도하면
  "이메일 인증이 완료되지 않았습니다" 안내 + 재전송 버튼을 보여줘라

────────────────────────────────
[4] 비회원 주문 유지
────────────────────────────────
- 비회원 주문은 지금처럼 그대로 유지하라. 어디에서도 로그인을 강제하지 마라
- 주문서 상단에 "로그인하면 배송지가 자동 입력됩니다" 정도의
  가벼운 안내만 두고, 건너뛰기 쉽게 하라
- 로그인 화면 하단의 비회원 주문 안내 문구는 그대로 유지하라

────────────────────────────────
[5] 마무리
────────────────────────────────
- 인증 관련 페이지는 모두 noindex 유지
- npm run build 통과 (Type error·Failed to compile 0건)
- 깃허브에 올려라
- 마지막에 한국어로 알려줘라:
  1) 구글 로그인이 안 될 때 확인할 것 (Google Cloud 테스트 사용자 등록,
     리디렉션 URI, Supabase Provider 활성화)
  2) 이번에 바뀐 화면 목록
